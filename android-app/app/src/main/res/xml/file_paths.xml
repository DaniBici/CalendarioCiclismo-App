<?xml version="1.0" encoding="utf-8"?>
<!--
  Paths exportados por el FileProvider. Actualmente solo se expone la carpeta
  donde FileAssetCache guarda los PDFs / mapas / perfiles descargados del CDN
  R2 para uso offline (context.filesDir/OfflineAssets).
-->
<paths>
    <files-path name="offline_assets" path="OfflineAssets/" />
</paths>
