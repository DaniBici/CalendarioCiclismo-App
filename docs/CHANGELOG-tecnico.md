# Changelog técnico (web + backend + features)

> Registro cronológico de migraciones de BD, fuentes de resultados, saneos de datos y features de web/apps ya implementadas; consolidado el 2026-07-20.
>
> Gran parte de estas entradas tienen además un puntero resumido en la memoria automática (`MEMORY.md`). Este archivo conserva el detalle largo consultable.

---

## Estado / migraciones / fuentes de resultados / saneos


**🏗️ WEB — regeneración solo al crear por primera vez una página de jornada (migración `20260814162106`, 2026-08-14):** se elimina la reconstrucción del artifact tras editar una jornada ya publicada. El panel comprueba la URL canónica con `HEAD` y solo marca `admin_mark_web_pages_dirty()` cuando responde 404; cualquier respuesta distinta o error de red queda sin dispatch. Se retiran los triggers de `start_order_entries` y `race_days.startOrderImportedAt`, y los workflows UCI dejan de lanzar `build-site.yml` tras nuevos resultados. Notas, TV, assets, startlists, órdenes de salida y clasificaciones se leen en vivo desde Supabase una vez creada la página. Las skills `cc-*` reservan `admin_trigger_web_pages_workflow()` para la creación inicial de una URL ausente. El cron diario y el `workflow_dispatch` manual se mantienen para mantenimiento y recuperación operativa. Migración aplicada a producción.


**▶ RESULTADOS UCI — volcado manual dirigido a UNA etapa («Volcar esta etapa», migración `134`, 2026-07-21):** el botón «Volcar esta carrera» del panel (pestaña Resultados de una jornada) siempre re-volcaba la carrera **ENTERA** — todas las etapas que la fuente tuviera publicadas — porque el `--race-id` del cron va sin `--skip-existing` (forzado manual = re-vuelca todo a propósito). En una grande (Tour, 21 etapas) actualizar la etapa 16 re-escribía las 15 anteriores ya asentadas: minutos de trabajo y de contención de Postgres para nada (Dani lo cazó: el diálogo decía «va a volcar TODAS» y tardaba ~4 min). **Fix — nuevo camino etapa-específico a lo largo de toda la cadena** (panel → RPC → workflow → cron → upsert): (1) **upsert** `uci-results-upsert.mjs` gana `--only-stage N` → en `buildPlan`, tras calcular `stageNumber`, `if (ONLY_STAGE != null && stageNumber !== ONLY_STAGE) continue` descarta el resto de etapas del JSON (ni purga, ni cabecera, ni DELETE+INSERT de filas). Comparación **estricta** → el prólogo (`stageNumber` 0) es válido (`!= null`, no truthiness) y la Final Classification (`stageNumber` null) se excluye salvo que N sea su número. (2) **cron** `uci-results-cron.mjs` gana `--stage N` (SOLO con `--race-id`; sin él se ignora) → pasa `--only-stage N` al upsert en el bucle. (3) **workflow** `uci-results-today.yml` gana el input `stage_number` → con `race_id` presente añade `--stage`. (4) **RPC** `admin_trigger_uci_results_workflow` gana la sobrecarga `(p_race_id text, p_stage integer)` que mete `stage_number` en los inputs del dispatch solo si `p_stage` no es NULL; la sobrecarga `(text)` existente pasa a delegar en ella con `p_stage = NULL` (carrera entera, comportamiento idéntico al anterior). Grants iguales (authenticated + service_role EXECUTE). (5) **panel** `js/panel.js`: `_uciRunCronNow(btn, raceId, stageNumber)` con 3 modos (global / carrera entera / etapa) y nuevo botón **«▶ Volcar esta etapa»** en la sección de la jornada — gateado a fuente automática (`source NOT IN pdf/sportstiming/manual_timing`) y `stageNumber != null`. **Mensajes del panel actualizados** para el nuevo modelo: el `title` y el `confirmDialog` de «Volcar esta carrera» ahora advierten que re-vuelca la carrera ENTERA (lento en carreras largas) y sugieren «Volcar esta etapa» si solo se quiere una. El cron automático de 30 min (sin `--race-id`) NUNCA pasa `--stage` → su comportamiento (skip-existing por defecto) no cambia. **Verificado:** los 10 tests de guards del upsert + 410 web verdes; filtro probado en modo SQL (JSON de 3 etapas → `--only-stage 16` emite solo la 16; prólogo `--only-stage 0` emite solo la 0). Migración 134 aplicada a prod.

**⏱️ RESULTADOS UCI — nueva variante de feed: `ResultValue` = tiempo ABSOLUTO de cada corredor en NOTACIÓN DE PRENSA → sin gaps ni m.t. (fetcher `uci-results-fetch.mjs`, SIN migración, 2026-07-21):** el **Memorial Andrzej Trochanowski 2026** (competitionId `77761`) salió con **TODOS los corredores con el mismo tiempo** (`3h 00'02"`) y **cero diferencias ni m.t.** La UCI publicó ese evento en una variante NUEVA del feed `Results/`: en vez del clásico «`ResultValue` del rank 1 = tiempo absoluto (`3:00:02`), resto = gap con `+` (`+5`/`+1:54`)», mandó el **tiempo absoluto de CADA corredor ya formateado con `h`/`'`/`"`** (`3h 00'02"`, `3h 01'56"`, …). `parseResultValue` (sin `+` → timeText) lo dejó como timeText absoluto en las 155 filas; y las salvaguardas previas **no lo cazan**: `fixDisguisedGaps`/`fixInvertedAbsoluteGaps` y el `deriveGaps` de la web usan `_toSeconds`/`timeToSeconds`, que parten por `:` → el formato de prensa (sin `:`) devuelve `null` → `allTimed` falso → 0 gaps. **Fix:** nuevo parser `_pressToSeconds` (`"3h 00'02\""` → segundos; exige ≥1 marcador `h`/`'`/`"` para no colar un entero suelto) + nuevo post-proceso `fixPressFormattedAbsolute(rows, isTeamEvent)` encadenado tras `fixInvertedAbsoluteGaps`: si el ganador y TODOS los rank>1 clasificados traen timeText en notación de prensa (y ninguno trae gap explícito ni `:`), deja al ganador su timeText y reescribe el resto como `gapText = _secsToGap(abs − ganador)` → el grupo del ganador queda a `+0` (la web/apps lo pintan **m.t./s.t.** vía `formatGap("+0:00")→"+0\""`), los rezagados con su diferencia real (`+1'54"`, `+4'30"`), DNF/DNS intactos. No toca el formato clásico ni las cronos (donde cada uno tiene su tiempo real y no hay grupo). **Datos:** re-volcado el evento con `--apply` (155 filas, 155 enlazadas). Verificado en BD (ganador `3h 00'02"`; 118 a `+0:00`; cola `+1:54`…`+4:30`) y la cadena de render de `resultados.js` reproducida en node. +9 tests en `uciResultsFetch.test.js` (410 verdes). ⚠️ Vigilar si más carreras llegan en esta variante (el cron ya la normaliza al re-volcar). Ver memoria `project_uci_press_absolute_format`.

**⏱️ ORDEN DE SALIDA — guardar una jornada borraba el asset `startOrder` y la dejaba sin badge (web/panel, SIN migración, 2026-07-19):** el Tour de Francia 2026 **etapa 16** (CRI) no mostraba el badge «Orden Salida» en NINGUNA plataforma, pese a tener sus 166 `start_order_entries` y su `startOrderImportedAt` correctos (el import había ido bien: ambos timestamps coinciden al segundo). **Lo que faltaba era la fila `assets` de tipo `startOrder`** — que es lo que las 3 plataformas usan como FLAG para pintar el badge (`js/app.js` `_startOrderAsset`, iOS `RaceCardView.hasStartOrder`, Android idem), aunque la navegación sea nativa y esa URL no se llegue a abrir nunca. **Causa raíz — `saveRaceDay` reconstruye los assets DESDE EL DOM:** borra TODOS los de la jornada y reinserta los `.asset-url-input` con valor (`js/panel.js`, bloque «Guardar assets»). Eso es correcto para los documentos que el editor renderiza siempre, pero el asset `startOrder` **no lo crea el editor: lo inserta el importador de orden de salida** (`attachImportHandler`). El auto-añadido del editor (`if (rd.startOrderImportedAt && !existing.find(...))`) mete la fila en el **formulario** con `id: ''` — es un placeholder VISUAL, no persistido. Así que basta con guardar la jornada desde un editor cuyo `#assetsList` no tuviera cableada esa fila (pestaña no abierta, editor montado antes del import…) para que el DELETE se la lleve y el INSERT no la reponga: el badge desaparece y `startOrderImportedAt` + los entries siguen intactos, que es lo que despista al diagnosticar. **Fix (2 partes, ambas en `js/panel.js`):** (1) el bloque de guardado **preserva** el `startOrder` existente en BD cuando no hay ningún input suyo en el DOM (re-lee `type, sourceType, url` del asset viejo y lo reinserta, conservando `sourceType`); (2) para no resucitar un borrado deliberado —el botón ✕ hace `row.remove()`, así que «borrado a propósito» y «el editor nunca lo renderizó» eran el MISMO estado en el DOM— el handler del ✕ apunta el tipo en `#assetsList[data-removed-types]` y el guardado lo respeta; el importador **limpia esa marca** (importar deshace un borrado previo de la misma sesión). El tombstone vive en el `#assetsList`, que se re-renderiza entero por jornada → no se filtra entre editores. **Parche de datos:** insertada a mano la fila que faltaba en la etapa 16. **Barrido:** era la ÚNICA jornada afectada de toda la BD (`startOrderImportedAt IS NOT NULL` sin asset `startOrder`) → 0 limpieza adicional. ⚠️ **NO se tocó el gate del badge en las apps** (evaluado y descartado hoy): derivarlo de `startOrderImportedAt` en vez del asset sería más robusto, pero ese campo **no existe en los modelos de iOS/Android** (ni en `RaceDay.swift` ni en los DTO Kotlin) → exigiría añadirlo a modelos, `CodingKeys`, `select` de las queries y revisar Room, con bump de las dos apps, para tapar un síntoma cuya causa ya queda cerrada aquí. Si algún día se toca, hacerlo en las 3 a la vez. Verificado: 404 tests web verdes + 7 casos de las ramas del guard (preserva / ✕ no resucita / ✕ de otro tipo no afecta / fila editada gana / no inventa sin previo) + **badge «ORDEN SALIDA» confirmado en la card de producción** enlazando a `/orden-salida/tour-de-francia-2026-etapa-16/`.

**🏴 RESULTADOS — el override de país por jornada se aplica aunque la clasificación in-house NO traiga `raceDayId` (web + apps, Android `355` / iOS `1158`, SIN migración, 2026-07-18):** una etapa disputada en un país distinto al de la carrera (p. ej. **Giro della Valle d'Aosta 2026 et1 = Passy → Passy Plaine Joux, en FRANCIA**; carrera italiana, `race_days.countryCode='FR'`) mostraba la bandera del país de la CARRERA (Italia) en el feed «Últimos resultados» y en la cabecera del detalle. **Causa:** tanto el feed (`entryRd`) como la cabecera del detalle resolvían la jornada de la clasificación **SOLO por `race_uci_stages.raceDayId`**, que está **NULL** cuando el volcado in-house precedió a la creación de la jornada (condición ya documentada del panel). Sin jornada resuelta, `rd?.countryCode` cae a `race.countryCode`. El fix de render anterior (iOS `1154`) tocó el `??` pero no la resolución de `rd` → seguía nil. **Fix (3 plataformas, mismo patrón):** resolver la jornada por **`(raceId, stageNumber)`** cuando falta `raceDayId` — fuente única `rdFor(s)`/`rdForStage` que encadena `raceDayId → (raceId,stageNumber) → jornada única (un día)`. Feed: `ResultsFeedLogic.buildEntries` (Kotlin/Swift/`js/resultados-feed.js`) gana el mapa `rdByRaceStage`. Detalle: iOS `UciResultsData` gana la lista `raceDays` (todas las jornadas, con countryCode) y el header se resuelve **en memoria** por raceDayId→stageNumber (adiós al fetch `raceDayById` por etapa); Android igual (`data.raceDays` + `daysById`/`daysByStage` en `ResultsScreen`); web `resultados.js` toma el raceDayId de `dayByKey.get(activeKey)` cuando la etapa no lo trae. **Inofensivo si `raceDayId` está poblado** (es un fallback: `raceDayId` gana siempre → separa dobles sectores 3A/3B). ⚠️ Un doble sector SIN raceDayId caería a un sector arbitrario por stageNumber, pero mismo día/país → irrelevante, y esa rama solo actúa sin raceDayId. +2 tests en `ResultsFeedLogicTest(s)` (Android+iOS); web `node --check` (sin suite para el feed). Compile NO ejecutado (entorno sin Xcode/Android SDK) → CI/Xcode Cloud.

**🖼️ LOGOS DEL BUNDLE — el build de iOS fallaba con `logo_….webp couldn't be opened` (folder reference + fetch en CI, iOS `1157`, 2026-07-18):** al mover los logos de carrera a `.gitignore` (preparación del repo público, mismo día) el checkout de Xcode Cloud se quedó con `BundledLogos/` VACÍO, pero el `.pbxproj` seguía **enumerando ~500 logos uno a uno** (`PBXFileReference`+`PBXBuildFile`+`Resources`) → cada fichero ausente = error `The file logo_<hash>.webp couldn't be opened because there is no such file` y build roto. **Fix de raíz:** `ios-app/CalendarioCiclismo/BundledLogos/` pasa a ser **REFERENCIA DE CARPETA** en `project.yml` (`type: folder`, excluido del escaneo recursivo) → el `.pbxproj` tiene **UNA** entrada de carpeta (surgido a mano en el pbxproj committeado + `project.yml` para que xcodegen lo reproduzca) → el build empaqueta lo que HAYA en disco, sin listar ficheros → nunca vuelve a romperse cuando un `logoUrl` cambia de hash o se purga. **De dónde salen ahora los logos** (ya NO se commitean, son obra de terceros): se reconstruyen desde `races.logoUrl` en Supabase — local con `node scripts/fetch-logos.mjs`, en Xcode Cloud con un paso **no bloqueante** de `python3` en `ci_scripts/ci_pre_xcodebuild.sh` (si falla, las apps caen a la capa 3 = red), y el bundle optimizado (192 px + pngquant/oxipng) sigue en `bundle-logos.yml` (cron OFF, `workflow_dispatch`, publica **artefacto** en vez de commitear). Android no sufría el bug (los `assets/` se empaquetan como carpeta). ⚠️ **NO volver a enumerar los logos en el `.pbxproj`**: si `setup.sh`/xcodegen los lista otra vez, revisar que `- BundledLogos` (exclude) + el source `type: folder` siguen en `project.yml`. Detalle en `docs/memory/race-data.md`.

**🔀 RESULTADOS DE DOBLE SECTOR A/B (etapa partida 3A/3B) — web + apps 4.0 (Android `354` / iOS `1156`), SIN migración, 2026-07-18:** una etapa partida en dos sectores el mismo día (p. ej. Baloise Ladies Tour E3 = 3A por la mañana + 3B por la tarde) se modela en `race_days` como **DOS jornadas que comparten el MISMO entero `stageNumber`** y `dateKey`, distinguidas por `neutralStartTimeUtc` (el sufijo A/B es de runtime, lo asigna `annotateDoubleSectors`). La pantalla de Resultados agrupaba `race_uci_stages` **solo por `stageNumber`** → 3A y 3B caían en la MISMA pestaña "Etapa 3" mezcladas (dos "stage", dos "gc", header de un sector al azar). **La clave para separarlos es el `raceDayId`** (cada clasificación lleva el de SU sector; el panel ya lo usaba así). **Fix (3 plataformas + pipeline):** (1) **Fetcher `uci-results-fetch.mjs`** deja de RENUMERAR los sectores a etapas consecutivas (antes 3A→3, 3B→4, y todo lo posterior +1 — desalineado con el modelo `stageNumber` igual en A/B, era el bug de raíz): ahora ambos conservan el `stageNumber` base (3) y emite `sectorIndex` (0=A,1=B) parseado de "Stage 3A"/"Stage 3B". (2) **Upsert `uci-results-upsert.mjs`** resuelve el `raceDayId` con `… WHERE stageNumber=N ORDER BY "neutralStartTimeUtc" ASC NULLS LAST, id ASC LIMIT 1 OFFSET sectorIndex` (etapa normal = 1 jornada → OFFSET 0 = idéntico a antes). **NO añade columna** (`sectorIndex` solo vive en el JSON intermedio y el SELECT; el esquema de `race_uci_stages` no cambia → las apps 3.2.x en producción decodifican exactamente igual). (3) **Lectura sector-consciente** (fuente única por plataforma): `sectorSuffixMap(days)` (raceDayId→sufijo + `stageNumber` sectorizados) + `resultStageEntryKey(stageNumber, raceDayId, …)` → clave `"final"`/`"3"`/`"3A"` + `parseResultStageKey` — en `js/services/races.js` (web), `UciResultsLogic` (Kotlin/Swift). La agrupación, el selector (cápsula "3A"/"3B"), los slugs (`/etapa-3a/`, `?stage=3a`) y el header pasan a esas claves; `applyCancelledStages` descarta la jornada cancelada **por `raceDayId`** (no por número, que en un doble sector arrastraba al sector hermano) y el aviso de arrastre lleva sufijo. Deep-links: web (`buildInhouseResultsUrl`/`_inhouseResultsUrl` con sufijo), Android (`Routes.results` gana `sfx`), iOS (`ResultsRoute.stageSuffix` + `ResultsView.initialStageSuffix`) — los CTA de la jornada/Hoy/competición pasan `stageSuffix`. **Compatibilidad con el despliegue actual (4.0 tarda semanas):** cero cambios para carreras normales (bytes idénticos en BD), sin migración, y desplegar el código **no reescribe datos ya volcados** (solo un re-volcado aplica el modelo nuevo) → **⚠️ re-volcar carreras con doble sector ya existentes (Baloise) conviene hacerlo tras publicar 4.0**, o asumir que las apps 3.2.x muestran ese único stage fusionado (ya estaba roto ahí: antes salía "3B" como "etapa 4"). Web verificada con `node --check` + 12 tests nuevos en `raceServices.test.js` (394 verdes); Android +9 tests, iOS +9 tests (compile NO ejecutado, sin SDK/Xcode → CI/Xcode Cloud). Ver memoria `project_uci_results_double_sector`.

**🎽 MERCADO — panel equipo-céntrico + plantilla 2027 MATERIALIZADA en el front (web + apps 4.0 —iOS `1165` / Android `360`—, SIN migración, 2026-07-20):** Dani rehízo el editor del panel para que ESPEJE el front (división → equipo → situación de cada corredor) en vez de la lista plana de movimientos, y **adelantó la materialización de la plantilla 2027**. **El front (web+iOS+Android) pasa a leer la plantilla 2027 de `rider_team_affiliations` (year=2027) en vez del roster 2026 (`currentTeamId`)** → un equipo sin afiliaciones 2027 sale VACÍO (decisión Dani: "vacío hasta materializar"; NO hay fallback a 2026; se va poblando equipo a equipo). ⚠️ **`rider_team_affiliations` NO tiene `contractUntil`** (mi lectura inicial de `information_schema` mezcló otra tabla — cazado EN CALIENTE cuando el panel dio `column rider_team_affiliations.contractUntil does not exist`): el contrato se guarda como **FECHAS** — `dateTo='<año>-12-31'` (el "31 de diciembre" que pidió Dani) + `dateFrom='2027-01-01'`; año desconocido → `dateTo` NULL. Front/panel/apps trabajan con el AÑO y convierten (`_affYear`/`_affDateTo` en panel; `affYear` inline en `js/fichajes.js` `loadRoster`, `ridersByAffiliation` en iOS `SupabaseService+Transfers.swift` y Android `SupabaseService.kt`). El `contractUntil` efectivo del corredor viene de la AFILIACIÓN (no de `riders_*.contractUntil`); `teamDetail`/`loadRoster` no cambian su lógica, solo la FUENTE del roster. **Traducción UI→DB de las 4 situaciones** (panel `openTeamSituationEditor`/`_tseSaveTeam`, T=equipo abierto): **Continúa** (+año / check "sin año") → afiliación 2027 (teamId=T, dateTo) + BORRA cualquier `rider_transfers` de T; un corredor solo tiene UNA afiliación/año → `_upsertAffiliation2027` borra las de OTROS equipos. **Duda** (+año opcional) → renewal+doubt (to=T) + afiliación 2027 (sigue afiliado, el front lo saca a "en duda"). **Cambio** (+destino +año) → transfer (from=T, to=nuevo) + BORRA afiliación a T. **Fin de contrato** → BORRA afiliación a T; check "se retira" → retirement (from=T), SIN el check → transfer con `toTeamName='?'` (baja sin destino, `TSE_UNKNOWN_DEST`). **Mercado — URL de equipo con SLUG legible + duda no es «renueva» (2026-07-20):** el `?equipo=` del front pasa del id feo (`team_1776…`) a un **slug del nombre 2027** (`?equipo=visma-lease-a-bike`) — `teamSlug(name)` + mapas `_slugToTeamId`/`_teamIdToSlug` en `loadData`; `openTeam` escribe el slug, `init` lo resuelve (retrocompatible con ids antiguos). **La DUDA ya no se muestra como «renueva con X»** en el listado de auditoría del panel (`renderTransfersList`): una duda es «duda de si sigue o se va», no una renovación → texto neutro «en duda · <equipo>» y etiqueta «Duda» (no «Renovación»). El modelo interno sigue siendo `renewal+doubt` (el CHECK de la 123 no cambia); solo cambia la presentación. La sección «En duda» del front ya era neutra.

**Panel — un corredor = UN SOLO movimiento del mercado (dedup en el editor independiente, 2026-07-20):** al fichar a un corredor desde otro equipo con el editor de movimiento (`openTransferEditor`→`saveTransfer`) el equipo SALIENTE no reflejaba el cambio: seguía mostrándolo como «fin de contrato». CAUSA: `saveTransfer` sincronizaba la afiliación 2027 del DESTINO pero NO borraba el movimiento PREVIO del corredor en el equipo de origen (típicamente un `transfer`/`toTeamName='?'` = fin de contrato), así que la vista de equipo (`openTeam` en `js/fichajes.js`, que lista TODAS las filas con `fromTeamId=T`) lo pintaba a la vez en «Terminan contrato» (fila vieja huérfana) y en «Se marchan» (fila nueva). Mismo origen del bug de dos movimientos coexistentes al mismo equipo (Ben Turner). `_tseSaveTeam` (editor equipo-céntrico) SÍ deduplicaba (`_tseClearRiderTransfersForTeam`); el editor independiente no. **FIX (decisión Dani: «todos menos el guardado»):** helper `_clearOtherTransfersForRider(riderId, keepId)` borra CUALQUIER otra fila de `rider_transfers` del corredor en `MARKET_SEASON` (salvo la que se guarda), cableado en `saveTransfer` (tras el insert/update, ANTES de `_syncSigningAffiliation` para no repisar la afiliación buena) y en `confirmTransferQuick` (al confirmar un rumor sobra el fin de contrato huérfano). Lee `_transfersCache` (estado previo al guardado → un insert nuevo no está en caché, así que `keepId` nunca casa y no intenta borrarse a sí mismo; en un edit la fila SÍ está y queda excluida por `keepId`). **Barrido de datos:** 6 corredores ya tenían 2 movimientos en 2027 (5 con «fin de contrato→? + rumor de fichaje a un equipo»: Arrieta, Herregodts, Molano, Sivakov, Tesfatsion; + Pedersen con 2 rumores) → conservado el más reciente (`announcedAt DESC, updatedAt DESC, id DESC`), borradas 6 filas (backup `market_dup_transfers_bak_20260720`; las 6 conservadas son rumores → 0 afiliaciones 2027 huérfanas). Verificado: 0 corredores con >1 movimiento en 2027. Solo web (panel). `node --check` OK; el panel logueado lo prueba Dani (su auth vive en su navegador).

**Panel — selector de SEXO en el editor de ficha (2026-07-20):** al CREAR una ficha (también desde «+ Crear ficha de …» de Fichajes) el editor `riderEditorBodyHtml` muestra un toggle Masculino/Femenino que fija `_ridersGender` (→ tabla riders_men/women); antes el género lo prefijaba el equipo de origen SIN poder cambiarlo (imposible crear una ficha de otro género → cazado con Thaïs Poirier). Al EDITAR se oculta (lo fija su tabla). Solo web (panel).

**Panel** (`js/panel.js`, `panel/app.html`): `fichajesView` = landing con **WT desplegada** (reusa `renderMarketTeams`; tocar equipo → `openTeamSituationEditor`); editor de equipo en drawer con cabecera "Editar identidad 2027" (→ `openTeamEditorForSeason`, el de chapa/duda de siempre), toggle de 4 estados por corredor 2026, sección "Incorporaciones" con **"Nueva incorporación"** (`openTransferEditor(null, {presetToTeamId, onSaved})` → materializa afiliación + vuelve al equipo), y "Guardar equipo" (lote, solo lo cambiado vs `initial`). La lista plana queda como drawer de auditoría "Ver todos los movimientos" (`openAllTransfersDrawer`). RLS de `rider_team_affiliations`: authenticated escribe ✓. **Orden de secciones (Dani): continúan → en duda → TERMINAN CONTRATO → llegan → se marchan** (en las 3 plataformas). **TERMINAN CONTRATO** = fin de contrato SIN destino (`transfer` con `toTeamName='?'`, sin retirada); las retiradas y los fichajes con destino siguen en «Se marchan». `teamDetail`/`openTeam` parten las salidas con `isContractEnd`. **«En duda» sin badge DUDA** (redundante: ya están bajo esa sección). **Badges + separador (Dani):** el año de contrato y el estado (rumor/duda) van como BADGES al final de la fila (web `.tr-contract` como pill + fuera de `.tr-row__body`; iOS/Android `YearBadge`); en el FEED el nombre se separa del movimiento con un DIVISOR (web `.tr-move::before` línea vertical; apps " · " atenuado). **Contrato SIN "hasta" en front** (web+apps): i18n `until` → solo `{year}` (`js/i18n.js`, `i18n/en.json`); iOS/Android muestran el año pelado (`transfers_until` = `%1$d`). El panel conserva "hasta N" (admin). **Cambio de equipo = confirmado O RUMOR + fecha de confirmación:** el estado "Cambio" del editor gana un check "Rumor" (→ `status='rumor'`) y, si NO es rumor, un date picker "Confirmado el" (→ `announcedAt`, que ordena el feed; un rumor no sale en el feed). **El feed público = SOLO FICHAJES REALES:** `confirmedFeed` exige además `isRealSigning` = `type='transfer'` con destino conocido (`toTeamId` o `toTeamName != '?'`) → FUERA renovaciones, retiradas y bajas sin destino (`transfer` con `toTeamName='?'`); fuente única `isRealSigning` en web/`TransfersLogic` (iOS/Android, +1 test cada uno). **⚠️ CONTINÚAN = solo quien estaba en el equipo en 2026** (`currentTeamId == teamId`), NO todo afiliado 2027 — un fichaje de fuera también tiene afiliación 2027 al destino pero va a "Llegan"; `teamDetail`/`openTeam` filtran `staying` por `currentTeamId`. **La afiliación 2027 solo se materializa con un fichaje CONFIRMADO** (`_syncSigningAffiliation`: confirmed→upsert, rumor→borra; en `saveTransfer`/`confirmTransferQuick`/`deleteTransfer`); un rumor de llegada NO entra en plantilla. El **desplegable de equipo destino** del estado "Cambio" (panel) usa el nombre/categoría de `team_seasons[2027]` (con el que va a correr), no el del catálogo `teams`. En "Se marchan", una **retirada** usa la etiqueta con mayúscula («Se retira» / string `transfers_retired`), distinta del `transfers_retires` en minúscula que el feed usa mid-sentence. Web verificada end-to-end contra prod en navegador (sembrado SQL simulando el panel → continúan/duda/llegan/se-marchan/feed correctos, badges y separador OK; datos de prueba borrados). Android compile+tests verdes (AAB NO generado, Dani no lo quiso); iOS parse-check (sin Xcode → Xcode Cloud). **El panel logueado lo prueba Dani** (su auth vive en su navegador). Ver memoria `project_transfer_market`.

**🎽 MERCADO — la chapa 2027 OCULTA muestra los COLORES ANTIGUOS del equipo (no vacío) + fix de raíz del leak de equipos nuevos (migración 129, web + apps 4.0, 2026-07-18):** cambio de producto (Dani, tras reflexionarlo): mientras la chapa 2027 de un equipo no se publica, en vez de dejar el hueco vacío (comportamiento de `346`/`347`/`1150`/`1151`) se muestran sus **colores ACTUALES** (temporada en curso, 2026) — los que la gente ya conoce — hasta que el panel active los de 2027. **Los equipos NUEVOS (nacidos en 2027) SÍ quedan vacíos** hasta crear su chapa: no hay kit antiguo que enseñar. **El `badgeVisible` NO cambia de significado en BD** (sigue siendo "colores 2027 publicados"); lo que cambia es el FALLBACK cuando está oculto: `colores 2027 → colores 2026 → nada`. **Raíz del problema (lo destapó Dani): los equipos 2027 se creaban mal.** El FK `team_seasons."teamId" NOT NULL REFERENCES teams(id)` obliga a que TODO equipo del mercado tenga fila en `teams` (no existe un `team_seasons[2027]` "suelto"); al crear un equipo nuevo desde «+ Equipo 2027» (editor estándar → INSERT en `teams`), el trigger `sync_team_to_season` (mig. 077) le estampaba una fila `team_seasons[2026]` del año en curso → ganaba una identidad 2026 fantasma que rompía la señal "sin fila 2026 = equipo nuevo". **Fix de raíz — migración `129`:** nueva columna `teams."firstSeason"` (SMALLINT, NULL = preexistente); el trigger NO estampa la temporada del año en curso si `firstSeason` es ESTRICTAMENTE futura → un equipo nacido en 2027 nunca tiene fila 2026, ni aunque se edite luego. Retrocompatible: las 55 filas 2027 sembradas (equipos reales de 2026) tienen `firstSeason` NULL y conservan su fila 2026 → muestran sus colores antiguos. Los clientes **NO leen `firstSeason`**: detectan "equipo nuevo" por la AUSENCIA de fila `team_seasons[2026]`, que el guard garantiza. ⚠️ **Solo 55 equipos sembrados a día de hoy → 0 limpieza de datos** (aún no se había creado ningún equipo nuevo por «+ Equipo 2027»; si se hubiera hecho, habría que borrar su fila 2026 fantasma + poner `firstSeason`). **Panel:** «+ Equipo 2027» fija `firstSeason = MARKET_SEASON` en el INSERT (flag `_newTeamMarketBorn`, reseteado en cada `openTeamEditor`); el checkbox «Mostrar la chapa 2027» → **«Mostrar los colores 2027»** (sin marcar = colores actuales, o vacío si es nuevo); el chip de la lista de Fichajes pasa a tri-estado: `Colores 2027` (accent, publicado) / `Colores 2026` (gris, equipo con fila previa) / `Nuevo · sin chapa` (equipo sin fila previa). **Clientes:** la lógica efectiva vive en una fuente única por plataforma — `badgeSeason(season, prev)` → `TransfersLogic` (iOS/Android, +3 tests cada uno) / `badgeOrPlaceholder` con `_prevColorsByTeamId` (web `js/fichajes.js`): publicado → fila 2027; oculto + fila 2026 → colores antiguos; oculto + sin fila 2026 → nil/''. `MarketData` gana `prevSeasonsByTeamId` (las filas 2026 completas, no solo nombres); la web amplía el `select` de `PREV_SEASON` a las 4 columnas de chapa. `TransfersSeasonBadge`/`SeasonBadge` dejan de auto-gatearse (ya no `isVisible`): el gate `if let/!= null` vive en el call site (mismo motivo de spacing de `347`/`1151`). Android `versionCode 353`, iOS build `1155` (sin `.swift` nuevos → NO requiere `./setup.sh`). ⚠️ **La migración 129 debe APLICARSE a prod antes de desplegar el cambio del panel** (el INSERT de «+ Equipo 2027» escribe `firstSeason`); editar equipos existentes no la necesita. Compilación local NO ejecutada (entorno sin Xcode/Android SDK) → se confía en CI/Xcode Cloud; web verificada con `node --check` + lógica pura testeada en iOS/Android.

**📄 MATCH-REPORT UCI → Storage: arregla la auto-detección de candidatos del panel, ROTA en producción (migración 133, 2026-07-19):** el `match-report.json` del matcher UCI (candidatos de enlace carrera↔competición) se **commiteaba a `main`** dos veces al día por `uci-link-discover.yml` (05:40) y `uci-link-evening.yml` (19:00), y el panel lo leía con una **ruta relativa** al fichero versionado (`UCI_REPORT_URL = '../scripts/results-fetchers/…'`). **Pero `build-site.yml:97` excluye `scripts/` del rsync a `_site`** → en el sitio desplegado esa ruta **no existe**. Verificado contra prod: `/panel/` da **200** y el JSON **404**. O sea: la sección **"Origen UCI"** del editor de jornada y la vista del rail **"Resultados UCI"** llevaban **sin funcionar** desde la migración a Pages-por-artifact — **regresión silenciosa**, y el runbook `deploy-pages-por-artifact.md:180` la selló sin querer al decidir "el match-report se queda commiteándose como hoy" (los comentarios de ambos workflows seguían afirmando que el fichero "se sirve por Pages", premisa ya falsa). Lo destapó una pregunta ESTÉTICA de Dani (quitar de `main` los commits `[auto]` de cara al repo público), no un informe de fallo. **FIX: bucket PRIVADO `uci-reports` de Storage** (mig. 133) — un solo cambio arregla el bug Y quita los commits. **Por qué privado (a diferencia de `route-gpx`, mig. 106):** el report expone el calendario interno y los candidatos sin casar, y su ÚNICO consumidor es el panel, que va **autenticado** (`supabase.auth.getSession()`) → política de SELECT solo para `authenticated`, **sin políticas de escritura** (quien escribe es el workflow con `service_role`, que **bypassa RLS**; `route-gpx` sí las tiene porque allí escribe el panel desde el navegador). Verificado tras aplicar: anónimo **400**, service_role **200**. **Piezas:** `scripts/results-fetchers/upload-match-report.mjs` (REST directo con `fetch` + `x-upsert`, sin añadir `@supabase/supabase-js` a CI — **no había precedente server-side de Storage**, los `.mjs` existentes solo hablan PostgREST; **valida que el JSON parsea y trae los 3 buckets `unique`/`ambiguous`/`none` y ≥1 registro antes de subir**, y **falla ruidosamente** con exit≠0: degradarse en silencio dejaría al panel con un report viejo, que es justo el fallo mudo que esto arregla); los dos workflows cambian el paso de commit por la subida y **bajan `permissions: contents: write` → `read`**; `_loadUciReport` (`js/panel.js`) pasa a `supabase.storage.from('uci-reports').download('match-2026.json')` → el **indexado por `rec.our.id` y los tags de bucket NO cambian**, y el año sale de la constante `UCI_REPORT_YEAR` (⚠️ **mantenimiento anual**: subirla al abrir la temporada, espejo del `ROAD_SEASON` de los scripts). El fichero sale de git (`git rm --cached` + se retira la excepción de `.gitignore:89-90`; la regla genérica `*_run/**/*.json` vuelve a cubrirlo). **NINGÚN script se ve afectado:** `uci-backfill-links.mjs:87` y `uci-link-evening.mjs:143` **generan el report ellos mismos** en el mismo proceso y lo leen del disco del runner — nunca leían la copia commiteada. `SUPABASE_SERVICE_ROLE_KEY` y `SUPABASE_URL` ya existían como secrets (`translate-content.yml:54`) → 0 provisioning. ⚠️ **Orden de despliegue:** migración → **siembra manual del report al bucket** → panel+workflows+`git rm` (si se despliega el panel antes de sembrar, la sección sigue 404 un rato; no hay riesgo de pérdida de datos: el report se regenera entero en cada pasada). ⚠️ **Descartadas** dos alternativas: **rama huérfana de datos** (el repo es PRIVADO → `raw.githubusercontent.com` da 404 sin token, y el token acabaría en `panel.js`, que se sirve al navegador) y **meter `scripts/` en el rsync** (arreglaría el 404 pero no quita los commits, y publicaría 455 KB de datos internos). **Los dos call sites del panel YA mostraban el error de forma visible** (`_uciManualSaveRow` + aviso rojo) — el fallo no era mudo; simplemente nadie reparó en el aviso.

**🧹 REFORMA "ABIERTO" + LIMPIEZA DE CÓDIGO MUERTO + SENTRY/UPTIMEROBOT DESTRUIDOS (migración 127, 2026-07-17):** tres tareas de la preparación del repo público (Dani). **(1) `/abierto.html` suavizado:** la sección "Código abierto (AGPL-3.0)" ya NO afirma "el repositorio es público" → reescrita a **intención a futuro** ("Queremos abrir parte del código bajo AGPL-3.0… actualizaremos esta página cuando el repositorio esté disponible"), en ES (`abierto.html`) y EN (`en/open/index.html`) + metadatos (description/OG/Twitter/JSON-LD) sin "código abierto" como hecho (keywords open source/AGPL conservadas). ⚠️ `en/open/` NO está en `build-i18n-html.py` → editar el EN A MANO en espejo. El resto de la página (transparencia de fuentes/mapas/atribución) se conserva. **(2) Retirada de utilidades de importación de datos ya en desuso** + limpieza de la tabla asociada: **migración 127 DROP de `broadcasts_suggested`** (backup `broadcasts_suggested_bak_drop_20260717`, 360 filas; 0 FKs/vistas/funciones/triggers dependientes verificado). ✅ **Backup RETIRADO por la migración 132** (2026-07-18): nació con RLS OFF y 0 políticas → `anon` leía las 360 filas enteras y el dashboard lo marcaba CRITICAL ("RLS Disabled in Public"), el único advisor abierto. Con el productor y el consumidor ya en desuso, esas sugerencias no se iban a reprocesar nunca. Re-verificado antes del DROP (protocolo de la 111): 0 FKs, 0 vistas/matviews, 0 funciones (`prosrc`), 0 referencias en código. **Resultado: 0 tablas con RLS desactivada en `public`, 0 advisors CRITICAL.** Cuarta tanda de la misma serie (098 → 111 → 126 → 132): cada saneo deja su backup "por si acaso" y nadie vuelve a mirarlo. **SE CONSERVA:** `uci-ingest-riders.mjs` + los 6 scripts UCI (van a `uci.org`/`dataride.uci.ch`) y los seed-*.sql aplicados. 0 punteros colgantes tras limpiar skills + docs (`riders-database`, `broadcasts`, `analytics`). **(3) Sentry + UptimeRobot DESTRUIDOS por completo** (decisión Dani: no se usan, superfluos con ~100 visitas/día). Elimina el `SENTRY_TOKEN` + `UPTIMEROBOT_KEY` que vivían HARDCODEADOS en HEAD de `workers/cyclocal/src/index.js`. Tocado: web (`js/sentry.js` borrado + `shared.js` + comentarios HTML), iOS (SDK SPM + `SentrySDK.start` + `Package.resolved`; pbxproj regenerado con xcodegen → ⚠️ restaurar `CalendarioAnalytics.xcscheme`; build **1153**), Android (dep + manifest + toml; versionCode **349**), ambos Workers (`cyclocal` → puente 301 puro sin toucan-js/proxies/secretos; `og` sin toucan), panel (`health.html` sin Uptime/Sentry), `privacidad.html` (4 menciones a diagnóstico de fallos vía Sentry, documento legal RGPD; `en/privacy` regenerado; el `PrivacyInfo.xcprivacy` de Apple NUNCA declaró crash data → intacto), docs (`observability-setup.md` borrado). El DSN de Sentry es público (no secreto); el token/key vivos eran solo del Worker. ⚠️ **Rotar los tokens en Sentry/UptimeRobot/GitHub lo hace Dani** (ya filtrados a la historia). ⚠️ Desfase: apps con Sentry en stores hasta publicar 1153/349. **Ver memorias [[project_open_source_repo]], [[project_sentry_uptimerobot_removed]].**

**🔑 JWT ANON LEGACY DESACTIVADO + 3 tablas de respaldo con RLS OFF barridas (migración 126, 2026-07-17):** cierre del frente de seguridad que destapó la auditoría del repo para Open Source. **(1) JWT legacy DESACTIVADO** (Dashboard → Settings → API Keys → "Legacy anon, service_role API keys" → **Disable JWT-based API keys**): el `SUPABASE_ANON_KEY` JWT (`eyJ…`) vivía en el historial de git (`ios-app/Config/Supabase.xcconfig`, commit `ef5d88f474e`; el `git rm --cached` de `b826f42c58b` lo quitó del índice pero NO del historial → recuperable por cualquiera que clonara) y **seguía VIVO**. ⚠️ Dato corregido: expiraba el **2036-04-01**, no en 2090 — el inventario leyó el timestamp Unix `2090674674` como si fuera un año. **VERIFICADO tras desactivar**: ese JWT da `401 "Legacy API keys are disabled"` y la publishable actual (`sb_publishable_…`, la de `js/config.js`) da 200; **ninguna build vivía de la legacy** — dispositivos reales siguieron escribiendo (Android hace 1 min, iOS hace 3; 11 y 18 escrituras en la media hora posterior). El botón es **Disable, no Delete** → reversible si algo hubiera fallado. Con esto el blob del historial es dato muerto: publicar el repo ya no expone nada. ⚠️ El dashboard **NO muestra "Last used"** por clave legacy (solo el botón de desactivar), y **los logs de la API de Supabase NO registran qué clave usa cada petición** (verificado: 0 coincidencias de apikey/Authorization/`eyJ`/`role`; solo traen método, path, status y user agent — y `get_logs` topa en 100 filas, que en este tráfico son ~27 segundos). O sea: no hay forma de auditar el uso de una clave legacy; la vía es desactivar y observar. **(2) Migración 126 — DROP de 3 tablas de respaldo** (`blt26_startlist_bak` 124 filas, `blt26_rider_fix_bak` 3, `qinghai2026_sl_dorsal_bak` 11): snapshots jsonb de saneos ya cerrados (Baloise Ladies Tour 2026 + Tour of Qinghai 2026) con **RLS desactivada y 0 políticas** → `anon` las leía enteras. Los advisors las marcaban **CRITICAL** ("RLS Disabled in Public"). Mismo patrón que ya se limpió en la **098** (35 tablas, 18 con RLS OFF) y la **111** (9 más): cada saneo deja su backup "por si acaso" y nadie vuelve a mirarlo. Verificado el protocolo de la 111 antes de borrar: 0 FKs entrantes, 0 vistas/matviews dependientes, 0 funciones que las nombren (`prosrc`), 0 referencias en TODO el código, 0 menciones en CLAUDE.md. **Resultado: 0 advisors "RLS Disabled in Public", 0 ERROR.** Quedan 44 WARN que NO son bugs: `anon/authenticated_security_definer_function_executable` (35) es el diseño de `set_push_subscription_v3`/`resolve_*`/`fold_*` — funciones SECURITY DEFINER que anon DEBE poder ejecutar; + `rls_policy_always_true` (6), `extension_in_public`, `public_bucket_allows_listing` (el bucket `route-gpx`, público a propósito), `auth_leaked_password_protection`. **Ver memoria [[project_push_rls_lockdown]].**

**🔕 PUSH WEB RETIRADO — las notificaciones son producto EXCLUSIVO de las apps (2026-07-17):** decisión de producto (Dani), en la línea del buscador de las apps: **5 suscripciones y CERO altas desde 2026-05-20** (2 meses) no justifican mantener la superficie. Además, `js/push-web.js` era el ÚNICO cliente que atacaba `push_subscriptions` directamente (POST upsert + PATCH `isActive:false`), así que la migración **124** ya le había cortado el registro. **Lo retirado:** `js/push-web.js` (109 L, borrado), el modal push + su script inline de cableado (~103 L ×2, en `index.html` y `en/index.html`), el CSS `.push-modal*` + `.footer-link.is-subscribed` (`css/app.css`), la **VAPID pública de los 3 sitios** (`js/config.js`, `js/config.template.js` y el heredoc de `inject-web-config.yml` — ⚠️ si no se quita del workflow, CI la reinyecta) y el entry de `knip.json`. **Lo que SE CONSERVA:** el botón **"Alertas"** del footer, que ahora abre el **modal de apps** — no es código nuevo: `index.html:222-224` YA hacía eso en móvil (`if (IS_MOBILE) openAppsModal()`), solo se extendió la rama a todos los anchos. De paso, el botón del sitio EN decía "Alertas" (bug preexistente: el push web NO tenía i18n, todo hardcodeado en español incluso en `/en/`) → ahora "Alerts". **⚠️ `sw.js` NO se borra: se convierte en TOMBSTONE.** Los service workers ya instalados SOBREVIVEN al borrado del fichero (el navegador reintenta buscarlo y un 404 no los desregistra de forma fiable ni inmediata) → el SW nuevo hace `self.registration.unregister()` en su `activate` + recarga las pestañas. Borrarlo de verdad, en meses, no semanas. **Las 5 suscripciones: despedida + desactivación.** Retirar la UI sin más las dejaba recibiendo push SIN poder darse de baja (el botón que llamaba a `unsubscribe()` ya no existe) — peor que el estado previo. Se les envió un último aviso ("Las notificaciones, ahora en las apps") desde el panel con la casilla **Web** marcada → **3/5 entregadas**; las 2 restantes fueron **fallos BLANDOS** (verificado: siguen con `failCount=0` y sin `lastFailedAt` → el backend NO las marcó como hard bounce, `send-push:1123`/`:1129`), se asume. Luego `isActive=false` en las 5 (verificado: web 0 activas; **iOS 470 y Android 389 intactas**). **⚠️ GOTCHA del envío manual:** `send-push` **exige un JWT de USUARIO admin** (`auth.getUser()`, `:1364`); la `service_role` SOLO vale para `processScheduled` (`:1344`). Un script con el `.env` da **401** — el envío a personas pasa por el panel a propósito. **⚠️ GOTCHA del panel:** las casillas de Plataformas **sin marcar = TODAS** (864 dispositivos); marcarlas es lo que restringe (`_getSelectedTargetPlatforms` → `targetPlatforms:['web']` → `query.in('platform',…)` `:1069` + segundo filtro al armar `webTokens` `:1088`). El diálogo de confirmación dice el recuento real ("5 dispositivos suscritos (solo Web)") — **leerlo siempre**. **Backend:** `sendWebPush` + su bloque cripto (~235 L, `send-push:637-870`), `'web'` en `VALID_PLATFORMS` y las env vars `VAPID_*` quedan como código MUERTO pero inofensivo (`webTokens=[]` → no envía). Limpieza opcional, sin prisa. El panel conserva la casilla/filtro `web` (genéricos). `manifest.json` no se toca (no declara nada de push; `display:"browser"`, no hay PWA). Ver memoria [[project_web_push_retired]].

**🔒 PUSH_SUBSCRIPTIONS — cierre del acceso anónimo (migración 124 APLICADA; ⛔ la 125 va con la 4.0, 2026-07-17):** las 4 políticas RLS de `push_subscriptions` eran `USING (true)` **sin cláusula `TO`** → aplicaban a `anon`, es decir a **la clave publishable que va embebida en `js/config.js`** y se sirve en cada carga de la web. Cualquiera con las DevTools abiertas podía **leer los 937 `deviceToken`** (con `region`/`countryGroup`/`language`), **apagar las notificaciones de cualquiera** (`UPDATE isActive`) y **borrar la tabla entera** con un `DELETE` sin filtro — irrecuperable: los tokens solo los regenera cada dispositivo al reinstalar. Es el ataque que ya describía la **012** en su cabecera. **NO tenía nada que ver con Open Source ni con el JWT legacy del historial**: lo destapó el ejercicio de preparar la publicación (auditar antes de abrir). **Por qué se pudo cerrar AHORA:** la 012 intentó esto mismo y **rompió producción** (los upserts de las apps mandaban `Prefer: return=representation` → PostgREST hacía un SELECT sobre la fila devuelta → sin política de SELECT para `anon` el upsert entero se rechazaba); la **013** lo revirtió dejando el plan escrito: *"coordinar con una release de las apps que llame a una RPC en vez de hacer upsert"*. **Esa release ya está en producción**: iOS (`SupabaseService.swift:319`) y Android (`SupabaseService.kt:595`) llaman a `set_push_subscription_v3`, que es SECURITY DEFINER, su owner (`postgres`) tiene `rolbypassrls` y la tabla NO tiene `FORCE ROW LEVEL SECURITY` → **bypassa RLS**, así que cerrar la tabla no la afecta. **FASE 1 (124, aplicada):** `SELECT` pasa a `authenticated` (el panel: `js/panel.js:6958`/`11620`/`11814`/`12070`), `INSERT`/`UPDATE` fuera de `anon`, `DELETE` acotado a filtrar por `deviceToken` (lo usan iOS `:336` y Android `:600` en `deleteAllData`, RGPD). ⚠️ **GOTCHA CLAVE — el `GRANT SELECT ("deviceToken")` no es un capricho:** un `DELETE … WHERE "deviceToken" = x` necesita privilegio de **LECTURA** para evaluar su propio filtro; un `REVOKE SELECT` a secas **rompe el borrado RGPD** con `permission denied for table push_subscriptions` (cazado EN CALIENTE con rollback: la primera versión de la migración se lo cargaba). El grant de columna **NO reabre la lectura**: sin política de SELECT para `anon`, la RLS filtra todas las filas igual (verificado: enumerar devuelve **0 filas**). Las dos capas colaboran — el privilegio permite evaluar el `WHERE`, la política impide ver nada. **⚠️ CORTA EL PUSH WEB, asumido con datos:** `js/push-web.js:48,64` es el ÚNICO cliente que ataca la tabla directamente (POST upsert + PATCH `isActive:false`) sin pasar por la RPC → deja de registrar. La web tiene **5 suscripciones y CERO escrituras desde 2026-05-20** frente a 937 tokens expuestos; el arreglo (migrarla a `set_push_subscription_v3` pasándole **`p_categories := ARRAY['general']`** — hoy se apoya en el trigger de categoría por defecto, `push-web.js:34-38`, y la RPC exige el array explícito o se degrada el baseline gratuito) es un commit aparte **sin dependencia de releases**. **VERIFICADO tras aplicar, desde fuera y con las claves reales:** la publishable de `js/config.js` da **401 permission denied** en leer/UPDATE/INSERT; el JWT legacy del historial da 200 pero **devuelve `[]`** (mismo rol `anon` → misma RLS); y **dispositivos reales siguieron registrando** (Android hace 0 min, iOS hace 2 min). **⛔ FASE 2 = migración `125`, ESCRITA Y ENSAYADA PERO NO APLICADA** (`supabase/migrations/125_push_subscriptions_delete_rpc.sql`): RPC `delete_push_subscription(p_token)` SECURITY DEFINER (con `SET search_path` fijo) + DROP de la política de DELETE + `REVOKE ALL` → `anon` se queda **SIN NINGÚN** acceso directo. **Requiere los 2 cambios de código que van en la 4.0** (iOS `:336` y Android `:600`: `.delete()` → `.rpc("delete_push_subscription")`; los llamadores `NotificationManager.swift:215`/`PushNotificationManager.kt:177` no cambian). **ORDEN OBLIGATORIO: publicar 4.0 → esperar adopción → aplicar la 125.** Invertirlo rompe el borrado RGPD de las apps ya publicadas (las <4.0 conservan su DELETE hasta ese punto). Sin prisa: la fase 1 ya mató el ataque escalable (sin SELECT nadie DESCUBRE tokens; solo quien CONOZCA uno puede borrar esa fila). Ensayada en caliente con rollback: DELETE directo denegado ✅, RPC de borrado OK ✅, RPC v3 intacta ✅, fila borrada ✅, 0 hijas huérfanas ✅ (las 4 tablas hijas tienen `ON DELETE CASCADE`, verificado). **Pendiente aparte (sin urgencia):** revocar el **JWT anon legacy** (`ios-app/Config/Supabase.xcconfig` en el historial, commit `ef5d88f474e`; `exp` = **2036-04-01**, no 2090 — el inventario lo leyó mal; sigue VIVO pero solo lee lo que ya es público y no escribe) — runbook `docs/runbooks/rotate-supabase-key.md`, cuyo paso 8 quedó condicionado a un rollout y nunca se ejecutó; comprobar antes que ninguna build vieja lo use.

**🔍 BUSCADOR WEB ARCHIVADO — se retira de todas las superficies, el código se conserva (web, 2026-07-17):** cierra la asimetría abierta al archivar el buscador NATIVO en las apps 4.0 (`archive/buscador-apps-2026/`), que dejó vivo el web a propósito. Decisión de Dani: archivarlo también, **sin borrar código**. **Archivar = dejar de MOSTRAR, no romper**: `/buscar.html` + `/en/search/` + `js/buscar.js` siguen intactos y la página **responde por URL directa y funciona** (verificado: `?q=tour` devuelve resultados reales contra prod, 0 errores de consola) — solo deja de ofrecerse. **Superficies apagadas:** (a) `js/header.js` gana el flag **`SEARCH_ENABLED = false`** que gobierna las DOS entradas del header (lupa `.nav-search-link` del cluster de utilidades y `.nav-menu-search` del menú ☰) — el `SEARCH_SVG` y las `searchHref`/`searchTitle` de `STRINGS` SE CONSERVAN; poner el flag a `true` lo revive entero, en ES y EN, sin tocar nada más; (b) enlace "Buscar" del bloque SEO `.static-prerender` de la home; (c) enlace "Buscar carrera" del 404 (+ su re-etiquetado EN, que quedaba huérfano); (d) entrada `/buscar.html` del **sitemap** en los DOS generadores (`.github/workflows/sitemap.yml` y `scripts/generate-sitemap.sh` — hay que tocar ambos o el cron los reintroduce) → deja de indexarse; (e) línea `/buscar.html` de `llms.txt`. **Las páginas EN se REGENERAN, no se editan a mano**: `python3 tools/build-i18n-html.py` las deriva de las maestras ES (`en/search/` sigue en `PAGES` a propósito: la página archivada debe seguir viva en ambos idiomas). **NO se tocan a propósito:** el `SearchAction`/`urlTemplate` del JSON-LD de la home (es la sitelinks searchbox de Google sobre una página que sigue respondiendo, no una superficie nuestra), el mapeo ES↔EN de `js/lang-switch.js` ni la ruta del Worker `workers/cyclocal` (la URL directa debe seguir resolviendo en ambos idiomas), y `knip.json` conserva `js/buscar.js` (si no, lo reporta como código muerto y alguien lo borra). **NO se redirige `/buscar.html` a la home (evaluado y DESCARTADO, Dani 2026-07-17):** redirigir una URL cuyo contenido no continúa en el destino es el patrón que Google trata como **soft 404** → no preserva ranking y es PEOR SEO que el estado actual (200 con contenido real, fuera del sitemap y sin enlaces: Google la degrada sola y sin errores); además mataría el acceso por URL directa, que es justo lo que "archivar sin borrar" preserva. Una redirección solo tendría sentido el día que se BORRE `buscar.html` — y entonces el sitio para ella es la regla de `404.html` (el precedente es `/corredor/` tras la retirada de las fichas), no un shell tipo `mes.html`. **Esa red de seguridad YA ESTÁ PUESTA** (Dani, mismo día): al final del bloque de fallback de `404.html` (y por tanto de `en/404.html`, que se REGENERA de él), `/buscar` y `/buscar.html` → `/`, `/en/search/` → `/en/`. Va la ÚLTIMA del bloque (solo actúa si ninguna otra regla rescató antes la URL) y **hoy está INERTE**: las páginas existen → 200 → el 404 nunca se sirve. El día que se borren empieza a actuar sola, sin tocar nada. Verificado simulando el borrado con un server que imita a GitHub Pages (sirve `404.html` con status 404): `/buscar.html` y `/buscar` → home, `/en/search/` → home EN, una URL cualquiera sigue viendo el 404 normal; y con la página viva `/buscar.html?q=tour` responde 200 y busca (13 resultados), sin redirección. ⚠️ El buscador web listaba CORREDORES, cuyas fichas públicas se retiraron en junio (`8a8a0bc`) — al archivarlo, ese cabo suelto deja de tener superficie por la que asomar. Verificado en navegador: header sin lupa y menú de 4 entradas (Inicio·Resultados·Calendario·Sobre) en ES y EN, `/buscar.html?q=tour` vivo, 84 tests verdes.

**🔁 MERCADO — DUDAS (corredor y equipo) + fecha ocultable + gestión desde el panel (migración 123, web + apps 4.0, 2026-07-17):** tres huecos del mercado 2027 detectados por Dani al empezar a poblarlo. **(1) `rider_transfers.dateVisible`** (BOOLEAN NOT NULL DEFAULT true): la **carga inicial** mete de golpe anuncios de hace semanas que llenarían el feed de días viejos → con la fecha OCULTA el movimiento **NO sale en el feed de últimos** pero **SÍ cuenta en la vista de equipo** (puebla continúan/llegan/se marchan igual). Es un flag de PUBLICACIÓN en el feed, no una fecha ausente (`announcedAt` sigue NOT NULL: ordena y agrupa). Índice parcial `idx_transfers_feed` (season, announcedAt DESC) WHERE confirmed AND dateVisible. Confirmar un rumor/duda desde el listado **la hace visible** (`dateVisible: true` en `confirmTransferQuick`): confirmar es publicar; si no, quedaría confirmado pero fuera del feed. **(2) `status='doubt'` — CUARTA situación del corredor** (ni continúa, ni sale, ni entra): **SOLO válido con `type='renewal'`** (CHECK `rider_transfers_doubt_check`) porque la duda es siempre sobre una RENOVACIÓN (¿sigue o no sigue?) — una duda sobre ir a OTRO equipo YA se modela como `transfer`+`rumor`, y el CHECK impide meterla mal. Saca al corredor de "continúan" y lo lleva a su sección **"En duda"**, que va **2ª: continúan → en duda → se marchan → llegan** (decisión Dani; los equipos listados son los de **2027**, así que la duda es sobre su continuidad en 2027 → `teamLabel` la trata como cualquier renovación, con el nombre 2027, sin caso especial). **NO sincroniza `contractUntil`** (el guard `status !== 'confirmed'` de `_syncTransferContractToRider` ya lo cubría): una duda no es un hecho y no puede pisar el contrato de la ficha. Fuera del feed, como los rumores. **(3) `team_seasons.continuityDoubt`** (BOOLEAN NOT NULL DEFAULT false): el EQUIPO puede estar en duda (caso Kern Pharma, sin sponsor para 2027) → sigue listado en su división con **chip ámbar-no: VIOLETA "Continuidad en duda"** + **banner** en su vista de equipo. Distinto de la **AUSENCIA de fila 2027**, que sigue significando "no continúa" (señal dura, 122): esto es el estado intermedio. **Color**: violeta `#8B5CF6` propio (`.tr-chip--doubt` / `TRANSFERS_DOUBT_COLOR` / `Color.transfersDoubt`), NO el ámbar del rumor — un rumor es una noticia sin confirmar, una duda es la AUSENCIA de noticia; no deben leerse como el mismo estado. **PANEL (2ª tanda, mismo día):** (a) radio **"Duda"** en el editor de movimiento, **gateado a renovaciones** por `_trRefreshTypeVisibility` (si estaba marcado y cambias de tipo, cae a Rumor; + guard en `saveTransfer` por si se edita el tipo sin tocar el estado) — la UI no deja elegir lo que el CHECK rechazaría; (b) checkbox **"Ocultar del listado de últimos"** junto a la fecha; en la lista, fecha **tachada** = oculta, chip Duda violeta, botón "Confirmar" también sobre dudas, y filtro con **Dudas** + **Fecha oculta** (`hidden` NO es un status: cruza los tres); (c) checkbox **"Continuidad en duda"** en el panel Temporada 2027; (d) **lista de equipos 2027 EN la vista Fichajes** (`renderMarketTeams`, espejo de la pública: 4 divisiones con recuento, chips Duda/Chapa, pista **"· 2026: <nombre viejo>"** cuando hay renombre de sponsor) → tocar una fila abre su editor **enfocado en el panel Temporada 2027** (`openTeamEditorForSeason`: scroll + foco en el nombre) — antes renombrar un equipo 2027 obligaba a salir a la vista Equipos; `_refreshMarketTeamsIfVisible` la repinta al guardar/descontinuar; (e) **"+ Crear ficha de «…»"** en el buscador de corredor del editor de movimiento (con y sin resultados: puede haber homónimos) → abre el **editor de ficha ESTÁNDAR** en drawer nivel 2 precargado con lo tecleado ("Apellido, Nombre" o "Nombre Apellido") y, al guardar, la ficha **queda seleccionada** en el movimiento (hook de un solo uso `_onRiderSavedOnce`, que se limpia al usarse y en `openRosterRiderEditor`). ⚠️ Se REUTILIZA el editor estándar a propósito, NO un mini-formulario: el `id` sale del plegado canónico (`fold_name_rpc`) y las colisiones de `identityKey` siguen ofreciendo FUSIONAR en vez de crear el duplicado que ensucia el catálogo. Verificado end-to-end en navegador contra prod (feed sin la fecha oculta y sin cabecera de día huérfana, secciones en orden con chips, banner, ES+EN, lista del panel con Kern Pharma en duda + renombre simulado; datos de prueba borrados: 0 movimientos, 55 seasons 2027 intactas), CHECK probado en BD con rollback (renewal+doubt entra, transfer+doubt rechazado), Android compile + **22 tests** verdes + AAB, web 84 tests, iOS parse-check. Ver memoria `project_transfer_market`.

**🔁 MERCADO DE FICHAJES — pantalla "Fichajes" en las 3 plataformas + panel (migración 122, apps 4.0, 2026-07-16):** seguimiento del mercado ciclista de la **temporada 2027** (4 divisiones: WT/WWT/PT/PRW). **BD (mig. 122):** tabla **`rider_transfers`** (un movimiento por fila; convención por `type`: `transfer` from→to · `renewal` toTeamId = equipo con el que renueva, from NULL · `retirement` from = equipo que deja; `status` confirmed|rumor; `fromTeamName`/`toTeamName` = texto libre para equipos fuera de catálogo; `contractUntil` año; **`announcedAt` EDITABLE** — hay anuncios previos al 1-ago en que abre el mercado; RLS patrón 096) + **`riders_men/women.contractUntil`** (año, NULL = desconocido; lo muestra "continúan") + **`team_seasons.badgeVisible`** (chapa OCULTABLE por temporada: los kits 2027 no se anuncian hasta dentro de meses → las **55 filas `team_seasons` año 2027 sembradas** copiando la identidad 2026 nacen con chapa OCULTA; el panel la activa en equipos estables) + `today_highlights` admite **`targetType='transfers'`** (destino fijo, calco de `championships` pero visible TAMBIÉN en web). La lista de equipos del mercado lee **`team_seasons[2027]`** → renombres de sponsor y ascensos/descensos 2027 se editan ahí sin tocar `teams` (cuyo trigger `sync_team_to_season` pisa siempre el año en curso). ⚠️ `recompute_current_team()` solo mira el año natural en curso → nada de esto toca `currentTeamId`/2026; la **materialización de plantillas 2027** (afiliaciones year=2027 desde confirmados) es una fase posterior, al cierre del mercado. **REGLAS de producto (Dani):** el feed lista SOLO CONFIRMACIONES (fichajes + renovaciones + retiradas) en cronología inversa; los rumores NO salen en el feed pero SÍ en el detalle de equipo con badge Rumor; una salida registrada (confirmada O rumoreada) saca al corredor de "continúan" y lo pinta como baja (·Rumor) y como alta (·Rumor) en el destino; el contrato de una renovación registrada gana al de la ficha; **el nombre de un equipo depende del LADO del movimiento** — el ORIGEN (de dónde sale un corredor, y el equipo que deja una retirada) se pinta con su nombre de `team_seasons[2026]`, la temporada EN CURSO en la que ese equipo se llama así; el DESTINO y las renovaciones, con el de `team_seasons[2027]` (aquel con el que va a correr). Aplica también a las listas "llegan"/"se marchan" de la vista de equipo. ⚠️ NO vale resolver el origen contra `teams`: su trigger `sync_team_to_season` pisa siempre el año en curso, así que hoy coincide con 2026 por casualidad (verificado: 0 divergencias) y mentirá en cuanto `teams` refleje 2027. Implementación = DOS mapas de nombres por año (web `_teamNamePrev`/`_teamNameById` en `js/fichajes.js`; apps `MarketData.teamNamePrev`/`teamNameById` + `teamLabel(side:)` en `TransfersLogic`), con `teams` como último recurso solo para equipos sin fila en NINGUNA de las dos temporadas. Al abrir el mercado 2028, `PREV_SEASON`/`season - 1` siguen solos a `MARKET_SEASON`. **WEB:** página única `/fichajes/` + `/en/transfers/` (directorios reales con shell propio y SEO completo: meta/OG/hreflang/JSON-LD + sitemap en ambos generadores; **SIN entrada en el menú del header hasta que Dani lo diga** — acceso por cintillo/URL directa), `js/fichajes.js` + `css/fichajes.css` (feed → 4 botones de división → lista de equipos → vista de equipo con continúan/llegan/se marchan; estado compartible `?div=` y `?equipo=`; chapa oculta → hueco circular neutro `.tr-badge-ph`), i18n sección `transfers` (ES+EN), lang-switch mapea ambos sentidos; cintillo web ramifica `transfers` → `/fichajes/` con icono de flechas (customTitle/Logo opcionales). **PANEL:** rail nuevo **"Fichajes"** (cluster Más utilizadas; `switchTab` `fichajes` + `fichajesView`): listado con filtro por estado + buscador, alta/edición en drawer (`openTransferEditor`: buscador de corredor en ambas tablas, selects de equipo filtrados por género + texto libre, tipo/estado/contrato/fecha de anuncio) y botón **"Confirmar"** sobre un rumor (pasa a confirmado con `announcedAt`=hoy, editable); al guardar CONFIRMADO con contrato se sincroniza `riders_*.contractUntil`; el editor de equipo gana el panel **"Temporada 2027"** (nombre/categoría 2027 + **checkbox "Mostrar la chapa 2027 en Fichajes"** + colores 2027 en `<details>` + upsert a `team_seasons` por `teamId,year`) y el editor de ficha el campo **"Contrato hasta"**; el radio `transfers` está en el editor del cintillo. Botón **«+ Equipo 2027»** en la cabecera de Fichajes (equipos que NACEN en 2027: abre el editor de equipo estándar — crear identidad y luego su Temporada 2027; sin startlists 2026 no aparece en ninguna superficie pública del año en curso) y botón **«No continúa en 2027»** en el panel de temporada (equipos que CIERRAN: borra la fila `team_seasons` 2027 — **la AUSENCIA de fila ES la señal**, no hay flag; reversible re-guardando la temporada). ⚠️ Gotcha del shell del panel: toda vista nueva debe añadirse a la **regla de anclaje por IDs de `css/panel.css`** (`#racesView, #teamsView, …`) o se renderiza a `left:0` DEBAJO del rail (cazado con `#fichajesView`: el título salía recortado). **APPS 4.0:** pestaña **Fichajes = 3ª** (Hoy · Resultados · Fichajes · Calendario; icono flechas `CompareArrows`/`arrow.left.arrow.right`) — **Buscar RETIRADO y ARCHIVADO en `archive/buscador-apps-2026/`** (código Android+iOS + `_ARCHIVO.md` con pasos de restauración; el buscador WEB se archivó después, el 2026-07-17 — ver la entrada "🔍 BUSCADOR WEB ARCHIVADO" abajo); pantallas solo-online `ui/transfers/TransfersScreen+TransfersTeamScreen` (Android, rutas `transfers` + `transfers_team/{teamId}`) y `Views/Transfers/TransfersView+TransfersTeamView` (iOS, push por valor `TransfersTeamRoute`); lógica pura espejo **`util/TransfersLogic.kt`** (11 tests) / **`Services/TransfersLogic.swift`** (+`TransfersLogicTests`), const `MARKET_SEASON=2027` (subirla en las 3 plataformas al abrir el mercado 2028); deep link/tab `transfers` (Android `handleDeepLink`; iOS `tabMap` transfers:2, calendar:3, `search`→Hoy para pushes antiguos); cintillo nativo ramifica `transfers` → pestaña Fichajes (las versiones <4.0 lo descartan solas). **Onboarding 4.0: el paso de modo OFFLINE se retiró** en ambas plataformas (la función vive solo en Ajustes; Android borra `OfflineOnboardingScreen.kt` + strings, iOS `OfflineOnboardingView.swift` + el case del enum). Verificado: web end-to-end en navegador contra prod (feed/rumor/renovación/equipo/EN con 3 movimientos de prueba luego borrados), migración aplicada (55 seasons 2027, RLS), Android compile+tests+AAB, iOS parse-check + `./setup.sh` (scheme `CalendarioAnalytics` restaurado). ⚠️ Worktrees: el build Android necesita `local.properties`/`secrets.properties`/`google-services.json` y xcodegen `Config/Supabase.xcconfig` — copiar del checkout principal (mismo patrón que `.env`). Ver memoria `project_transfer_market`.

**Publicar una jornada auto-dispara la regeneración de sus páginas estáticas (web, migración 120, 2026-07-13):** las páginas `/jornada/<slug>/` (+ `inscritos`/`perfil`/`mapa`/`orden-salida`/`resultados` y las EN) son ESTÁTICAS: solo las genera `og-pages.yml` (cron 05:00 UTC) y las indexa `sitemap.yml` (05:20). Al crear una carrera nueva y publicar su jornada con slug (dejarla clicable), su página NO existía hasta el siguiente cron → **GitHub Pages devolvía 404** (cazado por Dani con La Périgord Ladies, La Picto en Nouvelle-Aquitaine y el Memorial Andrzej Trochanowski). FIX: nueva RPC `admin_trigger_web_pages_workflow()` (migración `120`, SECURITY DEFINER, grants a `authenticated`+`service_role` como el resto de RPCs del panel) que replica el patrón de `admin_trigger_uci_results_workflow` (088/099) — lee el PAT de Vault (`github_dispatch_token`) y encola vía `pg_net` un `workflow_dispatch` de **AMBOS** workflows (og-pages + sitemap). `js/panel.js`: `saveRaceDay` la dispara **fire-and-forget** al final del `try` SOLO si `status==='published' && slugVal` (helper `_triggerWebPagesRegen`, non-blocking, no afecta al toast; un fallo de red solo hace `console.warn`). **DEBOUNCE MOVIDO AL SERVIDOR (migración `121`, 2026-07-14):** el diseño original tenía el debounce en el NAVEGADOR (`setTimeout(8s)` en `_webPagesRegenTimer` que al vencer llamaba a `admin_trigger_web_pages_workflow()`). Dos fallos: (a) **fiabilidad** — el timer vive en el navegador; si tras guardar se cambia de jornada, se cierra el editor, se recarga o la pestaña pasa a segundo plano (el navegador congela sus timers), el dispatch NUNCA salía y la página quedaba en 404 hasta el cron diario (cazado con la **Clásica Castilla y León 2026**: se guardó publicada pero su dispatch de 8 s no llegó a ejecutarse — 0 filas en `net._http_response` entre el guardado y el disparo manual de rescate); (b) **coalescencia** — un `setTimeout` por navegador no colapsa guardados espaciados >8 s ni ráfagas repartidas entre pestañas/recargas: una sesión de edición llegó a disparar ~15 runs de og-pages + 15 de sitemap en 30 min (cada run regenera las ~3.300 páginas del sitio). **Nuevo modelo:** el panel solo MARCA "hay cambios pendientes" con la RPC barata `admin_mark_web_pages_dirty()` (instantánea, garantizada DENTRO del guardado — un `UPDATE` de la tabla singleton `web_pages_regen_state` que fija `dirty_since`/`last_touched`). El debounce y el disparo los hace un **pg_cron cada minuto** (`web_pages_regen_tick()`, jobname `web-pages-regen-tick`): si hay marca pendiente Y pasaron **45 s** desde el ÚLTIMO cambio (quiet period → colapsa la ráfaga de etapas en UN solo run tras la última), dispara og-pages + sitemap (vía `_dispatch_web_pages_workflows()`, el POST extraído de la 120) y limpia `dirty_since`. Latencia máxima ~1-2 min (antes 8 s) pero **SIN pérdidas por cierre de pestaña y con coalescencia real**. `admin_trigger_web_pages_workflow()` SE CONSERVA (ahora comparte `_dispatch_web_pages_workflows()` + limpia la marca) para disparos manuales inmediatos. Los dos workflows ya toleran pushes concurrentes a `main` (fetch+`rebase -X ours` en bucle) y tocan ficheros disjuntos (`jornada/…` vs `sitemap.xml`/`atom.xml`) → dispararlos juntos es seguro. Verificado end-to-end contra prod: `admin_mark_web_pages_dirty` → tick devuelve `waiting` dentro del quiet period y `dispatched` (2×204 a GitHub) al cumplirse, marca limpiada. ⚠️ NO cubre el borrado de una jornada (la página huérfana la barre el siguiente cron diario) ni cambios que no pasen por `saveRaceDay` (seeding ad-hoc por MCP/SQL → disparar a mano con `SELECT admin_trigger_web_pages_workflow();` o `SELECT admin_mark_web_pages_dirty();` y esperar al tick).

**Resultados de una ETAPA CANCELADA — pestaña "Etapa" con aviso + generales ARRASTRADAS de la etapa anterior (web, 2026-07-16):** regla de producto (Dani): una etapa cancelada no tuvo clasificación, pero la carrera sigue → su página de resultados muestra (1) pestaña **Etapa** con el aviso "Etapa cancelada" (NO una tabla vacía: "no hay datos" se lee como un volcado que falta), (2) las **generales (gc/points/kom/youth/teams) de la ETAPA ANTERIOR**, porque la clasificación no se movió, y (3) **si no hay etapa anterior con datos, NO aparece ninguna pestaña de general** (solo el aviso). **100% PRESENTACIONAL — NO se vuelca nada a BD** (mismas filas por id de clasificación; calco del precedente Android `311`/iOS `1101`, que ya inyecta las finales bajo la última etapa sin duplicar datos). Implementación en `js/resultados.js`: se cargan las jornadas (`race_days`) ANTES de armar las pestañas — la señal `isCancelledDay` vive en `race_days`, NO en `race_uci_stages`, que es lo único que leía esta página; la etapa cancelada se SINTETIZA (`stagesByNum.set(num, [marcador stage + generales de la anterior])`) e **IGNORA sus propias filas** (si el cron volcó algo antes de la cancelación, no se muestra: no describe una carrera que no acabó). **"Etapa anterior" = la anterior EN ORDEN CRONOLÓGICO** (`dateKey`, luego `neutralStartTimeUtc`) saltando descansos y otras canceladas → cubre sola la regla de Dani para dobles sectores (la anterior de un sector B es su A; la del día siguiente a un doble sector es el B) sin caso especial, porque el orden cronológico YA los distingue (`stageNumber` es el mismo entero en A y B). Avisos nuevos: `.res-cancelled-note` (rojo, lenguaje del banner de la jornada) y `.res-carried-note` ("La clasificación no varía: la etapa se canceló. General tras la etapa N" / EN "Standings unchanged…") — sin él, una GC idéntica a la de ayer se lee como un volcado viejo o roto. **SEO (og-pages.yml + sitemap.yml):** la exclusión `isCancelledDay` de `res_days_by_race` se RETIRA en ambos (su premisa —"una cancelada nunca tendrá clasificación real → no prometerla"— ya no vale: ahora la página tiene contenido real) y se añade la rama `res_cancelled` al texto SEO, que ya no promete "vuelve tras la etapa" sino que cuenta que se canceló y que la general se mantiene. ⚠️ Sin esto, al borrar la última clasificación de una cancelada su `/resultados/` DESAPARECE del generador → 404. **Precedente de datos:** la etapa 6 del Tour of Magnificent Qinghai 2026 tenía una `kom/overall` **VACÍA (0 filas) y BLOQUEADA** (`lockedAt`, del panel) que era lo único que ponía el "6" en el selector y pintaba "No hay datos para esta clasificación" — se borró (OK de Dani); su `kom/stage` con 5 filas reales (`keepForWeb=false`, de la hora que sí se corrió) SE CONSERVA. Verificado contra prod: Qinghai E6 (aviso + General/Puntos/Montaña de la E5 con 97 filas reales y el aviso de arrastre; ES+EN; URL limpia `/etapa-6/`), E5 intacta (129 filas propias, sin avisos) y Vuelta a Murcia E2 (cancelada cuya E1 no tiene clasificaciones → solo el aviso, 0 pestañas). **APPS (Android `343` / iOS `1146`, mismo día): paridad completa.** La lógica vive en el espejo puro `UciResultsLogic.applyCancelledStages(stages, days, raceId)` (Kotlin + Swift, **6 tests nuevos en cada plataforma**: caso Qinghai, sin etapa anterior, ignora sus propias volcadas, salta descansos/canceladas encadenadas, **doble sector A/B**, y no-op sin canceladas). `loadResultsData` (repo Android / `SupabaseService+UciResults` iOS) carga AHORA las jornadas SIEMPRE (`raceDaysByRace`/`raceDays(byRaceId:)`) y aplica la síntesis antes del `guard stages.isEmpty → Empty` (si no, una carrera cuya única cancelada no tiene clasificaciones caía a Empty). Campos SINTÉTICOS nuevos en `RaceUciStage`: `isCancelledStage` + `carriedFromStage` — **fuera de la (de)serialización** (Kotlin `@Transient`; Swift: fuera de `CodingKeys` + init propio) para que no se pidan a PostgREST. ⚠️ La copia arrastrada se re-emite con el `stageNumber` de la CANCELADA (es la clave de agrupación de `stagesByKey`) pero CONSERVA su `id`: las filas se piden por `id` → sigue leyendo las de la etapa anterior, que es justo lo que se quiere. Render: `CancelledStageNotice`/`cancelledStageNotice` (rojo, lenguaje del banner) corta ANTES de pedir filas, y `CarriedStandingsNotice`/`carriedStandingsNotice` encabeza la tabla (string `results_carried_standings` ES+EN con `%1$d`). Verificado en emulador contra prod (Qinghai E6): pestañas Stage/GC/Points/KOM, aviso en Stage, GC de la E5 con el aviso de arrastre + 97 filas con banderas/chapas/gaps.

**Resultados — páginas `/resultados/` adelantadas a la publicación de la jornada, no a la existencia de datos (web, 2026-07-06):** decisión de producto (Dani): igual que las jornadas se publican con tiempo prudencial antes de la carrera, **toda jornada publicada (no descanso, no cancelada) recibe ya su página `/resultados/`**, tenga o no clasificación real volcada (`race_uci_stages.keepForWeb`). Antes, la página solo existía una vez había datos reales — cero anticipación, indexación tardía justo cuando más tráfico de búsqueda hay (minutos tras meta). **`og-pages.yml`/`sitemap.yml`**: `res_by_race` pasa a ser la UNIÓN de `res_real_by_race` (clasificaciones reales, como antes) y `res_days_by_race` (toda jornada de `racedays_all`/`racedays` con `isRestDay=false` e `isCancelledDay=false` — una etapa cancelada nunca tendrá clasificación real que sustituya el placeholder, así que no se le promete una); un flag `has_real` por página decide si el texto SEO es el "oficial" de siempre o una variante "aún no disponible / vuelve tras la etapa" — **misma URL, mismo `<title>`, mismo canonical, mismo `robots: index, follow`** en ambos casos, solo cambia `<meta description>`/cuerpo; cuando el cron UCI vuelca la clasificación real, el siguiente run reescribe esa MISMA página con el contenido rico (sin migración de URL). **`js/resultados.js`**: nuevo estado vacío cuando se pide una etapa CONCRETA (segmento de path o `?stage=`) sin volcar todavía — antes caía en silencio a la última etapa CON datos (contenido engañoso: la URL de la etapa 10 mostraba la etapa 9), ahora avisa de que esa etapa está pendiente y no reescribe la URL; cubre también el caso de la carrera entera sin ningún dato (antes mostraba un genérico "para esta carrera" que perdía el detalle de la etapa pedida); un `?stage=` no numérico se trata como "sin etapa pedida" (degrada con gracia) en vez de mostrar "etapa NaN"; se añadió un `gtag('page_view')` para esta categoría nueva de páginas (si no, quedaba invisible en GA4). **Gating de navegación interna SIN CAMBIOS**: el trofeo/CTA "Ver resultados" (`race-data-modal.js`, `jornada.js`, `campeonatos.js`, `resultados-feed.js`) sigue leyendo `keepForWeb` tal cual — solo se activa con clasificación real (rank 1 existente); mientras no haya datos, la navegación interna sigue cayendo al modal FC/PCS de siempre, y la página adelantada solo es alcanzable por buscador/enlace directo. **Alcance**: se aplica a toda jornada publicada con `raceId` válido, exista o no fuente UCI configurada (`race_uci_links`) para esa carrera — una carrera que nunca llegue a tener resultados in-house queda con una página "aún no disponible" permanente (barato, sin `noindex`, pero revisar si la escala crece mucho). Detalle completo en `docs/memory/seo-og-pages.md`.

**RETIRADA de fichas públicas de corredor y de equipo — reducción de scope (web + apps + BD, 2026-06-29):** decisión de producto (Dani, por desgaste/tiempo con ~500-600 usuarios/día): se ELIMINAN por completo las fichas públicas de corredor (`/corredor/<id>/` + EN `/en/rider/`) y de equipo (`/equipo/<slug>/` + EN `/en/team/`) y la pantalla nativa de ficha de corredor en las apps. **Borrado real, no flag.** Aprovecha el release de apps de esta tarde para romper limpio. **Lo que SE CONSERVA (clave): el CATÁLOGO y el pipeline de poblado intactos** — `riders_*` (id, identityKey, currentTeamId, nationality, birthDate, otherNames), `teams` (categoría/colores/chapa/specialEdition), `team_seasons`, `rider_team_affiliations`, los RPCs `resolve_*` y `fold_*`. La retirada NO toca nada que enlace corredores en startlists/resultados; al contrario, quita LECTURAS (queries de ficha) y ~3.444 páginas SEO. **WEB:** borrados `corredor.html`/`equipo.html` + `js/corredor.js`/`js/equipo.js` + los dirs generados `corredor/` (1309), `en/rider/` (1310), `equipo/` (413), `en/team/` (413); `js/shared.js` → `teamUrl`/`riderUrl`/`isPublicTeamCategory`/`PUBLIC_TEAM_CATEGORIES`/`TEAM_LINKS_ENABLED`/`teamCategoryLabel` eliminados, `teamLinkUrl`/`riderLinkUrl` quedan como **stubs que devuelven null** (los call-sites de inscritos/resultados/orden-salida ya eran null-safe → degradan a texto plano sin tocar 15 sitios); el **buscador** deja de listar corredores (solo carreras); el **tooltip de corredor** (`rider-tooltip.js`) se reduce a SOLO el motivo de abandono (DNF "ABN · etapa 2") — bandera/edad/equipo/"Ver ficha" fuera; SEO: bloque equipos+corredores retirado de `og-pages.yml` (1112-1333) + `git add` sin `corredor/ equipo/`, idem `sitemap.yml` y `generate-sitemap.sh`; 404.html (ES+EN) sin el redirect de `/corredor/`; i18n secciones `rider`/`team` + claves `search.rider`/`startlist.riderView*` fuera; CSS muerto de fichas barrido de `app.css` (~64 reglas) y `buscar.css` (`.buscar-rider-tag`). Verificado en preview con datos reales (Dauphiné 2026): inscritos 22 eq/154 corredores con 0 enlaces + 62 abandonos tachados + tooltip DNF vivo, resultados 91 filas/5 pestañas/filtro por equipo/182 chapas/91 banderas/0 enlaces, orden de salida y buscador OK, 0 errores de consola, 76 tests verdes. Se quitaron además queries de ficha ya inútiles (`curTeamById`/`birthDate`/`currentTeamId` del select de resultados; la query de equipo actual del tooltip en inscritos) → MENOS lecturas. **APPS:** ver tabla de versiones (Android `327`, iOS `1120`). **PANEL:** SIN cambios — los editores de equipo y de ficha de corredor + dupScan + matching SE CONSERVAN (son infraestructura de poblado del catálogo, no fichas públicas); `teams.slug` lo generaba un trigger en BD, `saveTeam` no lo edita. Único residuo inofensivo: deep-link muerto "Editar equipo" (`panel.js:481`) que venía de la página `/equipo/` borrada. **BD — migración 113 (`113_drop_teams_slug.sql`, aplicada):** drop de `teams.slug` + índice `uq_teams_slug` + trigger `set_team_slug` (`trg_set_team_slug`) + función `team_base_slug` (lo único exclusivo de fichas; verificado 0 dependencias, 0 código vivo lo lee/escribe). NO acelera el poblado (slug no es lectura caliente ni índice del path de resolve) — es limpieza cosmética del schema. `teams` 419 filas intactas; folding (`set_team_folded_names`) y sync a `team_seasons` conservados. ⚠️ **Coordinación cross-session:** otra sesión pusheó a `main` en paralelo la inversión de `rider_team_affiliations` (commit `8a8a0bc`, SIN migración numerada → 113 quedó libre); único solape = `js/equipo.js` (ella editó / aquí se borra) → conflicto modify/delete trivial al rebasar (resolver con `git rm`).

**Slug de las jornadas de Campeonatos arreglado + red de seguridad en BD (migración 110, 2026-06-23):** 131 de 281 `race_days` de Campeonatos Nacionales 2026 tenían **`slug` (ES) = NULL** mientras su `race` padre sí tenía slug bien formado → sus URLs caían al fallback feo `/jornada.html?id=<uuid>` (sin SEO, y los **App Links nativos de Android no resuelven** ese formato; mismo síntoma que [[project_android_applink_slug_resolution]] pero por slug ausente, no por slug-como-id). CAUSA: el pipeline de sembrado de CN por MCP "solo datos" (memoria `project_cn_uci_dump_pipeline`) insertaba las jornadas rellenando **`slugEn` pero dejándose `slug` ES en NULL** — al no pasar por el editor del panel (que autorrellena el slug), nadie lo ponía. En una carrera de un día la jornada **comparte el slug del race padre** (invariante que cumplían las otras 349 one-day: `rd.slug == race.slug`). FIX en dos partes: (1) **backfill** — `UPDATE race_days SET slug = race.slug` para esas 131 (0 colisiones —el índice `idx_race_days_slug` no es único—, 0 malformados; `slugEn` NO se tocó: 129 ya tenían su slug EN propio y 2 —Italia línea fem y sub23 fem— lo tienen NULL como su padre; backup en `cn2026_rd_slug_backfill_bak`). Resultado: los 281 CN con `rd.slug == race.slug`, 0 NULL. (2) **Trigger `race_day_inherit_slug_from_oneday_race`** (BEFORE INSERT OR UPDATE OF slug,"raceId" en `race_days`): si la jornada viene con `slug` NULL y el race padre es `one_day` con slug, lo hereda. Red de seguridad universal para cualquier futuro seeding ad-hoc (no solo CN). NO toca stage races (cada etapa lleva su propio slug) ni pisa un slug provisto (verificado con 3 tests: one-day-sin-slug→hereda, one-day-con-slug→conserva, stage-race-sin-slug→NULL). El siguiente run de `og-pages.yml`/`sitemap.yml` generará las páginas `/jornada/<slug>/` de estas 131 jornadas (antes inexistentes). ⚠️ Si el pipeline de CN se repite el año que viene, el trigger ya cubre el agujero, pero conviene poblar `slug` ES al insertar de todos modos (ver memoria del pipeline).

**🔑→∅ Mapa del recorrido: MapTiler → OpenFreeMap SIN CLAVE (web + Android, 2026-06-19):** MapTiler **invalidó ambas claves por exceso de uso** (web `whtuVPma3KkyAJb1yb30` y Android `BX8rhRgiK9MqELSfOZ9e`) → el mapa base salía en gris/403. Migrado a **OpenFreeMap** (`tiles.openfreemap.org`, estilo VECTOR completo, **sin API key**, uso comercial permitido, sin cuota) en AMBAS plataformas, mismos estilos por tema: **`liberty` (claro) / `dark` (oscuro)**. Lo NO-MapTiler ya era keyless y se conserva: **relieve 3D = DEM de AWS Terrain Tiles** (terrarium), **satélite = Esri World Imagery** (`server.arcgisonline.com/.../World_Imagery`, raster gratis sin clave; sustituye al `satellite-v2` de MapTiler). **iOS no se tocó** (usa MapKit/Apple, nunca tuvo clave). **Web** (`js/mapa-pub.js`): el estilo pasó de objeto raster inline a **URL de estilo vector de OpenFreeMap** → reescritura de `initRouteMap`: las capas propias (satélite Esri, DEM, hillshade, sky, recorrido) se añaden ENCIMA del estilo OFM en un `addCustomLayers()` que corre en `style.load` y **se RE-EJECUTA tras cada `setStyle`** (el cambio de tema ya NO es `setTiles` sino `map.setStyle(url)` → reemplaza sources/layers; los marcadores DOM sobreviven y se añaden 1 vez). Estado del toolbar (`mapState {base,dim}`) se conserva y se reaplica tras el reload de tema. `setBase('base')` ahora oculta la capa `sat` (queda el callejero vector debajo); `setBase('sat')` la muestra. **Claves eliminadas de los 3 sitios web** (`js/config.js` + `js/config.template.js` + `inject-web-config.yml`) y `MAPTILER_KEY` ya no se referencia en `mapa-pub.js`; preconnect `api.maptiler.com`→`tiles.openfreemap.org` en `mapa.html` + `og-pages.yml` (`LEAFLET_HEAD`) + las 52 páginas ya generadas (`mapa/**` + `en/route-map/**`, parche directo + se regeneran solas en el siguiente run de og-pages). **Android** (`MapTilerConfig.kt`): se borró `API_KEY` y `styleUrl{Light,Dark}` devuelven las URLs de OpenFreeMap; `MapLibreView` sin cambios (`Style.Builder().fromUri(...)` es drop-in). **versionCode 315→316** (sin tocar `versionName 3.2`); AAB regenerado. Verificado en navegador (Tour 2026 E20 Alpe d'Huez): base claro/oscuro, satélite, relieve 3D, recorrido, marcadores, cambio de tema y toggle satélite OK con 0 claves; Android compila (`compileDebugKotlin` + `bundleRelease` OK). ⚠️ Si OpenFreeMap fallara/limitara en el futuro, alternativas keyless: self-host de tiles (Protomaps en R2) u OSM raster (sin dark mode). El warning de consola `circle-11 could not be loaded` es del sprite de OFM (cosmético, no afecta recorrido/marcadores). Detalle de la migración a MapLibre 3D (anterior) abajo ⬇️.

**⬆️ Mapa del recorrido migrado a MapLibre GL JS — terreno 3D (web, 2026-06-15):** el mapa pasó de **Leaflet 2D a MapLibre GL JS con relieve 3D real**. Toda la cáscara (cabecera con botón volver, hero, route-grid, cajas de puntos clave, SEO/clean-URLs/EN `/en/route-map/`, i18n, control de pantalla completa propio sin Fullscreen API por iOS, cambio de tiles por tema) se mantiene; la lógica de parseo de GPX y proyección de marcadores por km (`parseGpx`/`kmToLatLng`/`markerLatLng`) y los badges `indicatorBadgeSVG` NO cambian (se convierte `[lat,lon]`→`[lng,lat]` en el uso). **Mapa base = MapTiler** (raster `streets-v2`/`streets-v2-dark` por tema + `satellite-v2`; clave WEB propia `MAPTILER_KEY` = `whtuVPma3KkyAJb1yb30`, **distinta** de la del mapa nativo Android `MapTilerConfig.kt` (`BX8rhRgiK9MqELSfOZ9e`); plan Free 100k/mes — **⚠️ la clave web exige `calendariociclismo.app` en Allowed origins del panel MapTiler o los tiles dan 403 en web; ya configurado**). ⚠️ `config.js` lo **regenera CI** (`inject-web-config.yml`, dispara al tocar `config.template.js`) → la clave va hardcodeada EN ESE workflow además de en `config.js`/`config.template.js`, o CI la borra (cazado en el porte). **Relieve 3D = DEM de AWS Terrain Tiles** (terrarium, público/gratis/CORS abierto, no consume cuota MapTiler) + `setTerrain`/`hillshade`/`setSky`; exageración 1.2. Toolbar nuevo dentro del mapa: **Mapa/Satélite · 2D/3D (3D por defecto) · Perfil** (mostrar/ocultar). **Perfil SVG superpuesto** al fondo del mapa a sangre (silueta `buildElevationProfileSVG` `iconsOnly`; viewBox recortado al área de dibujo vía `hoverData` → a todo el ancho y a ras del borde inferior). `mapa.html` + og-pages.yml (`LEAFLET_HEAD`) cargan **MapLibre 4.7.1** (antes Leaflet 1.9.4). `.cc-map*` en `app.css` reescrito (mapa 560px, marcadores a 30px fijos —en MapLibre el pin es la raíz, sin wrapper de Leaflet—, popups `.maplibregl-popup-*`, toolbar, overlay de perfil). El prototipo `/proto/` se borró tras aprobar el porte. **Android NO se tocó en esta sesión**: su mapa nativo MapLibre (commit `5f9559139b`) y su clave antigua `BX8rhRgiK9MqELSfOZ9e` quedan intactos. Detalle del diseño original (opt-in, Storage, proyección, etc.) abajo ⬇️.

**Mapa interactivo del recorrido (Leaflet) — opt-in por jornada (web, 2026-06-15):** página pública nueva `/mapa/<slug>/` (+ EN `/en/route-map/<slug>/`), **gemela de la vista Perfil** pero con un **mapa Leaflet** en lugar del gráfico de elevación. La **línea** del recorrido sale de un **GPX crudo subido a R2** (`assets.calendariociclismo.app/route-{rdId}.gpx`); los **marcadores** (puertos, sprints, sectores) NO salen del GPX sino de `profileSummits`/`profileWaypoints` de la BD (que solo tienen `km`), **proyectados sobre la traza por kilómetro** (escalado proporcional `km_oficial/distanceKm × longitud_gpx` + interpolación lineal entre vértices; error medido 0–65 m = medio segmento). Los marcadores reutilizan **`indicatorBadgeSVG` de `js/elevation-profile.js`** → iconos IDÉNTICOS al perfil (S verde sprint, B amarillo bonificación, círculo rojo con categoría HC/1/2/3/4/M, cronómetro punto intermedio, pavé/sterrato) + salida ▶/meta 🏁. Debajo del mapa van las MISMAS cajas de puntos clave que el perfil (puertos/sprints/sectores). **Opt-in por carrera** = migración `105` (`race_days.routeGpxUrl TEXT`, NULL = sin mapa); subir el GPX del mapa por el panel activa la página. **Botón "Mapa" con prioridad GPX** (espejo de cómo el perfil dinámico gana al asset `profile`): si `routeGpxUrl` → el botón abre `/mapa/`; si no → cae al asset estático `map` (imagen/PDF) como hasta ahora — implementado en `buildActionButtons` (jornada/perfil/etc., `view:'mapa'` excluye su propio botón + `isStageView` lo incluye para "Ir a la etapa") **y** en `buildAssetButtons` de `race-assets.js` (cards de Hoy/competición; badge enlace, no modal). Piezas: `mapa.html` + `js/mapa-pub.js` (clon de `perfil.html`/`perfil-pub.js`; Leaflet 1.9.4 desde unpkg CDN + tiles OpenStreetMap, sin API key), panel (`_mapHandleUpload`/`_wireMapDelete` en `js/panel.js`: sube el GPX crudo con `r2PutObject` → `routeGpxUrl` con cache-buster `?v=ts`; control en el editor de jornada bajo el bloque GPX del perfil), CSS `.cc-map*` en `app.css` (mapa 480px, marcadores con badge SVG, popups con variables de tema), i18n sección `map.*` (ES+EN) + `profile.kmLabel/cat/climbsOne/splitsOne`, SEO (og-pages.yml genera `mapa/<slug>` ×2 idiomas + `git add mapa/`; sitemap.yml entradas 0.5 con `en_url`; 404.html rutea `/mapa/` y `/en/route-map/`; lang-switch.js mapea ambos sentidos). El **CORS del fetch cross-origin del GPX ya funciona** (el host de assets devuelve `Access-Control-Allow-Origin` para GET — verificado en vivo; no requiere tocar infra). El GPX del perfil y el del mapa son **independientes** (puedes tener uno sin el otro), aunque normalmente se sube el mismo archivo: el perfil va destilado a `{km,alt}` en BD, el mapa lee la traza cruda de R2. **Las apps NO consumen esto todavía** (el GeoJSON/GPX en R2 queda listo para un futuro MapKit/Google Maps nativo). Verificado end-to-end con la etapa 20 del Tour 2026 (Alpe d'Huez): carga real de BD, fetch+parse GPX, línea + 7 marcadores con iconos por categoría + 2 cajas de puntos clave, 0 errores de consola, título SEO correcto. **Pensado para carreras MUY selectas** (Tour primero), no todas. Para reponer un GPX: panel → editor de jornada → "Subir GPX del mapa". **⚠️ CORRECCIÓN (2026-06-15, mismo día): el GPX se sirve desde SUPABASE STORAGE, no desde R2.** Al subir GPX reales se descubrió que el proxy nginx de `assets.calendariociclismo.app` devuelve el header `Access-Control-Allow-Origin` DUPLICADO en TODOS los assets (uno del proxy + uno de R2). Las imágenes/PDF se cargan con `<img>`/`<embed>` y no validan CORS, así que nunca dio problema; pero el mapa hace `fetch()` cross-origin del GPX → el navegador lo rechaza con `Failed to fetch` (afecta por igual a los subidos por panel o por terminal; NO es de caché). Solución: **bucket público de Supabase Storage `route-gpx` (migración 106)**, que devuelve `access-control-allow-origin: *` (un solo header) de fábrica, sin proxy. El panel (`_mapHandleUpload`) sube ahora con `supabase.storage.from('route-gpx').upload(..., {upsert:true})` + `getPublicUrl` (ya NO `r2PutObject`), `routeGpxUrl` apunta a `…/storage/v1/object/public/route-gpx/route-{rdId}.gpx?v=ts`. Las 21 etapas del Tour 2026 ya están en Storage y `routeGpxUrl` poblado (los 21 GPX huérfanos del R2 se borraron). `mapa-pub.js` no cambió (solo hace `fetch(routeGpxUrl)`, le da igual el origen). Subida masiva inicial = script puntual con la `service_role` (la Edge Function `r2-upload` exige JWT de USUARIO, no service key → se genera vía Admin API magic-link→verify; Storage en cambio sí acepta la service key directamente). El GPX del perfil va destilado a `{km,alt}` en BD; el del mapa va crudo en Storage. **Las apps NO consumen esto todavía.** Verificado end-to-end con varias etapas del Tour 2026 (1 CRI, 19/20 Alpe d'Huez): mapa carga, línea + marcadores por categoría + cajas, 0 errores. Ver memoria `project_route_map_leaflet`.

**Calendario web fusionado — Mes+Temporada → `/calendario.html` (web, 2026-06-12):** el menú ☰ pasa a **Inicio · Resultados · Calendario · Buscar · Sobre** ("Hoy"→"Inicio"; entradas Mes/2026 retiradas junto a sus labels dinámicos `navMesLabel`/`navTemporadaLabel` de theme.js). Mes y Temporada se fusionan en **`calendario.html`** (+ EN **`/en/calendar/`**) con toggle de subvista en la barra de filtros, espejo del CalendarScreen de las apps 3.1: subvista persistida en localStorage **`cc-cal-subview`** (≙ DataStore `calendarSubview`), deep-link **`?vista=mes|temporada`**, **subvista por defecto = MES** (decisión Dani) y **toggle SIEMPRE fijo arriba a la derecha** de la barra de filtros (último item de la fila 1 + `margin-left:auto`, mismo sitio en ambas subvistas — espejo del action del TopAppBar). La **rejilla de Mes antigua se RETIRÓ** (decisión Dani: nadie la visitaba; `js/mes.js` + `css/mes.css` borrados): la subvista Mes es una **agenda mensual nueva** (`js/calendario-mes.js` + `css/calendario.css`, port 1:1 del `MonthScreen` Android) — sección por día (círculo con el número, hoy relleno en accent, día de la semana), cards de carrera con tinte de marca al 5% (logo+bandera+badge UCI+etiqueta de etapa con (CRI/CRE)+salida›meta), chips de meses ene–dic con el MISMO lenguaje que las píldoras del selector de días de Hoy (sin borde, activo = relleno --accent-dim + texto accent) y autocentrado (⚠️ el offsetLeft del chip se mide contra la fila, no contra el contenedor de scroll → posición vía getBoundingClientRect+scrollLeft) + selector de año + botón Hoy + filtros de categoría con pin, placeholders teóricos (misma heurística de descansos), fila sintética "Campeonatos Nacionales" por día CN → /campeonatos (las CN individuales se filtran), autoscroll a hoy y canonical/SEO por mes (`?mes=YYYY-MM`; acepta `month=` legacy). La subvista Temporada es `temporada.js` INTACTO (import diferido; sus IDs `#temporadaFilters`/`#temporadaContent` viven en calendario.html). Orquestador `js/calendario.js`: lazy-import por subvista, replaceState del `?vista=`, y ⚠️ al activar retira el `.static-prerender` de `#calMain` además del `.loading` estático — desde `97395ca` el prerender es MARCADOR del overlay de carga y aquí sería hijo permanente → el overlay nunca cerraría por observer (el `.loading` de la subvista se inyecta ANTES de retirar nada para no dejar hueco). **`mes.html` / `temporada.html` / `/en/month/` / `/en/season/` = shells de redirección** que conservan params (`month=YYYY-MM`→`mes=`, `year`, `cat`) — NO borrarlos (enlaces históricos/indexados; GitHub Pages no hace 301). Barrido de referencias: breadcrumbs de og-pages.yml (6 líneas `temporada.html`→`calendario.html`; se regeneran en el siguiente run), sitemap.yml + generate-sitemap.sh (entrada única 0.9), 404.html (ES+EN), index.html, lang-switch.js (mapeos nuevos + legacy), back-buttons de jornada.js/competicion.js (`from=temporada|mes` → calendario con `vista`) y JSON-LD de inscritos.js/jornada.js/competicion.js. i18n: clave nueva `cal.noRaces` (ES+EN). `initPhTooltip` de shared.js ganó guard de idempotencia (en /calendario pueden inicializar ambas subvistas). Verificado en preview: ES/EN, claro/oscuro, móvil/desktop, toggle ambos sentidos, persistencia, redirects con params, placeholders (octubre), fila CN, overlay de carga en ambas subvistas.


**Pantalla de carga de marca (web, 2026-06-12):** overlay a pantalla completa (`js/page-loading.js`, importado por `header.js` → activo en TODAS las páginas, también las generadas; estilos `.page-loading` en `app.css` con `--bg`/`--accent` → claro/oscuro gratis): logo + "Calendario Ciclismo" + un **perfil de etapa REAL horneado en el módulo** que se traza en bucle con un punto-"ciclista" recorriéndolo. **5 perfiles reales que se sortean en cada carga** (etapas reina GV 2026 de la BD, simplificados con Douglas-Peucker a ~90-170 pts; La Vuelta 2026 aún sin perfiles → cuando lleguen, añadir uno): Feltre→Alleghe (Giro, 5.000 m+), Bourg d'Oisans→**Alpe d'Huez** (Tour, 5.360 m+), Pau→Gavarnie-Gèdre (Tour), Formia→**Blockhaus** (Giro), Pola de Laviana→**L'Angliru** (Vuelta Femenina); leyenda con la etapa abajo-izda. Reglas (decisión Dani, iterado el mismo día): se muestra **en TODAS las cargas** (es marca, no se racanea), mínimo 600 ms (anti-parpadeo) + fade 300 ms + **SETTLE_MS 350 ms de reposo del DOM antes de cerrar** (la home limpia el listado con innerHTML='' y SIGUE montando tarjetas en pasos async → sin debounce el overlay caía a mitad de despliegue, cazado por Dani; cada mutación re-arma el temporizador y un marcador reaparecido lo anula); el perfil va **pegado bajo el bloque del logo** (`top: calc(50% + 72px)`) y un div `__ground` con el mismo tinte prolonga el relleno hasta el borde inferior. Ocultado **automático sin tocar los JS de página**: MutationObserver sobre el contenedor del marcador estático — `.loading`/`.pfe-loading` en los shells raíz y **`.static-prerender` en las ~5000 páginas generadas por og-pages** (su main trae contenido SEO que el JS reemplaza al hidratar; sin este tercer marcador el overlay NO salía en /jornada//inscritos/… — cazado por Dani) — (cierra cuando no queda ninguno: cubre contenido, vacío y error) + fallback duro 12 s; páginas sin marcador (about/buscar/privacidad/panel) no lo muestran nunca. `prefers-reduced-motion` → perfil estático sin animación. **Cortina anti-flash**: el primer paint puede adelantarse 1-2 décimas al módulo (los module scripts corren tras el parse) → `theme.js` (síncrono pre-paint, mismo mecanismo que el anti-flash de tema) añade `html.cc-booting`, cuyo `::before` (app.css, z-799) tapa la página; `page-loading.js` la retira con el overlay ya montado (o al instante si no hay marcador) y un keyframes-failsafe la autodestruye a los 5 s si header.js no llegara a correr; el panel (`/panel`, sin header.js) queda excluido. ⚠️ Gotchas de render: el punto-ciclista es un span HTML posicionado en **%** dentro de `__svgwrap` (un `circle` dentro del SVG con `preserveAspectRatio="none"` se deforma en elipse, y medir `clientWidth` por frame se rompe con resizes); la línea lleva `vector-effect: non-scaling-stroke`. Añadir/cambiar perfiles = entrada en `PROFILES` (puntos `[km, alt]` + distance/min/max/caption).

**Fichas públicas de corredor + feed "Últimos resultados" (web, 2026-06-11):** los corredores de las **dos primeras divisiones (WT/WWT/PT/PRW)** son clicables y tienen ficha pública en `/corredor/<id>/` (+ EN `/en/rider/<id>/`). **SIN migración**: el `id` de `riders_men`/`riders_women` YA es un slug URL-safe (apellido-nombre por `fold_name`; verificado: 0 ids feos, 0 colisiones cross-tabla en el alcance) y es el mismo valor que `globalRiderId` → enlazado directo. (1) **Ficha** (`corredor.html` + `js/corredor.js`, patrón equipo.js): header con chapa del equipo como avatar (fotos a futuro), nombre, bandera, nacimiento (edad) y equipo enlazado; resultados 2026 por carrera en cronología inversa — **el puesto en la primera columna a la izquierda, pegada la etapa/clasificación** —, clásicas con línea única "Clasificación", vueltas con la **General encabezando (la ÚLTIMA volcada: provisional en carrera, sin etiqueta; SOLO se muestra si el corredor figura en la última general)** + Puntos/Montaña/Jóvenes SOLO finales + etapas en orden inverso con fecha; abandono → chip IRM rojo. **Gate ESTRICTO** por `currentTeamId` → no top-división redirige a home. (2) **Enlaces**: nombre clicable en `/inscritos/` (conserva tachado de abandonos) y en `/resultados/` (filas individuales + sub-filas CRE), gateado por `riderLinkUrl(globalRiderId, equipoActualDelCorredor)` (shared.js; **fix 2026-06-11**: el gate pasó del equipo de la startlist al equipo ACTUAL — `currentTeamId`, el mismo gate estricto de la ficha — porque los devo invitados corren bajo el bloque del senior WT sin tener ficha y se enlazaban a un redirect; afectaba a 263 corredores en 119 carreras); el tooltip gana CTA **"Ver ficha →"** (data-rider-url; en táctil un tap sobre el enlace navega sin abrir tooltip). **`TEAM_LINKS_ENABLED` pasó a `true`** (shared.js): los enlaces de equipo reviven en cabeceras de startlist, órdenes de salida, tooltip y resultados. `/equipo/` enlaza su plantilla a las fichas y gana bloque **"Últimos resultados"** (mejor top-10 de la plantilla por etapa/clásica, 12 entradas). (3) **Feed `/resultados/`** (bare ES; EN `/en/results/` reutiliza su shell): `resultados.js` sin carrera → modo feed (import diferido `js/resultados-feed.js`): SOLO etapas y clásicas (sin generales/secundarias) en cronología inversa agrupadas por fecha, con ganador (`winnerName`; pseudo-fila "Cancelled" filtrada); jornadas concluidas (`isRaceConcluded`, meta+30) SIN volcado → fila con botones FC/PCS (se convierte sola al volcar); ventana 14 días + "Cargar más". (4) **Accesos**: entrada "Resultados" en el menú ☰ (header.js, entre Hoy y Mes, `detectActive` por `/resultados|/en/results`), **chip-trofeo** en la franja del selector de días de la home (app.js `buildDateBar`, etiqueta visible en desktop/solo icono en móvil) y migas "← Todos los resultados" en las páginas de carrera. (5) **SEO**: og-pages.yml genera `corredor/<id>/` ×2 idiomas (~1.300×2; `git add corredor/` añadido al workflow), sitemap.yml + `generate-sitemap.sh` emiten fichas + feed, 404.html rutea `/corredor/` y `/en/rider/` (cubre fichajes antes del siguiente run). **✅ Fase apps 3.1 ENTREGADA (mismo día)**: Mes+Temporada fusionadas en "Calendario" (toggle arriba-dcha ambas), 2ª pestaña Resultados (feed nativo) y ficha nativa enlazada desde startlist/resultados — Android `286` / iOS `1068`, detalle en la tabla de versiones. **Iteraciones del mismo día (2026-06-11, todo en main):** (a) **la HOME conmuta Agenda ⇄ Resultados del día** — el chip-trofeo de la barra de días ya NO navega a /resultados/: pinta los resultados del día seleccionado en `#raceList` (donde van las cards, `renderDayResults` del feed refactorizado a `fetchEntries`+`entryRowHtml` compartidos) y se transforma en "Calendario"; el selector de días navega en ambos modos; filtros ocultos en modo resultados; con los MISMOS tamaños que las cards del calendario (overrides `.feed-day--home`; ⚠️ debe ir DESPUÉS de `.feed-day` en el CSS y con `margin:0` — el `margin: 0 auto` del feed en un flex column cancela el stretch). (b) Filas del feed: logo de carrera (race-logo-img + bandera debajo, como las cards), "Etapa X" y km en negrita, salida›meta + badges de tipo reducidos, ganador a 3ª línea con **nombre canónico en negrita** (rank1→globalRiderId→riders_*; **CRE → nombre del EQUIPO**, señal `primaryType='ttt'` + resolución por startlist), generales FINALES de vueltas como entrada propia POR DELANTE de su etapa (orden del día = canónico de Hoy con hora POR CARRERA-DÍA precomputada — sin ella el comparador no es transitivo y entrelaza bloques), fechas NULL de volcados PDF resueltas por raceDayId, sin etiqueta "Clásica" ni subtítulo. (c) Ficha: fecha POR DELANTE del nombre, recorrido por etapa (desktop) + chip CRI/CRE, general provisional como chip PEGADO al puesto, sin "clásica", y en CRE variante B (corredor con rank NULL) el puesto se reconstruye con el **arrastre por rank** del render TTT (= puesto del equipo). (d) Caché mixta tras deploy (Pages 10 min): el import del feed se auto-cura re-importando con query fresco. (e) Fix global: el menú ☰ ancla a `right: max(1.5rem, calc((100% − 860px)/2))` para caer JUNTO a la hamburguesa (el inner del header está centrado a 860px). Ver memoria `project_rider_pages_results_feed_plan`.

**Rendimiento + seguridad de la BD — paralelización de queries + colapso RLS + limpieza (migraciones 096–098, 2026-06-11):** la web tardaba en cargar datos no por falta de índices (las columnas de filtro —`dateKey`/`editorialStatus`/`slug`/`raceId`/`globalRiderId`/`currentTeamId`— YA estaban indexadas y se usan), sino por tres cuellos sistémicos, todos atacados: (1) **Cascadas de queries en serie en el frontend** → paralelizadas con `Promise.all`. `inscritos.js`: fase A (`race_days`+`startlist_teams`+`race_uci_stages`, solo dependen de raceId) y fase B (`startlist_riders_resolved`+`race_uci_results`) en paralelo, + `teams`/`team_seasons` del enriquecido juntos (de ~9 round-trips a ~5). `corredor.js`: lookup hombres/mujeres en paralelo, equipo+season+resultados juntos, y carreras+jornadas+generales en un lote (de ~9 a ~5). `jornada.js`: `races` metido en el `Promise.all` de broadcasts/assets/uci (las clásicas de un día se ahorran un RT). (2) **Política RLS doble por tabla** (`<x>_write_*` FOR ALL cubría también SELECT → 2 permisivas evaluadas por lectura anónima + `auth.uid()` por fila): migración **096** acota la escritura a INSERT/UPDATE/DELETE (no toca SELECT) y la envuelve en `(select auth.uid())` (initplan) en las 17 tablas calientes; queda 1 sola permisiva de SELECT (`= true`). Sin cambio de comportamiento (anon lee todo; authenticated escribe; service_role salta RLS). Limpia ~100 advisors (0003 + 0006). (3) **Catálogo inflado por 35 tablas de backup del saneo** (snapshots de rollback de las fases dedup + 0610/0611): **097** elimina 2 índices duplicados idénticos (`idx_riders_*_lastname` == `_lastname_lower`) y **098** DROPea las 35 tablas (18 tenían RLS OFF → eran 18 advisors de SEGURIDAD CRITICAL 0013; verificado 0 FKs/vistas entrantes antes de borrar; OK de Dani). **Efecto medido** en `startlist_riders_resolved` por raceId: de **Planning 660–1180 ms / Exec 350–1440 ms** a **Planning 4.7 ms / Exec 17 ms** (~140× planning). El grueso de la lentitud era overhead de planificación (catálogo + expansión de políticas RLS), no I/O ni índices. RLS desactivado en `public` = 0 tras la limpieza. PENDIENTE opcional (no hecho, bajo riesgo/beneficio): índices "no usados" que respaldan RPCs de volcado (`idx_teams_folded_names`/`_name`/`_special_edition_race`) — NO tocados porque las stats pueden estar frías y respaldan `resolve_uci_startlist`.

**Auto-enlazado UCI — pasada de tarde que resuelve rezagados + AMBIGUOS sin panel (migración 107, 2026-06-16):** el enlace carrera↔competición UCI (`race_uci_links`) tenía dos altas automáticas y un hueco. Lo automático: `uci-link-discover.yml` (cron diario **05:40**) corre el matcher (`uci-match-poc.mjs`) contra el calendario UCI y enlaza los `unique ∩ horizonte` (pre-enlace barato y temprano: la UCI publica el calendario con semanas de antelación, así que la mayoría limpia queda enlazada ANTES de correr y el volcado del día la recoge sola). El hueco: (1) **timing** — carreras creadas tarde o competiciones que la UCI publica el mismo día no estaban a las 05:40; (2) **ambigüedad** — los `ambiguous`/colisiones masc-fem NUNCA se enlazaban solos (por diseño, para no enlazar la competición equivocada) y dependían de que alguien entrara al panel a resolverlos. **Solución: nueva pasada de TARDE** (`uci-link-evening.yml`, cron diario **19:00 UTC** — cuando las carreras europeas ya terminaron y hay resultados publicados; el `schedule:` de GitHub SÍ es fiable a cadencia diaria, igual que discover). El pre-enlace de la mañana SE CONSERVA (quitarlo retrasaría a la mayoría limpia de "minutos tras meta" a las 19:00). La pasada de tarde (`uci-link-evening.mjs`): (a) re-corre el matcher con datos UCI frescos → enlaza los `unique` rezagados (idéntico al backfill, `ON CONFLICT (raceId) DO NOTHING`, nunca pisa nada) — esto solo cierra el hueco de timing; (b) para cada `ambiguous`/colisión ya TERMINADO y sin enlace, **valida por STARTLIST**: descarga los participantes de cada candidata (Races→Events→Results de la 1ª etapa, fetch ligero acotado a ~25 corredores) y mide el solape contra la startlist curada de la carrera (`startlist_riders_resolved`, claves de corredor `apellido+nombre` plegadas NFD). Enlaza la candidata cuyo solape ≥ umbral (default **0.5**), con ≥ **5** corredores compartidos y ventaja ≥ **0.2** sobre la 2ª; si ninguna pasa → se deja manual (como hoy). Esto resuelve los pares masc/fem (corredores completamente distintos → solape ~0 en la equivocada) **sin riesgo de falso positivo**: una carrera sin startlist curada NO se auto-resuelve (se deja manual). Tras enlazar, el workflow **vuelca de una** lo recién enlazado del día: `uci-results-cron.mjs --scope today --ignore-window --skip-existing` (la franja de meta normal ya cubrió el resto; `--ignore-window` coge todo lo que tenga etapa hoy aunque su ventana cerrara, `--skip-existing` salta barato lo ya volcado). **Migración 107** = `race_uci_links.matchMethod` (`'unique'` | `'ambiguous-startlist'` | `'manual'` | NULL legacy) para AUDITAR/revertir a ojo los ambiguos auto-resueltos (los delicados); no toca `autoMatched` (sigue alimentando la UI). Cambio mínimo en `uci-results-cron.mjs`: `--skip-existing` explícito fuerza el skip aunque sea `--ignore-window` (antes incompatibles). Mismo `concurrency: uci-results` que los volcados (serializa escrituras UCI → evita transacciones zombi). Args afinables: `--threshold/--margin/--min-shared/--dry-run`. **Pendiente opcional (no hecho):** badge en el panel que muestre `matchMethod='ambiguous-startlist'` para spot-check; afinar umbrales con datos reales (no se pudo probar end-to-end contra la UCI/BD en la sesión — la lógica de normalización de nombres sí está verificada en local). Mantenimiento anual = añadir el `seasonId` del año nuevo a `ROAD_SEASON` también en `uci-link-evening.mjs`.

**Panel — pestaña "Resultados" en el editor de jornada (web, 2026-06-10):** 5ª pestaña del editor de jornada (General·TV·GPX·Documentación·**Resultados**; oculta en jornadas de descanso). (1) **Origen UCI**: ver/editar el enlace `race_uci_links` de la carrera desde la propia jornada — auto-detección con el match-report estático (reusa `_loadUciReport`/`_uciCandidateRow`/`_writeUciLink`/`_deleteUciLink` del editor de carrera, mismo manejo de colisiones masc/fem y de conflicto UNIQUE) + alta manual por competitionId; junto a Cambiar enlace/Desenlazar está **«▶ Volcar esta carrera»** (`_uciRunCronNow(btn, rd.raceId)`; **migración 099, 2026-06-12**) que vuelca SOLO esa carrera ignorando fecha/ventana → trae TODAS las etapas que la UCI tenga publicadas, también las de DÍAS ANTERIORES (cazado en el Tour de Beauce: faltaban resultados de ayer y el viejo «Volcar hoy ahora» —`--scope today`— solo coge carreras con etapa con `dateKey=HOY`, así que nunca los recogía). Implementación: sobrecarga RPC `admin_trigger_uci_results_workflow(p_race_id text)` (099) → input `race_id` en `uci-results-today.yml` → `uci-results-cron.mjs --race-id <id>` (salta la selección por estado/scope; upsert idempotente). El botón global del rail "Resultados UCI" sigue siendo «Volcar hoy ahora» (sin raceId, `ignore_window=true`). **Enlazar por UCI desde el panel fija `source='uci'` (2026-07-15):** `_writeUciLink` NUNCA escribía la columna `source` → una carrera en `source='pdf'` (la fija `uci-results-upsert.mjs --source pdf` al volcar con la skill `cc-resultados-pdf`) seguía siendo saltada por el cron para siempre aunque el panel enlazara un `competitionId` real de DataRide, y el toast mentía diciendo «el cron volcará los resultados en su próxima pasada». Cazado con el **Tour of Magnificent Qinghai 2026** (`cElmxyphJwXoXo617DfD`, comp 77439): etapas 1-3 volcadas solas desde DataRide, la 4 a mano desde PDF → el `--source pdf` apagó el cron y la etapa 5 no se recogía. Ahora `_writeUciLink` lee la fuente actual y escribe `source='uci'` SOLO si es `null`/`'uci'`/`'pdf'` (`UCI_PANEL_OWNED_SOURCES`); las de **cronometrador** (tissot/matsport/sportstiming/manual_timing/raceresult/sts/livetiming) NO se pisan — su `competitionId` es sintético y manda su código propio (`tissotCode`…), pasarlas a `'uci'` las rompería en silencio. El **híbrido Domtel** (`source='uci'` + `domtelCode`) entra en la rama pero es no-op (ya es `'uci'`; su `domtelCode` no se toca). Los CHECK per-columna son unidireccionales (`source <> 'x' OR xCode IS NOT NULL`) → pasar CUALQUIER fuente a `'uci'` nunca los viola. Además la cabecera "Origen UCI" muestra ahora un **badge de fuente** (`UCI_SOURCE_LABELS`) y, si la fuente es de las que el cron salta (`UCI_MANUAL_SOURCES` = pdf/sportstiming/manual_timing — espejo del `NOT IN` de la query + el guard del bucle de `uci-results-cron.mjs`), un **aviso ámbar** de que el volcado es manual (antes el fallo era MUDO: nada en la UI delataba que el cron no la recogía); el toast solo promete el cron si la fuente quedó automática. ⚠️ NO hay selector de fuente en el panel: las de cronometrador se siguen conmutando por SQL/terminal. (2) **Clasificaciones de la jornada**: `race_uci_stages` keepForWeb con `raceDayId` de la jornada (fallback por `stageNumber` si el volcado precedió a la creación de la jornada); las FINALES (`stageNumber` NULL) aparecen en la última etapa y en carreras de un día. Cada clasificación se edita en **drawer nivel 2** (tabla cruda: # · dorsal · corredor · **equipo** · **tiempo/gap** · pts · valor · IRM; añadir/quitar/reordenar filas + "Ordenar por puesto"; el corredor REAL se resuelve por dorsal contra la startlist y se muestra como hint bajo el nombre). **Edición manual (mig. 112, 2026-06-29):** además del autocasado por dorsal, cada fila tiene (a) un botón **🔗** que abre un buscador de corredor (espejo ligero de `_slOpenRiderMatchPicker` de inscritos, `_ruOpenRiderMatchPicker`) que fija `globalRiderId` a mano, y (b) un **selector de equipo** (`_ruOpenTeamPicker`, valida specialEdition contra la carrera) que persiste en la nueva columna `race_uci_results.teamId`. Ese `teamId` es un **OVERRIDE de equipo**: gana a la resolución por dorsal/globalRiderId en el render (web `js/resultados.js` `enrichOverrideTeams`/`overrideTeam` + apps `byTeamOverride` en `buildIndividualRows`/`collapseTtt`/`teamsInClass`), y como el cron nunca lo escribe, es estable. Las **dos columnas de tiempo (Tiempo/Gap) se fusionaron en una**: el input deduce gap si empieza por `+`, si no tiempo absoluto, y `save()` lo reparte entre `timeText`/`gapText`. El **fix de `resolve_uci_results` (mig. 112)** añade `AND src.gid IS NOT NULL` al UPDATE → el RPC ya no pone a NULL un `globalRiderId` casado a mano cuyo dorsal no casa con la startlist (antes el `save()` lo borraba en el mismo guardado); sigue re-enlazando por dorsal donde sí casa. Sin flag `manualRider`: el override de equipo es la garantía dura, y el corredor manual sobrevive porque sin dorsal casable el RPC ya no lo toca. (3) **BLOQUEO — migración `087` (`race_uci_stages.lockedAt`)**: guardar desde el panel fija `lockedAt` → `uci-results-upsert.mjs` salta el UPDATE de cabecera y el DELETE+INSERT de filas de las clasificaciones bloqueadas (guardas EN el SQL → protegen igual en `--apply` y `--emit-sql`); candado por clasificación para bloquear sin editar / desbloquear (= el siguiente volcado del cron vuelve a mandar). `resolve_uci_results`/`_by_name` NO se gatean: solo re-enlazan `globalRiderId` por dorsal (la startlist es la verdad del corredor), nunca pisan datos de clasificación. Guardado del panel: lock → delete → insert (sortOrder = orden visual; `rankText` derivado) → `rowCount`/`winnerName` → RPC `resolve_uci_results`. El `<details>` FC/PCS ("Resultados (avanzado)") se movió de General a esta pestaña. **Siguiente migración = 101** (088 = `admin_trigger_uci_results`; 089 = fuente Tissot, abajo; 090 = `source='pdf'`, abajo; 091–093 = drenaje del backlog, abajo; 094 = `rider_identity_aliases`, saneo abajo; 095 = guard anti-filial + filtro de género en `resolve_uci_startlist`, 2.ª pasada del saneo abajo; **096–098 = optimización de rendimiento/seguridad de la BD, abajo**; **099 = sobrecarga `admin_trigger_uci_results_workflow(p_race_id)` para volcar una carrera concreta — ver «Volcar esta carrera» arriba**; **100 = el guard de ventana de meta excluye los DÍAS DE DESCANSO — ver abajo**; **101 = fuente MATSPORT: `source='matsport'` + `matsportCode` — ver abajo**; **102 = trigger workflows orden de salida a nivel STATEMENT**; **103 = fuente SPORTSTIMING: `source='sportstiming'` + `sportstimingCode` — ver abajo**; **104 = fuente manual_timing: `source='manual_timing'` + `manual_timingCode` — ver abajo**; **105 = `race_days.routeGpxUrl` para el mapa interactivo del recorrido — ver abajo**; **106 = bucket Storage `route-gpx` (el GPX del mapa se sirve desde Supabase Storage, NO desde el proxy R2 que duplica el header CORS — ver abajo)**; **107 = `race_uci_links.matchMethod` para la pasada de tarde de auto-enlazado — ver abajo**; **108 = fuente RACE|RESULT: `source='raceresult'` + `raceresultCode` — ver abajo**; **109 = fuente STS/Wiclax: `source='sts'` + `stsCode` — ver abajo**; **110 = trigger `race_day_inherit_slug_from_oneday_race`: en carreras de un día la jornada hereda el slug ES del race padre si viene NULL — ver abajo**; **111 = limpieza: drop de 9 tablas de respaldo acumuladas tras la 098 (snapshots de saneos/migraciones de corredores/equipos/jornadas, sin uso por la app; 0 FKs/vistas dependientes verificado — `cn2026_rd_*_bak`, `cn_cri_sub23_*_20260625`, `saneo_0612_*`, `saneo_0616_rider_merges`, `saneo_0621_masurian_rider_merges`)**; **112 = `race_uci_results.teamId` (override manual de equipo) + fix `resolve_uci_results` (no borra `globalRiderId` casado a mano sin dorsal) — ver arriba**; **113 = `drop teams.slug` + trigger `set_team_slug` + función `team_base_slug` (retirada de las fichas públicas de equipo/corredor — ver "RETIRADA de fichas públicas" arriba)**; **114 = `ALTER VIEW startlist_riders_resolved SET (security_invoker = true)`: cierra el advisor de seguridad CRÍTICO 0010 (Security Definer View). La vista se evaluaba con permisos/RLS del creador (`postgres`, salta RLS); ahora con los del rol que consulta. SEGURO: sus 4 tablas (startlist_riders, races, riders_men, riders_women) tienen RLS + SELECT a `anon` + política de lectura `public USING (true)` → las lecturas públicas pasan igual (verificado: anon ve 42.819 filas tras el cambio)**; **118 = fuente DOMTEL: `source='domtel'` + `domtelCode` — ver arriba**; **119 = fuente LIVETIMING.AT: `source='livetiming'` + `livetimingCode` — ver arriba**; **120 = RPC `admin_trigger_web_pages_workflow()` que auto-dispara la regeneración de páginas estáticas al publicar una jornada — ver abajo**; **121 = debounce de la regeneración de páginas MOVIDO AL SERVIDOR: tabla `web_pages_regen_state` + RPC `admin_mark_web_pages_dirty()` (la que llama ahora el panel) + `web_pages_regen_tick()` (pg_cron cada minuto, quiet period 45 s) + `_dispatch_web_pages_workflows()` — ver arriba**; **122 = MERCADO DE FICHAJES: tabla `rider_transfers` + `riders_*.contractUntil` + `team_seasons.badgeVisible` + siembra team_seasons 2027 (4 divisiones, chapa oculta) + `targetType='transfers'` en el cintillo — ver "🔁 MERCADO DE FICHAJES" arriba**; **123 = MERCADO DE FICHAJES, dudas + fecha ocultable: `rider_transfers.dateVisible` + `status='doubt'` (CHECK que lo restringe a `type='renewal'`) + `team_seasons.continuityDoubt` — ver "🔁 MERCADO — DUDAS" arriba**; **124 = push_subscriptions: cierre del acceso anónimo, FASE 1 — ver "🔒 PUSH_SUBSCRIPTIONS" abajo**; **125 = ESCRITA PERO NO APLICADA: fase 2, va con la release 4.0 — ver abajo**; **126 = limpieza: drop de 3 tablas de respaldo con RLS OFF (advisors CRITICAL) — ver "🔑 JWT ANON LEGACY" arriba**; **127 = DROP de `broadcasts_suggested` (tabla de una utilidad de importación ya en desuso; 0 dependencias; backup `broadcasts_suggested_bak_drop_20260717`, ya retirado por la 132 — ver "🧹 REFORMA ABIERTO + LIMPIEZA DE CÓDIGO MUERTO" arriba**). **128 = ESCRITA PERO NO APLICADA: `DROP COLUMN startlist_teams."isDev"`, fase 2 de la retirada que dejó pendiente la 063. VA DESPUÉS DE PUBLICAR LA 4.0 — iOS <4.0 decodifica `isDev` como `Bool` NO opcional, así que sin el campo la lista de inscritos NO CARGA en esas versiones (mismo patrón de orden obligatorio que la 125) — ver "🏷️ isDev RETIRADO" abajo**). **129 = `teams."firstSeason"` + guard en el trigger `sync_team_to_season`: un equipo que nace en una temporada futura (p. ej. «+ Equipo 2027» creado en 2026) NO recibe fila `team_seasons` del año en curso → el mercado lo distingue de un equipo preexistente (sin fila 2026 = nuevo = chapa vacía; con fila 2026 = colores antiguos hasta publicar la de 2027) — ver "🎽 MERCADO" arriba**). **130 = los dispatches de regeneración apuntan a `build-site.yml`**: la web pasa a desplegarse por artifact de Pages y los cuatro generadores se funden en un workflow único; OBLIGATORIA porque `_dispatch_web_pages_workflows()` (121) y `trigger_workflows_for_start_order()` (102) disparan los workflows POR NOMBRE vía la API de GitHub — ver [[project_pages_artifact_deploy]]. **131 = RPC `startlist_counts()`** (equipos y corredores por carrera): PostgREST tiene los agregados DESHABILITADOS (`db-aggregates-enabled = false`, default de Supabase) → `startlist_teams?select=raceId,count()` devolvía siempre `PGRST123` y las 802 páginas de `/inscritos/` salían con la descripción genérica. **132 = limpieza: DROP del backup `broadcasts_suggested_bak_drop_20260717` que dejó la 127** (RLS OFF → advisor CRITICAL; 360 filas; cuarta tanda tras 098/111/126 → 0 tablas sin RLS en `public`) — ver "🧹 REFORMA ABIERTO + LIMPIEZA DE CÓDIGO MUERTO" arriba. **133 = bucket PRIVADO `uci-reports` de Storage para el match-report del matcher UCI** (el panel lo leía de una ruta relativa al fichero commiteado → 404 en producción; ver "📄 MATCH-REPORT" abajo). **Siguiente migración = 134.** Ver memoria `project_panel_results_tab`.

**Cron de resultados UCI del día — el guard de ventana NO abre por días de descanso (migración 100, 2026-06-12):** el disparador `trigger_uci_results_today_workflow` (087, pg_cron `*/30`) estaba disparando el workflow toda la noche sin ninguna prueba activa (cazado con la Vuelta a Camerún). CAUSA: la rama "jornada de HOY sin hora de meta" del guard —pensada para no perder cobertura si a una etapa real le falta el `estimatedFinishTimeUtc` curado— abría ventana **todo el día** también para una **jornada de descanso** de una vuelta (Camerún tiene descansos con `stageNumber` NULL y sin horas el 06-06 y 06-12; al ser "hoy" el 12 → 48 disparos inútiles). FIX: esa rama solo abre ventana si la jornada es **competición real** — `raceFormat='one_day'` (su jornada ES la prueba) **O** `stageNumber >= 1` (etapa numerada). Un día de descanso (`stageNumber` NULL/0 en un `stage_race`) ya no abre ventana. Las etapas reales sin hora de meta siguen cubiertas. Espejo OBLIGATORIO en `uci-results-cron.mjs` (predicado `IN_WINDOW`, mismo commit): el `EXISTS` une con `races r` para leer `raceFormat`. ⚠️ Las jornadas de un-día y muchas etapas NO tienen `estimatedFinishTimeUtc` poblado (solo ~stage_races curadas) → NO endurecer más el guard sin revisar datos: un-día sin hora depende de la rama "hoy + raceFormat one_day".

**Resultados — fuente TISSOT TIMING conmutable por carrera (migración 089, 2026-06-10):** para las carreras que cronometra Tissot (ASO: ARA/Tour/Vuelta/París-Niza/clásicas; Suiza; Romandía; Mundial — el Giro NO), los resultados se pueden volcar desde su API REST sin auth (`prod.server.tissottiming.com`; contrato + correcciones verificadas en `scripts/results-fetchers/TISSOT-TIMING-API.md`), que publica **5–15 min tras meta, antes que UCI DataRide**. Diseño: `tissot-results-fetch.mjs` emite el MISMO JSON intermedio que `uci-results-fetch.mjs` → mismo `uci-results-upsert.mjs` (locks 087, resolve por dorsal 082, saneos), web/apps/panel intactos. `race_uci_links.source` (`'uci'` default | `'tissot'`) + `tissotCode` ("ara"); `uci-results-cron.mjs` elige fetcher y compone el comp_id `tissotCode + races.year` ("ara2026" — estable: su hash genera los **eventId sintéticos NEGATIVOS** que dan la idempotencia; los de DataRide son positivos → jamás chocan). ⚠ Conmutar una carrera ya volcada = purgar antes sus `race_uci_stages` (DELETE por raceId, cascade) o quedan clasificaciones duplicadas. CRE: Tissot NO publica tiempos individuales de meta (rider endpoint sin entrada de etapa, subRankings/members vacíos) → **HÍBRIDO**: el fetcher intenta la "Stage Classification" de DataRide (todos los corredores con su tiempo real, con centésimas) bajo el MISMO eventId sintético; si la UCI aún no publicó, fallback = expandir las filas de equipo con el roster (`/stages/{n}/teams`) al patrón UCI — líder con rank + tiempo ABSOLUTO (el render TTT deriva los gaps y exige que NINGUNA fila traiga gapText) y compañeros rank=NULL detrás — y el siguiente volcado de la ventana se auto-corrige al publicar la UCI. Bonus: pestaña **Equipos** (overall) que la UCI no publicaba para el ARA — y la pestaña Equipos de la web (`resultados.js`) casa ahora cada fila por nombre contra los equipos canónicos de la startlist (chapa + nombre del catálogo + enlace vía `teamLinkUrl`, patrón orden-salida; vale también para fuente UCI). Volver a UCI = `UPDATE race_uci_links SET source='uci'` + purga + re-volcado. **Primera carrera conmutada: ARA 2026** (Dauphiné, `ypyYoME1gtFtdci2sY2n`) — E1–E3 re-volcadas desde Tissot (22 clasificaciones, 1410 filas, 1300 enlazadas por dorsal, 0 sin enlazar; CRE E3 con tiempos individuales UCI, pestaña Equipos 22/22 con chapa y formatos verificados en navegador contra BD de prod). Ver memoria `project_tissot_results_source`.

**Resultados — fuente MATSPORT conmutable por carrera (migración 101, 2026-06-12):** cuarta fuente, para las carreras que cronometra Matsport (`api.cycling.matsport.com` — francesas y algunas belgas/europeas: Tour Féminin des Pyrénées, Dunkerque, Morbihan, Denain, Isbergues, Marsellesa, Luxemburgo…; la MISMA API que la skill de startlists usa para inscritos). Publica EN VIVO durante la etapa y valida minutos tras meta — en estas carreras pequeñas DataRide tarda días o no llega. Calco del patrón Tissot: `matsport-results-fetch.mjs` emite el MISMO JSON intermedio → mismo `uci-results-upsert.mjs`; `race_uci_links.source='matsport'` + `matsportCode` ("PYF"); el cron compone el comp id `{races.year}_{code}` ("2026_PYF", convención Matsport) y le pasa `--skip-existing-after-min 60` como a Tissot (publica parcial y corrige en la 1ª hora). **El `competitionId` del puente es SINTÉTICO NEGATIVO** (como PDF; no hay DataRide): `-(fnv1a('matsport:'+compId)%200000)` — lo imprime `matsport-results-fetch.mjs --suggest-id`. Mapeo `rankingType` (whitelist; resto descartado): ITE→stage · ITG→gc/stage (GC del día) · IJE→youth/stage · ETE/ETG→teams (su `bib` es el NÚMERO DE EQUIPO == `teams[].position`, verificado contra dorsales → se traduce a nombre y el bib se emite NULL) · IPG→points/overall · IMG→kom/overall · IJG→youth/overall; última etapa con ITE → pseudo-final (quirk 085). Las filas NO traen nombres (solo bib+position+capital+gap+status) → display reconstruido desde `competitions.teams[].riders` por dorsal ("APELLIDO Nombre"); el corredor real se resuelve POR DORSAL (082), los ASCII de Matsport dan igual. Status francés→IRM: AB→DNF · NP→DNS · HD→OTL · EX→DSQ. ⚠ Gotchas API: los filtros query se IGNORAN (filtrar en cliente), `/stages/{id}` pesa ~2 MB (usar solo `rankingTypes`), puntos llegan como `"61 pts"` en `capital`. NO verificado aún: CRI/CRE en esta fuente (validar antes de conmutar una carrera con crono). **Primera carrera: Tour de los Pirineos 2026** (`SdyXyCdYBmlfzduo0NHy`, comp `-451`) — etapa 1 volcada EN VIVO el 2026-06-12 con la carrera aún en meta (60 filas, 100% enlazadas por dorsal contra la startlist curada de 22 equipos), re-volcado en bucle hasta asentar. Ver memoria `project_matsport_results_source`.

**Resultados — fuente SPORTSTIMING.DK conmutable por carrera (migración 103, 2026-06-13):** quinta fuente, el cronometrador danés (Copenhagen Sprint UCI WT/WWT y carreras nórdicas). **No tiene API JSON**: la clasificación de meta se renderiza server-side en HTML y se lee por query param `GET /event/{eventId}/results?cat={catLabel}` (un evento agrupa varias carreras: `Elite Women (13. June)` / `Elite Men (14. June)`). **El volcado se ejecuta EN LOCAL (sin GitHub Actions; decisión Dani):** `sportstiming-results-fetch.mjs` + `uci-results-upsert.mjs --apply`, a mano o con el bucle `_watch-cph2026w.sh`. Calco del patrón tissot/matsport: emite el MISMO JSON intermedio → mismo upsert/locks/resolve-por-dorsal; carrera de un día → 1 clasificación `stage/stage`. `race_uci_links.source='sportstiming'` + `sportstimingCode` = `"{eventId}|{catLabel}"`; `competitionId` SINTÉTICO NEGATIVO (salt `sportstiming:`; `--suggest-id`). **Contrato de fila** (verificado contra la edición 2025, /event/16511): `Plac | Tiempo | gap +M:SS | "Nombre (DORSAL)" + <div>Equipo</div> | Land IOC-3 | Equipo`; **el nº entre paréntesis = DORSAL** (resolución por dorsal 082); ganador con tiempo absoluto, resto con gap; abandonos `DNF/DNS/DSQ/OTL` + tiempo `-`. NO se usan `?viewType=leader|points` ni las columnas de "crossings". **Dos flags de corrección manual en el fetcher** (sobreviven al re-volcado, sin tocar BD a mano): `--remap-bib FEED:STARTLIST` (sportstiming descuadra dorsales respecto a la startlist UCI; la web reconstruye por dorsal contra `startlist_riders_resolved` → un dorsal inexistente deja al corredor sin enlazar) y `--inject "rank|dorsal|Nombre|Equipo[|gap]"` (corredor sin transponder; se autodesactiva si aparece en el feed). El bucle `_watch` **vuelca SOLO si la clasificación cambió** (firma rank+bib+tiempo+gap+irm) para no machacar la instancia (⚠ el upsert sale con exit≠0 cuando no hay contenido nuevo → tratar ese caso como "sin cambios", no como fallo, o re-vuelca en bucle). Contrato completo en `scripts/results-fetchers/SPORTSTIMING-API.md`. **Primera carrera: Copenhagen Sprint femenina 2026** (`DnhlcUCroLfRLjUEvKuo`, comp `-101501`, code `18776|Elite Women (13. June)`) — volcada EN VIVO el 2026-06-13: 97 filas, 97/97 enlazadas; Wiebes 1ª, Kool 2ª (entró nativa), **Zanardi corregida con `--remap-bib 57:56`** (57 en sportstiming / 56 en la UCI). La masculina (14-jun) aún sin dorsales en sportstiming al cerrar. ⚠ Al volcar saltaron **transacciones zombi** (`idle in transaction` en `ClientRead`) de otros upserts colgados que bloqueaban `race_uci_links`/`race_uci_results` (contención de instancia documentada en memoria); se resolvieron con `pg_terminate_backend`. Ver memoria `project_sportstiming_results_source`.

**Resultados — fuente manual_timing conmutable por carrera (migración 104, 2026-06-14):** sexta fuente, el cronometrador italiano **manual_timing** (`timing.example.invalid`; Giro Next Gen / Giro Ciclistico d'Italia U23 y otras carreras italianas). Su capa "live highlights" expone **JSON crudo PÚBLICO SIN AUTH** (vía `curl`, sin navegador) que publica EN VIVO durante la etapa — patrón calcado de tissot/matsport/sportstiming. **Volcado EN LOCAL (sin GitHub Actions):** `manual_timing-results-fetch.mjs` + `uci-results-upsert.mjs --apply`, a mano o con el bucle `_watch-gironextgen.sh <STAGE> [DATE]`. Emite el MISMO JSON intermedio → mismo upsert/locks/resolve-por-dorsal. **Contrato**: JSON crudo en `export/<TAPPA>/<EXPORT>.json` (TAPPA=`TA01`,`TA02`… = `stageNumber`); polling barato en `export/indici/loadIndici.php?JSonFileNames=A:B&currentTappa=TA01` (CSV de firmas). Exports→clasificación: `ArriviHLData`→stage · `MrosaHLData`→gc (⚠ `Clas_Virtuale:"S"`=GC **virtual** en carrera → el fetcher la OMITE por defecto, solo con `--force-gc`; `"N"`=oficial) · `MrossaHLData`→points · `MazzurraHLData`→kom · `MbiancaHLData`→youth. **Fila**: `PlaPet`=DORSAL (resolución 082; cruce 1:1 verificado contra la startlist curada), `PlaCls`=`"(1.)"`, `PlaCognome`/`PlaNome` (display "APELLIDO Nombre", solo fallback), `PlaNaz` IOC-3, `PlaTeam`, `PlaTempo`/`PlaDistacco` (entre paréntesis si virtual); IRM italiano (RIT/NP/FT/SQ)→DNF/DNS/OTL/DSQ. `race_uci_links.source='manual_timing'` + `manual_timingCode` (estable por edición, p. ej. `gironextgen2026`; el host es fijo y la etapa = `stageNumber`); `competitionId` SINTÉTICO NEGATIVO (salt `manual_timing:`; `--suggest-id`). El bucle `_watch` vuelca SOLO si la firma de las clasificaciones cambió. `uci-results-cron.mjs` **SALTA** `source IN ('pdf','sportstiming','manual_timing')` (query + guard del loop; antes solo `pdf`). **CRI/CRE NO verificado** (layout `CRONOIND`/`CRONOSQ`, exports `*_CLCRON`/`*_TRASQ`): validar antes de conmutar una crono. Contrato completo + comandos + calendario 2026 en `scripts/results-fetchers/manual_timing-TIMING-API.md`. **Primera carrera: Giro Next Gen 2026** (`WceHUBiLcq06ZS03tRGn`, code `gironextgen2026`, 8 etapas 14–21 jun): contrato sondeado y pipeline validado en seco (startlist YA curada: 27 equipos/162 corredores, todos con dorsal); **el volcado real se hace al cerrar cada etapa** (Etapa 1 fin ~14:29 UTC del 14-jun). Ver memoria `project_manual_timing_results_source`.

**Resultados — fuente RACE|RESULT conmutable por carrera (migración 108, 2026-06-17):** séptima fuente, la plataforma **race|result** (`my.raceresult.com`; Tour of Slovenia, Tour of Norway y muchas carreras nórdicas/Europa central). Expone una **API JSON PÚBLICA SIN AUTH** (config + list) accesible por `curl`/Node — patrón calcado de tissot/matsport, y como ellas es **AUTOMÁTICA en el cron** (NO local como sportstiming/manual_timing). `raceresult-results-fetch.mjs` emite el MISMO JSON intermedio → mismo `uci-results-upsert.mjs`/locks/resolve-por-dorsal. **Contrato (verificado contra Tour of Norway 2025 `334313` EventOver + Tour of Slovenia 2026 `402988`):** **2 endpoints** — `GET my.raceresult.com/{eventId}/results/config?lang=en` (host FIJO) devuelve `key` + `server` (my3/my4…) + `EventOver` + las listas (`Tab.Config.Lists[].Name`); luego `GET {server}/{eventId}/results/list?key=KEY&listname=...&page=results&contest=1&r=all&l=0&fav=&openedGroups=%7B%7D&term=&selectorResult=SEL`. ⚠ **El `key` y el `server` ROTAN por edición** → el fetcher los resuelve del /config en cada run, NUNCA hardcodear. **Selector de etapa**: `list.SelectorResults[]` = `[{ResultID, ShowAs:"Stage 1"}]` → **`&selectorResult=ResultID`** filtra la etapa (solo Stage Results; las generales no llevan selector, son acumuladas). ⚠ **GOTCHA: el param es `selectorResult=`, NO `s=`** — con `&s=` la API ignora el filtro y devuelve siempre la etapa por defecto (cazado contra Norway 2025: las 4 etapas daban el mismo ganador). **Fila = array POSICIONAL cuyo ANCHO VARÍA por lista** (12 Stage Results, 13 General Classification, 9 Points/KOM/Young, 7 Team GC) → el mapeo va por TIPO de lista (`colsByWidth`), no por índice fijo; **DisplayBib = dorsal** (resolución por dorsal 082; el nombre `"James Matthew BRENNAN*"` se reordena a `"APELLIDO Nombre"` estilo UCI solo como display fallback, `*`=sub23). `data` puede ser **lista plana O dict anidado** de grupos (`data["#1_Tour"]["#1_Start"]`) → `flattenData` aplana ambos. **Normalización**: `2h53'29''`→`2:53:29` · `+1'15''`→`+1:15` · `+28''`→`+28` · `84 pt`→`84` · `"1."`→`1`; IRM en la col de rank (DNF/DNS/DSQ/OTL/HD/NP/AB→códigos UCI); solo rank1 trae tiempo absoluto, resto por gap (vacío=m.t.). **EventOver=true** → las generales definitivas van SOLO a la pseudo-etapa "Final Classification" (stageNumber NULL, isFinalClassification, scope='stage', quirk 085); carrera en curso → cuelgan de la última etapa con datos (mismo criterio que Matsport). **Mapeo listas**: Stage Results→stage/stage · General Classification→gc/stage · Points→points/overall · KOM→kom/overall · Young Rider→youth/overall · Team General→teams/overall (las listas LIVE se ignoran: las de la pestaña results ya reflejan el live). **Migración 108** = `race_uci_links.source` admite `'raceresult'` + columna `raceresultCode` (= el **eventId numérico** de race|result, p. ej. `"402988"`) + CHECK que lo exige; `competitionId` del puente SINTÉTICO NEGATIVO (salt `raceresult:`; `--suggest-id`). `uci-results-cron.mjs` rama `useRaceresult` (invoca con `--event <raceresultCode>`), NO se excluye (automática), `--skip-existing-after-min 60` como tissot/matsport (publica parcial en vivo). **Descubrir el eventId**: del HTML de la web de resultados de la carrera, `grep -oE 'RRPublish\([^,]+, *[0-9]{5,}'` (va inline: `new RRPublish(div, 402988, "live")`). **⚠ CRI/CRE NO verificado** (el esquema tiene isITT/TTT_StageID + lista "LIVE Team Time Trial", pero ni Norway 2025 ni Slovenia 2026 tienen crono): el fetcher trata toda etapa como en ruta; VALIDAR el layout antes de conmutar una carrera con CRI/CRE. Contrato completo + comandos en `scripts/results-fetchers/RACERESULT-TIMING-API.md`. **Primera carrera conmutada: Tour of Slovenia / Vuelta a Eslovenia 2026** (`ouii0TrVPaCfIYQCHbdW`, eventId `402988`, competitionId `-17212`): puente creado `pending`; verificado el camino completo cron→fetcher→∅-guard en real (5 etapas detectadas, sin disputar → no escribe, queda pending). Etapa 1 = 2026-06-17, meta 13:51 UTC → el cron `today` vuelca solo en la ventana de meta (14:06–16:51 UTC). Ver memoria `project_raceresult_results_source`.

**Resultados — fuente STS / WICLAX conmutable por carrera (migración 109, 2026-06-17; CRE validada 2026-07-30):** octava fuente, el cronometrador francés **STS** (`stsport.fr`; La Route d'Occitanie, Andorra Clàssica, Tour du Piémont Pyrénéen, pruebas/campeonatos FFC). STS publica el LIVE con el motor **Wiclax**, que expone un fichero de datos **`.clax` (XML) PÚBLICO SIN AUTH** (`curl`/Node, ~0.1 s) — patrón calcado de tissot/matsport/raceresult, y como ellas **AUTOMÁTICA en el cron** (NO local). `sts-results-fetch.mjs` parsea el `.clax` → MISMO JSON intermedio → mismo `uci-results-upsert.mjs`/locks/resolve-por-dorsal. **El `.clax` vive en `https://www.stsport.fr/LIVE/<stsCode>.clax`** (`stsCode` = el path tras `/LIVE/` sin extensión, p. ej. `LAROUTEDOCCITANIE/2026-RDO`; `/LIVE/` es un autoindex Apache navegable). El visor `g-live.html?f=…clax` (botón "liveclassement" en `stsport.fr/article<NNNN>.html`) es solo la cáscara Wiclax; el dato es el `.clax`. **Contrato (verificado contra La Route d'Occitanie 2025 y 2026):** `<Epreuve><Etapes><Etape>` (1 por etapa, orden = nº 1-based) con `<Engages>` (inscritos: `<E d=dorsal n="APELLIDO Nombre" c=equipo a=año x=género l2=códigoUCI na=IOC3>`), `<Resultats>` (CLASIFICACIÓN DE ETAPA), `<General>` (GC acumulada del día) y `<ClassementsAnnexes>` (`<Clt id="gpm|pts|JE">`→`<Rush id="GN">` = montaña/puntos/jóvenes overall). **`<Resultats>`/`<General>` van VACÍOS (self-closing) hasta que se corre la etapa** → el fetcher omite la etapa sin `<Resultats>` y el ∅-guard del cron deja el link `pending` sin escribir. **Fila `<R>`**: RANK POSICIONAL (1ª fila=ganador, sin atributo de puesto); `d`=dorsal · `t`=tiempo absoluto `HHhMM'SS`(`,cc`) o **`Abandon`** (con `tr="4"`)→DNF (otros estados FR: `Non partant`→DNS, `Hors délai`→OTL, `Disqualifié`/`Exclu`→DSQ) · `g`=gap (`-` líder/mismo-tiempo · `+0'04` · `+1h00'15` · `+ 1 tour` doblado) · resto parciales. **ENCODING m.t. (clave):** se emite el **tiempo absoluto de CADA clasificado en `timeText` y NUNCA `gapText`** (Wiclax da `t` de todos los finishers, verificado 127/127; los del mismo grupo comparten el `t` del cabeza) → la web entra en su **Caso A (`deriveGaps`)** y pinta m.t. sola (mezclar gapText rompería `deriveGaps`). **CRI**: `type="1"`/`chrono="1"` → `raceType='ITT'` (la web trunca). **CRE validada con Kreiz Breizh 2026:** `type="2"`/`chrono="2"` → `raceType='TTT'`; `<Resultats>` lista corredores agrupados por equipo y puede repetir más abajo a compañeros descolgados, así que el fetcher agrupa globalmente por `teamName`, deja rank+tiempo solo al primer corredor del equipo y conserva el resto con rank/tiempo NULL — mismo contrato que `expandTeamTimeTrial` de Tissot y el colapso CRE de web/apps. `<General>` sigue siendo individual. **Mapeo**: `<Resultats>`→stage/stage · `<General>`→gc/stage (última etapa→FINAL) · anexa gpm GN→kom/overall · pts GN→points/overall · JE GN→youth/overall; carrera terminada → pseudo-etapa "Final Classification" (stageNumber NULL, quirk 085) con la General + anexas overall de la última etapa. **Migración 109** = `source` admite `'sts'` + columna `stsCode` + CHECK que lo exige; `competitionId` del puente SINTÉTICO NEGATIVO (salt `sts:`; `--suggest-id`). `uci-results-cron.mjs` rama `useSts` (construye `--clax-url https://www.stsport.fr/LIVE/<stsCode>.clax`), NO se excluye (automática), `--skip-existing-after-min 60` como tissot/matsport. Normalización: `04h31'03`→`4:31:03` · `00h15'28,34`→`0:15:28` (centésimas fuera) · puntos `28`→`28`. Contrato completo + comandos en `scripts/results-fetchers/STS-TIMING-API.md`. **El PDF de STS (skill `cc-resultados-pdf`) queda solo como respaldo** si el `.clax` fallara (decisión D del árbol de fuente). **Primera carrera conmutada: La Route d'Occitanie / Ruta de Occitania 2026** (`RYfbJykI6YZBBJ7m7sME`, stsCode `LAROUTEDOCCITANIE/2026-RDO`, competitionId `-109623`): puente creado `pending`; verificado el camino completo cron→fetcher→∅-guard en real (3 etapas detectadas, sin disputar → no escribe, queda pending). Parser validado a fondo contra la **edición 2025** (16 clasificaciones: ITT + 3 etapas en ruta + abandonos + montaña/puntos/jóvenes + final; GC Prodhomme, KOM Bizkarra, puntos Barthe; upsert acepta el JSON, 1151 filas). Etapa 1 = 2026-06-18 → el cron `today` vuelca solo en la ventana de meta. Ver memoria `project_sts_results_source`.

**Resultados — fuente LIVETIMING.AT conmutable por carrera (migración 119, 2026-07-08):** **décima** fuente, el cronometrador **austriaco** `livetiming.at` (LIDL Tour of Austria / Vuelta a Austria y pruebas de Austria). Dos endpoints **JSON PÚBLICOS SIN AUTH** (`curl`/Node) que publican **EN VIVO** — patrón calcado de tissot/matsport/raceresult/sts/domtel, **AUTOMÁTICA en el cron**. `livetiming-results-fetch.mjs` → MISMO JSON intermedio → mismo `uci-results-upsert.mjs`/locks/resolve-por-dorsal. **Endpoints:** `GET live_links.php?V_ID=<vid>` (metadatos del día: `Rennen[]` con Name "1. Etappe…", has_Tour, isMZF=CRE, Renndatum) + `POST live_data_all.php` (body `V_ID=<vid>&lynx=1&tour=1`) → clasificaciones en JSON YA ESTRUCTURADO (objetos, no arrays posicionales): `FF`=etapa, `GC`=general acumulada, `PT`=puntos, `GP`=montaña, `YU`=jóvenes, `CLASS`=sub-clas. intermedias (sprints SP/KOM por puerto/Punktewertung/Aktivster/Sonderwertung) → **IGNORADO** (no publicables, como en las demás fuentes). El `&tour=1` hace GC/PT/GP/YU acumuladas del tour. **MODELO DE V_ID (clave):** un `V_ID` = **UN DÍA** de cronometraje, formato **AAMMDD** (`260708`=2026-07-08); en una vuelta **cada etapa es un V_ID DISTINTO** (uno por día): E1=260708, E2=260709… `livetimingCode` = **V_ID de la ETAPA 1** (base); el fetcher **DERIVA** los V_ID siguientes **sumando días** (usa `--total-stages` = max(stageNumber) de race_days que le pasa el cron); un día derivado que no exista/no tenga `has_Tour=1` se OMITE (no rompe la cadena; se prueban todos los días del rango + margen). Asume **una etapa por día natural**; sectores dobles/saltos → `--vids <lista explícita>`. **Contrato de fila:** `Place` numérico→rank, `Place`=DNF/DNS/DSQ/OTL→IRM (en FF con Time/Gap `-`); `BIB`=DORSAL (resolución 082); `Name` ya "APELLIDO Nombre" (quita `*` sub23); `Team` (3 letras)=ignorado (equipo por dorsal). **Tiempos:** FF trae **tiempo ABSOLUTO de CADA corredor** (`4:21:02`) → emitir en `timeText` para TODOS, NUNCA gapText (Caso A `deriveGaps` web, como STS); GC/YU: absoluto rank1 + gap resto (`+0:15`→`+15`, `+25:57`, `+1:01:21`). **Mapeo:** FF→stage/stage · GC→gc/stage · PT→points/overall · GP→kom/overall · **YU→youth/OVERALL** (⚠ no `stage`: `keepForWeb` —columna generada, mig. 092— exige classKind∈(stage,gc) O scope='overall' O isFinalClassification; con `stage` jóvenes quedaba INVISIBLE — cazado en el volcado real). Carrera terminada (`--event-over` o maxStage>=totalStages) → pseudo-etapa "Final Classification" (stageNumber NULL, scope='stage', quirk 085). **Migración 119** = `source` admite `'livetiming'` + columna `livetimingCode` (= V_ID etapa 1, AAMMDD) + CHECK per-columna; `competitionId` del puente SINTÉTICO NEGATIVO (salt `livetiming:` sobre el V_ID base; `--suggest-id`). `uci-results-cron.mjs` rama `kind==='livetiming'` (pasa `--vid`+`--total-stages`), NO se excluye (automática), `--skip-existing-after-min 60` como tissot. Whitelist `--source` del upsert ampliada con `domtel`+`livetiming` (faltaba domtel). **GOTCHA `riderDisplay` NOT NULL:** livetiming a veces trae el `Name` VACÍO (Tour of Austria 2026 E1, dorsal 172) → `riderDisplay` NULL viola el NOT NULL en el rollback del 1er volcado; fix `cleanName(v, bib)` cae a `#<bib>` (el resolve por dorsal lo sobrescribe con el nombre real de la startlist). **GOTCHA etapa FUTURA pre-montada — guard "exigir FF" (fix 2026-07-10):** livetiming.at pre-monta el V_ID del día SIGUIENTE ya con las generales acumuladas HEREDADAS de la etapa anterior (`GC/PT/GP/YU` pobladas) pero SIN `FF` (etapa no disputada) → el fetcher las volcaba como si la etapa futura ya existiera. Cazado en el Tour of Austria 2026: la E3 (V_ID 260710) se volcó por error durante la ventana de meta de la E2 (solo GC/PT/GP/YU heredadas, `stageDate` de hoy, timestamp de la tarde anterior) — el cron `today` coge la carrera por la ventana de la E2 pero el fetcher deriva TODOS los V_ID por fecha y el día de la E3 ya devolvía datos. Fix en `livetiming-results-fetch.mjs`: guard `if (!hasStageRows) continue` tras construir las clasificaciones del día — una etapa intermedia solo se vuelca si trae `FF` con filas (una etapa disputada siempre la produce, también CRI/CRE); el cierre de carrera NO se afecta (la "Final Classification" sale aparte de `lastStageOveralls`, que solo recoge etapas con FF real). Reparación del volcado erróneo = DELETE de las 4 clasificaciones (gc/points/kom/youth, 209 filas) de la E3 (no bloqueadas). ⚠ Este patrón "día siguiente pre-montado con acumuladas heredadas" puede darse en otras fuentes por etapas. **CRE/TTT (`isMZF=1`)** → `raceType='TTT'` pero layout de filas NO verificado (validar antes de conmutar crono por equipos); **CRI** sin flag claro, tratada como ruta (correcto). El WebSocket STOMP `wss://wf.livetiming.at:15671/ws` NO se usa (el POST da snapshot completo con hash/link_count). Contrato completo + comandos en `scripts/results-fetchers/LIVETIMING-API.md`. **Primera carrera conmutada: Vuelta a Austria 2026** (`IkST1piTSTWv5kgeNNBl`, competitionId `-104960`, livetimingCode `260708`, 5 etapas 08–12 jul, startlist curada 22 equipos): **E1 volcada EN VIVO el 2026-07-08 contra prod** — 5 clasificaciones (stage/gc/points/kom/youth), 339 filas, **100% enlazadas por dorsal** (incl. el 172 sin nombre → AAS Eirik Vang), todas keepForWeb=true; página `/resultados/vuelta-a-austria-2026-etapa-1/` con datos reales. El cron `today` recoge E2–E5 en la ventana de meta de cada día (deriva 260709… por fecha). Mantenimiento anual = el V_ID cambia (AAMMDD) → nuevo livetimingCode al sembrar la edición siguiente. Ver memoria `project_livetiming_results_source`.

**Fix STS/Wiclax — jóvenes sin tiempo + host alternativo `timerspeed.com` (2026-07-04):** cazado en la Volta a Portugal Feminina 2026 (`stsCode` = una URL completa `https://timerspeed.com/live/events/2026/6_vpf_2026.clax`, NO `stsport.fr` — mismo formato `.clax`/Wiclax servido por un host distinto; el fetcher ya soporta esto porque `--clax-url` es la URL tal cual). (1) La anexa `<Clt id="JE">` (jóvenes) codifica sus filas con **`tps=` (tiempo absoluto `HHhMM'SS`), NO `pts=`** como montaña/puntos — `parseAnnexeRows` solo leía `pts` → todas las filas de jóvenes salían con `timeText`/`resultValue` NULL. Fix: `parseAnnexeRows` ahora también parsea `a.tps` con `absTime()` cuando está presente (jóvenes es clasificación de TIEMPO, no de puntos; `js/resultados.js` ya la trata como `isTimeClass`). (2) **Gotcha separado, no del fetcher**: una clasificación de ETAPA con una sola fila stale (`timeText` NULL sin ser abandono, de un volcado a medio disputar) desactiva `deriveGaps` para TODA la clasificación en la web (`allTimed` exige que el 100% de las filas clasificadas tengan tiempo) → se ve como "tiempos absolutos en vez de m.t./+gap" en TODAS las filas, no solo la stale. Si se repite: re-volcar la clasificación entera (no solo la fila que falta) para refrescar cualquier stale.

**Resultados — modo HÍBRIDO UCI-preferente: DataRide reemplaza al cronometrador etapa a etapa (SIN migración, 2026-07-02):** para una carrera cronometrada por Domtel (`source='domtel'`, cuya web solo entrega **etapa + general**, nada de puntos/montaña/jóvenes/equipos) donde además queremos las clasificaciones OFICIALES y COMPLETAS de la UCI en cuanto salgan, se activa un modo híbrido en el que el cron corre **AMBOS** fetchers y, clasificación a clasificación, **DataRide (oficial, completo) REEMPLAZA a Domtel (provisional, rápido) donde ya publicó** y Domtel tapa las etapas que DataRide aún no da. Piezas (commit `b44b5e3a`): (1) **`uci-results-upsert.mjs` — guard UCI-preferente**: en `--apply` calcula las claves lógicas `stageNumber|classKind|scope` ya cubiertas por fuente oficial (`eventId > 0`) y OMITE toda clasificación ENTRANTE sintética (`eventId < 0`: cronometrador) cuya gemela ya publicó DataRide → el reemplazo es PERMANENTE (Domtel no re-duplica en cada pase). Solo afecta a entrantes negativas → un volcado oficial (positivo) nunca se bloquea. (2) **`uci-results-cron.mjs` — rama híbrida**: bucle refactorizado a `processSource(t, kind)`; con `source='uci'` + `domtelCode` poblado corre UCI PRIMERO (su purga 090 borra las gemelas sintéticas negativas viejas) y Domtel DESPUÉS (relleno; el guard le impide re-crear lo que UCI cubre). El **∅-guard pasa a ser por FUENTE** (no por carrera): si una fuente no publica aún, la otra vuelca igual. Carreras mono-fuente: sin cambios. **Convención SIN migración**: `source='uci'` + `competitionId` = el **id REAL de DataRide** (positivo) + `domtelCode` intacto → el CHECK `chk_race_uci_links_domtel_code` (`source<>'domtel' OR domtelCode NOT NULL`) se cumple por el `OR`. **Activar** en una carrera ya en Domtel: descubrir el `competitionId` real de DataRide (matcher `uci-match-poc.mjs --year <año>`, o la lista `Competitions/` de la temporada; road 2026 = seasonId `464`) → `UPDATE race_uci_links SET source='uci', "competitionId"=<REAL_ID> WHERE "raceId"='<id>'` (deja `domtelCode`) → disparo inicial `uci-results-cron.mjs --race-id <id>` (ignora ventana; vuelca etapas ya corridas). No hay que purgar a mano (purga 090 + guard limpian las gemelas Domtel). **Volver a solo-Domtel**: `UPDATE … SET source='domtel'` (restaurar el `competitionId` sintético negativo si se quiere el stamp). **Primera carrera conmutada: Course de Solidarnosc et des Champions Olympiques 2026** (`1x03Ty91oNjOzr3Lxx3j`, `domtelCode='8850'`, DataRide `competitionId=77997` — antes sintético `-137279`): verificado end-to-end el 2026-07-02: etapas 1-2 volcadas desde DataRide (12 clasificaciones, 756 filas, 100% enlazadas por dorsal, `eventId>0`; tabs Etapa/General/Puntos/Jóvenes en ambas — antes solo etapa+gc de Domtel), las gemelas Domtel purgadas (0 stages negativas restantes), Domtel omitió 1-2 por el guard, sin `stage/stage` duplicadas. De aquí en adelante el cron `today` lo mantiene en la ventana de meta y, cuando DataRide publique 3+, reemplazan a Domtel automáticamente. Contrato del híbrido en `scripts/results-fetchers/DOMTEL-TIMING-API.md`. Ver memoria `project_domtel_results_source`.

**Resultados — skill `cc-resultados-pdf`, tercera fuente: PDFs de organizadores (migración 090, 2026-06-10):** para carreras SIN fuente automática (DataRide no publica o tarda días), Claude actúa de "tercer fetcher": extrae las clasificaciones del PDF del organizador/cronometrador, las valida y emite el MISMO JSON intermedio → mismo `uci-results-upsert.mjs` (locks 087, resolve por dorsal 082, saneos) → web/apps/panel intactos. Skill en `.agents/skills/cc-resultados-pdf/SKILL.md` (mapeo libro→classKind/scope con la lista de qué NO extraer, IDs sintéticos negativos salt `pdf:{raceId}` — esquema Tissot; `uciRaceId` sintético también, es NOT NULL —, normalización tiempos/gaps/dittos/IRM multilengua, checklist de validación contra startlist, árbol de decisión de fuente) + **corpus de 4 PDFs reales 2026 en R2** (`assets.calendariociclismo.app/dev/pdf-samples/`: Brussels un-día, Giro Women RCS, libro STS con CRI, libro ChronoRace con capa de texto desalineada). ⚠️ **Ya NO están en el repo** (2026-07-18): son libros de organizadores/cronometradores, obra de terceros que no se redistribuye bajo AGPL. Siguen fuera de `.agents/skills/` por la razón de siempre — PDFs pesados colapsan el contexto de Claude Chat. Migración `090` amplía `race_uci_links.source` a `'pdf'`; `uci-results-cron.mjs` SALTA esas carreras (query + guard en el loop; sin fetcher automático, competitionId sintético negativo); `uci-results-upsert.mjs --source uci|tissot|pdf` fija el source del link (sin flag no se toca). **El volcado PDF es un PLACEHOLDER que la fuente oficial PISA (decisión Dani 2026-06-10):** el upsert purga las **gemelas sintéticas** — misma clasificación lógica (`raceId`+`stageNumber`+`classKind`+`scope`) bajo eventId negativo ≠ entrante, cascade a sus filas — ANTES de upsertar cada clasificación, **ignorando el candado `lockedAt`** (el candado protege correcciones del panel frente a re-volcados de la MISMA clasificación, no convierte un placeholder en verdad frente a la oficial). Verificado en prod con DO+RAISE (rollback): gemela bloqueada purgada, oficial intacta. Colateral bueno: conmutar tissot→uci ya no necesita purga manual (el siguiente volcado completo purga solo). **Extracción validada contra DataRide:** Clásica de Bruselas 2026 extraída del PDF y difeada contra las 133 filas reales de `race_uci_results` → **0 discrepancias** (ranks, dorsales, tiempos/gaps con dittos `' '` resueltos, 27 DNF); 5 difs solo de nombre-display (el PDF trae nombres más completos; irrelevante, el corredor se resuelve por dorsal). Correcciones puntuales post-volcado = panel (lockedAt), nunca re-emitir el PDF a mano. Ver memoria `project_pdf_results_skill`.

**Resultados — backfill 2026 drenado + cron de retrasadas (migraciones 091–093, 2026-06-10):** se drenó el backlog de ~246 carreras 2026 ya disputadas (lotes locales monitorizados + run final de CI) y `uci-results-backlog.yml` corrió cada 2 h (`--scope backlog --limit 25`) hasta que **el 2026-06-12 se DESACTIVÓ su schedule** (decisión Dani: ya cumplió su función; ahorra minutos de Actions): queda solo el disparo manual `workflow_dispatch`, y el día a día lo cubren `uci-results-today.yml` + «Volcar esta carrera» del panel. Reactivar = restaurar el bloque `schedule` (`cron '7 */2 * * *'`). La monitorización del drenaje destapó y arregló **4 agujeros sistémicos** del pipeline: (1) **∅-guard en `uci-results-cron.mjs`** — un fetch sin filas (comp enlazado, UCI sin publicar) ya NO upserta: el link queda `pending` y se reintenta (antes se marcaba `ok` vacío y salía del backlog para siempre). (2) **Migración 091 — duplicados de fichas**: la UCI publica el nombre LARGO ("SERRANO RODRIGUEZ Gonzalo") y el `identityKey` exacto no casaba con la ficha corta → `resolve_uci_results_by_name` ahora prueba **match por SUBCONJUNTO de tokens** (±1 día de nacimiento, desempates fecha→país) antes de crear; se fusionaron 96 duplicados ya creados (backups `riders_*_bak_uci_dup_merge_0610`). (3) **`dotnetToISODate` del fetcher restaba 1 día a TODOS los nacimientos** (`/Date(ms)/` a medianoche CET truncado en UTC; mismo bug que las fechas de etapa) + el guard `<1990` anulaba nacidos en los 80 → +3h antes de truncar, guard a 1900, y +1 día saneado a los 325 nacimientos ya escritos (backup `riders_birth_bak_uci_shift_0610`). (4) **Migración 092 — `keepForWeb` con WHITELIST de classKind** (cazado por Dani en el Tour de Lituania): el catch-all de `classifyEvent` tragaba secundarias desconocidas ("Sprint Classification" = metas volantes) como `classKind='stage'` visibles → kind propio `sprint` + lo desconocido a `other`, y `keepForWeb` exige kind ∈ {stage,gc,points,kom,youth,teams} (invisibles también en overall/finales). (5) **Migración 093 — filas SIN dorsal** (cazado por Dani: Edgar Nohales en el GP Beiras): la UCI a veces omite el bib (340 filas en 7 carreras, 318 solo en la Vuelta a Albania) y eran invisibles para ambas RPCs → el upsert pasa también las filas sin bib con su `riderDisplay` y la RPC las enlaza por display (solo sobre filas sin bib numérico). Tras el saneo: 0 filas sin-bib sin enlazar, ~100% de enlace en filas individuales.

**Resultados — saneo integral 2026 con sistema de agentes (migración 094, 2026-06-11):** barrido completo de inconsistencias en los datos volcados, ejecutado con un sistema de agentes detectores→jueces→reparadores re-ejecutable (**workflow guardado `.claude/workflows/saneo-resultados-uci.js`**, solo lectura; la reparación se aplica aparte — runbook `docs/runbooks/saneo-resultados.md` con TODAS las convenciones). Resultado: **0 filas sin enlazar (de 1.165) y 249 desalineadas (de 1.501), todas residuo legítimo** (corredores que no están en la startlist: sustituciones, equipos ausentes). Piezas: (1) **Migración `094` — `rider_identity_aliases`**: el identityKey de toda ficha FUSIONADA apunta a su superviviente; `resolve_uci_results_by_name` y `resolve_riders` consultan los alias antes de crear → las fusiones sobreviven a futuros volcados (el subconjunto 091 no cubría "Marjolein" vs "Maria Apolonia"). ~78 fusiones aplicadas (21 globales + race-local) con backup jsonb en `saneo_0611_rider_merges`; superviviente = ficha con el **nombre de uso** (no la de más refs). (2) **`isTeamEvent` ya NO se copia ciego de DataRide** (la UCI lo marca true en TODO el día CRE y en CREs de un día enteras): el fetcher lo deriva — solo `classKind='teams'` es team-event; se recuperaron las jornadas CRE enteras que ambos resolve saltaban (París-Niza E3, Feminin E1, Japón E4, Ses Salines: 1.165 filas). El fetcher también filtra filas-marcador "Race Cancelled" (Murcia E2/Setmana E3: cabeceras borradas, backup `saneo_0611_deleted_stages`). (3) **PROHIBIDO `resolve_uci_results` "a pelo"** en carreras con filas legítimas por nombre (las ANULA, LEFT JOIN): relinks = UPDATEs dirigidos. (4) **Selecciones nacionales**: +57 equipos NTM/NTW (Australia, India, France, las 16 de El Salvador, castellanizadas Argelia/Benín/Marruecos/Mauricio…) con colores de equipación y alias ES, +Credibom (CT pt que faltaba); 318 `startlist_teams` enlazados (selecciones + clase-Efapel vía `fold_team_name`=`foldedNames`; guard: nombres con "Development" no se enlazan a categoría senior). (5) **5 startlists con nombres vacíos** (Clàssica CV 1969, Llucmajor, Castellón, Terres de l'Ebre, Palma) rellenadas desde resultados por dorsal (backup `saneo_0611_startlist_bak`). (6) 6 refs colgantes de fusiones antiguas reparadas (208 filas; `garcia-canellas-margarita`→Mavi García) + `races.gender` de la Volta a Portugal Feminina corregido a `female` (habría creado fichas femeninas en `riders_men`). Deudas editoriales menores en la memoria. Ver memoria `project_results_consistency_saneo`.

**Resultados — 2.ª pasada del saneo: WT↔filial + cross-género + renames hispanos (migración 095, 2026-06-11):** continuación del saneo integral sobre `startlist_teams`/`riders_*`. (1) **Regla de producto (Dani): senior y filial en la MISMA carrera = un solo squad bajo el senior** — 142 bloques filiales fusionados (321 corredores movidos a `startlist_riders.teamId` del senior, fila filial borrada; `rider_team_affiliations`/`currentTeamId` NO se tocan) tras validar patrón "invitados" (combinados ≤8, 0 solapes). Marcas: Soudal Devo, Movistar Academy, Tudor U23, Lidl-Trek Future Racing, Red Bull Rookies, UAE Gen-Z, Hagens Berman Jayco, EF Aevolo, AG Insurance Devo, Canyon Generation, UAE Dev fem **y `Lotto - Groupe Wanty` (juzgado filial de Lotto Intermarché**: 24/39 carreras compartidas patrón invitados, edad media 21.1 vs 25.0, nombre = los dos sponsors). (2) **Filial sola = equipo propio**: 100 bloques que mostraban la identidad del SENIOR en carreras sub-23 re-enlazados a su entrada filial + 18 NULL del guard 06-11 enlazados (todas las filiales TIENEN catálogo, incl. `team_devo_picnic`). (3) **Causa raíz → migración `095`**: `fold_team_name` come development/team/continental/women → la filial pliega IGUAL que el senior (match exacto LIMIT 1 elegía senior) y el match NO filtraba género; `resolve_uci_startlist` lleva ahora filtro de género + guard anti-filial (exacto y contención; 14 casos test verificados). (4) **Cross-género**: 141 startlist_teams femeninos enlazados al equipo MASCULINO (Lidl/Movistar/Picnic/Uno-X/Visma/Cofidis Women→Cofidis PT/VolkerWessels/St Michel/REMBE/7 Saber/Amani) re-enlazados a la entidad correcta + **13 NTW nuevas** (Spain/Italy/Greece/United States/Ukraine/Colombia/China/Ecuador/Indonesia/Malaysia/Thailand/Uzbekistan/Austria, colores de kit + alias ES) + NTM Colombia/Thailand para 62 selecciones mal enlazadas; 9 carreras pasaron a `enrichedStartlist=true`. Plan auditable en `saneo_0611_wtdevo_plan` + backups `saneo_0611_wtdevo_st_*_bak`. (5) **TAREA 2 — dobles apellidos hispanos**: 123 fichas no-verified con refs renombradas al nombre de uso (recorte del 2.º apellido particle-aware, nombres de pila compuestos SE CONSERVAN — "Daniel Felipe Martínez", "Xabier Mikel Azparren"; nombre largo a `otherNames`, **identityKey viejo a `rider_identity_aliases` apuntando a la MISMA ficha** — sin eso el siguiente volcado re-crea el dup), backup jsonb `saneo_0611_rider_renames`; 4 colisiones del UNIQUE tratadas una a una (2 homónimos reales → forma larga: Mario Silva Baeza vs Mário Silva pt, Rubén Sánchez Estévez 2003 vs 2006; 2 duplicados fusionados: Azparren, Torrico); 1 ficha con fn/ln invertidos corregida; pt/br excluidos; 53 verified con doble apellido intactas (curadas). Convenciones en el runbook `docs/runbooks/saneo-resultados.md`. Ver memoria `project_wtdevo_xgender_renames_saneo`.

**Fase 7 apps — Resultados in-house UCI (Android 2.4.0/`272` + iOS 3.0/`1056`, AMBOS EN MAIN):** paridad nativa de las páginas de resultados de la web (`js/resultados.js`) y del tachado de abandonos (`js/inscritos.js`). **Solo-online** (sin Room/caché, como inscritos/orden de salida; decisión de Dani). Piezas Android: pantalla `ui/results/ResultsScreen.kt`+`ResultsComposables.kt` (selector de etapa, pestañas Etapa/General/Puntos/Montaña/Jóvenes/Equipos, tiempos/gaps normalizados, **CRE colapsada por equipo + desplegable**, filtro por equipo con m.t. dinámico, DNF/DNS/OTL/DSQ); lógica pura testeada `util/UciResultsLogic.kt` (+`UciResultsLogicTest`); datos `SupabaseService.raceUciStages/raceUciResults/startlistRidersResolvedFull` + `CalendarRepository.loadResultsData/loadResultRows`; modelos `UciResult.kt`; ruta `results/{raceId}?stage={n}`. **Piezas iOS (port 1:1, 2026-06-09):** `Views/Results/ResultsView.swift`+`ResultsComponents.swift`; lógica `Services/UciResultsLogic.swift` (+`UciResultsLogicTests`, espejo de los 19 tests de Android); datos `Services/SupabaseService+UciResults.swift` (mismas queries y compuestos); modelos `Models/UciResult.swift`; navegación **por valor** con `ResultsRoute` (`navigationDestination(item:)` en Hoy/competición; `NavigationLink(destination:)` en el CTA de jornada). **CTA "Ver clasificaciones"** en `StageScreen`/`StageDetailView` (gate por jornada con in-house) + FC/PCS degradados a "También en" (tarjeta unificada `ResultsButtonsCard` en ambas plataformas, también en "Así está la carrera"). **Trofeo en Hoy/competición** → pantalla nativa cuando hay in-house (`inhouseStagesForDays`). **Carreras de un día** (clasificación `gc` sin `raceDayId`) detectadas y con header completo (ruta/km/tipo). **Tachado de abandonos** en la startlist (`globalRiderId` en el modelo/DTO; el filtro `irm/globalRiderId IS NOT NULL` va EN EL SERVIDOR para no truncar a 1000 filas — si no, los abandonos de las primeras etapas no se tachaban); con in-house se oculta el aviso "no spoilers". **Cabeceras sticky** en Inscritos y Resultados (bloque de carrera fijo arriba; en Resultados también selector de etapa + pestañas). Fix transversal: el **kilometraje/desnivel/fecha de cabecera** siguen el idioma de contenido (EN→punto, ES→coma), no el locale del dispositivo — Android (`RaceDay.distanceFormatted`+`ElevationProfileScreen`) e iOS (`RaceDay.distanceFormatted/elevationGainFormatted` + `DateFormatting.formatDateLongContent` + `ElevationProfileView`). **Web**: la chapa del equipo se muestra junto a la bandera del corredor en la tabla de resultados (gustó en la app). **Web (2026-06-10) — badge trofeo/Revive in-house en cards (paridad apps):** las cards de Hoy (`js/app.js`), competición y challenge (`js/competicion.js`) pasan a modo terminado (trofeo+Revive, sin horario ni mini-perfil) en cuanto la etapa tiene clasificaciones propias — gate `rd._hasInhouse || heurística meta+30` (espejo de `hasInhouse || shouldShowResults` en TodayScreen/RaceScreen), sin exigir fcId/pcsSlug; detección batched `loadInhouseStageSet(raceIds)` en `race-data-modal.js` (1 SELECT a `race_uci_stages` keepForWeb+rowCount>0, misma clave por stageNumber que jornada.js → badge visible ⇔ el clic redirige a `/resultados/`). Con el volcado Tissot (5–15 min tras meta) el trofeo aparece en la web antes que la heurística. Ver memoria `project_uci_results_apps`. Las apps consumen `race_uci_*` a partir de aquí (antes solo la web).

**2.4.0 — Modelo temporal de equipos (Fase 5/6):** las startlists enriquecidas ya resuelven la denominación correcta del equipo por fecha vía `startlist_teams.teamId` (INEOS Grenadiers→abril, Netcompany→mayo+; Canyon-zondacrypto→hasta Giro). Migración `076` añade `teams.specialEdition{ValidFrom,ValidTo,RaceId}` para declarar **explícitamente** la vigencia de cada maillot especial (rango de fechas O carrera concreta); el panel valida de forma **bloqueante** al asignar un specialEdition a una startlist fuera de su vigencia. El render de inscritos (web+iOS+Android) toma los atributos visuales de `team_seasons[races.year]` con **fallback permanente a `teams`** (aditivo, sin downtime; en 2026 season==teams; `teams` NUNCA se degrada). `team_seasons` 2026 completado para los 33 canónicos sin season; los specialEdition NO reciben season. Ver memoria `project_catalogo_oro_dni_temporal`.

**Panel — zona Corredores fusionada en Equipos (web, 2026-06-08):** la pestaña independiente "Corredores" (navegador global del catálogo, sólo en el menú hamburguesa) se **eliminó**. Los corredores se gestionan **siempre desde la plantilla de un equipo** (vista Equipos). El botón "Buscar corredor" desapareció. El **editor de ficha** (`#riderEditorModal`) y el **detector de duplicados** (`#dupScanModal`, botón "🔍 Duplicados" en la cabecera de Equipos); el escáner tiene su propio toggle masc/fem (`_dupScanGender`) en vez de heredar el "tab activo". En `js/panel.js` se borraron `setupRidersView`/`_updateRiderTabs`/`loadRidersTable`/`_renderRidersList`; `_ridersGender` se conserva como "género de la ficha en edición" (lo fija `openRosterRiderEditor`); tras guardar/borrar/fusionar se refresca la plantilla abierta vía `_refreshOpenRoster()` (antes `loadRidersTable()`). El editor se cablea perezosamente con `_ensureRiderEditorWired()`.

**Panel — rediseño rail + drawer (web, 2026-06-09):** el shell del panel se reescribió. (1) **Rail lateral izquierdo permanente** (`.panel-rail`, fijo, `--rail-width` 210px) sustituye las 6 pestañas horizontales + la hamburguesa: clusters **MÁS UTILIZADAS** (Agenda·Analytics·Inscritos·Notificaciones·Cintillo) y **OTRAS** (Equipos·Carreras·Challenges·Resultados UCI) — hasta 2026-06-10 se llamaban DIARIO/CATÁLOGO, con Cintillo en el segundo — como `.rail-item[data-tab]` con icono+label, + footer (email·tema·Health·Salir). En `<768px` colapsa a iconos (`--rail-width:56px`). `switchTab`/`initTabs` usan `.rail-item[data-tab]`; las vistas (`#racesView`…) se anclan a `left:var(--rail-width)`. Logo = monograma "CC". (2) **TODA la edición vive en un drawer deslizante** (`js/components/drawer.js`, `openDrawer({title,level,wide,render,onClose})`, 2 niveles apilables): los 7 editores de tarjeta + el **editor de jornada** (la agenda es ahora una **lista** centrada que abre **siempre en el día en curso** — el recordatorio del último día visitado vía localStorage `panel_dateKey` se retiró el 2026-06-10 por incómodo; tap → drawer ancho con `#editorArea` montado dentro; `renderEditor`/`saveRaceDay`/`deleteRaceDay` lo siguen localizando por id) + el **selector de carrera** (`#raceModal` → drawer nivel 1/2 adaptativo) + el **escáner de duplicados** (`#dupScanModal` → drawer ancho). (3) **Cero `modal-overlay` y cero popups nativos**: `confirm`/`alert`/`prompt` → `js/components/dialog.js` (`confirmDialog`/`alertDialog`/`promptDialog`, z-400 sobre el drawer). Componentes compartidos nuevos: `gender-toggle.js`. La sección "Resultados" del editor de jornada es un `<details>` colapsable ("avanzado"). Ver memoria `project_panel_rail_drawer_redesign` + `docs/handoff-panel-rail-drawer.md` (queda cola incremental: barrido de estilos inline a `u-*`).

**Equipos públicos en la web (Fases 0–4, 2026-06-09):** página pública de equipo `/equipo/<slug>/` (+ EN `/en/team/<slug>/`). **Fase 0:** migración `080` añade `teams.slug` (UNIQUE parcial `uq_teams_slug`, NULL en ediciones especiales) + helper `team_base_slug(name)` + trigger `set_team_slug` (auto-genera en alta/rename; femenino homónimo → sufijo `-femenino`; specialEdition → NULL). **Fase 1:** tooltip de corredor en `/inscritos/` (hover desktop / tap táctil) con bandera + nombre + edad + equipo actual y enlace "Ver equipo"; el nombre NO enlaza (no hay ficha de corredor — decisión de producto). La lógica del tooltip vive en `js/rider-tooltip.js` (`setupRiderTooltips`), compartida por inscritos y equipo. **Fase 2:** `equipo.html` + `js/equipo.js` (patrón SPA igual que inscritos: regex de slug → query Supabase → render → `history.replaceState`). Cabecera (chapa `buildTeamBadgeSvg` + nombre + bandera `countryCode` + categoría vía `teamCategoryLabel`) + plantilla `SEASON` (const 2026; integrantes desde `rider_team_affiliations year=SEASON` → `riders_men`/`riders_women`, con bandera + edad en el mismo tooltip). Visuales con override `team_seasons[SEASON]` y fallback a `teams`. Ediciones especiales (slug NULL / specialEdition, solo alcanzables vía `?team=<id>`) → redirigen al `parentTeamId`. **Fase 3:** OG/SEO estático — `og-pages.yml` genera `equipo/<slug>/index.html` + `en/team/<slug>/index.html` (JSON-LD `SportsTeam` + `BreadcrumbList`; OG image por defecto, los equipos no tienen logo de imagen); `sitemap.yml` + `scripts/generate-sitemap.sh` añaden `/equipo/<slug>/` con hreflang ES/EN. **Fase 4:** la cabecera de equipo en la startlist (`js/inscritos.js`) enlaza a `/equipo/<slug>/` cuando el equipo enriquecido tiene slug. i18n: sección `team.*` (ES en `js/i18n.js`, EN en `i18n/en.json`). **PENDIENTE (hueco reservado en el diseño):** bloque "Ha corrido / Próximas" (carreras del equipo vía `startlist_teams.raceId → races`) — NO hasta que todas las carreras de 2026 tengan startlist; y FICHAJES (altas/bajas con `rider_team_affiliations.dateFrom/dateTo`). Solo temporada 2026 por ahora (diseñado para multi-temporada vía la const `SEASON`). Ver memoria `project_public_teams_feature`.

**Equipos — refinamientos (2026-06-09):** (1) la plantilla de `/equipo/` se renderiza como **tabla columnar** (reutiliza `.so-table` de los órdenes de salida): columnas **Corredor (bandera+nombre) · Nacimiento** con la fecha de nacimiento + edad como numeral entre paréntesis (`DD/MM/YYYY (NN)` vía `formatBirthDate` en `shared.js`); la página de equipo ya **no** usa el tooltip (la info va inline). (2) El **tooltip de corredor** (`/inscritos/`) muestra ahora `fecha de nacimiento (edad)` en vez de "NN años" — `inscritos.js` pasa `data-rider-birth` y `rider-tooltip.js` lo formatea. (3) Los **órdenes de salida** enlazan a equipos: en **CRI** el nombre del corredor enlaza a la página de su equipo, en **CRE** el equipo enlaza a la suya (`js/orden-salida.js` resuelve el slug con `findMatchingTeam` contra los equipos de la startlist de la carrera; clase `.so-link`). Sin migración (slug resuelto por nombre en cliente).

**Header web centralizado (runtime, 2026-06-09):** el header del sitio (logo + nav + dropdown Hoy/Mes/Temporada + hamburguesa + tema + buscar + Apps) dejó de estar duplicado en cada página. Ahora es **fuente única** en `js/header.js`. Cada página incluye solo el placeholder `<header class="site-header" id="siteHeader">` (con `data-back` en subpáginas → botón "← Volver") + `<script type="module" src="/js/header.js">`. El módulo deduce idioma (ES/EN) y sección activa por `location.pathname` e inyecta el header **tras el parse y antes de `DOMContentLoaded`**, de modo que el cableado existente lo encuentra ya montado: `theme.js` (theme toggle, hamburguesa, dropdown, labels mes/año) y `lang-switch.js` enganchan en `DOMContentLoaded`; el botón **Apps** lo enlaza el propio `header.js` a `window.openAppsModal` (apps-modal.js es script clásico → corre antes que el módulo). El placeholder reserva los 56px vía CSS (`.site-header`) → **sin salto de layout**. Migración de las ~5000 páginas ya generadas con `scripts/migrate-header.mjs` (idempotente: salta las que ya tienen `id="siteHeader"`); el generador `og-pages.yml` emite el placeholder (constantes `SITE_HEADER_HTML` / `_EN` colapsadas; el idioma lo resuelve `header.js` por la ruta `/en/`). **Rediseñar el header = editar solo `js/header.js`.**

**Rediseño header + cintillo-tarjeta (web, 2026-06-09):** se trae el lenguaje visual de las apps. (1) **Header — barra de utilidades (day-centric)**: por datos de uso, **Mes/Temporada apenas se usan** → lo que manda es el **día (Hoy, la home) y el cintillo**. El header deja de exponer la nav primaria; queda un **cluster de utilidades SIEMPRE visible** (`.header-actions`): `⌕ Buscar` (inline en desktop; en móvil va al menú) · **Apps** (visible también en móvil) · **selector de idioma como slider ES↔EN** (`.lang-switcher` con `.lang-knob` deslizante, lo inyecta `js/lang-switch.js` en `.header-actions`, `data-active` posiciona el knob) · **tema** · **☰ menú**. El **menú** (`#siteNav`, dropdown en desktop / tarjeta casi a ancho completo en móvil, lo abre la hamburguesa vía `theme.js`) contiene Hoy/Mes/Temporada/Buscar/Sobre. En móvil el logo es **solo los iconos** (calendario+ciclista; `.site-logo__text` oculto). Se retiró el handler muerto del dropdown de `js/theme.js`; CSS `.nav-primary-*`/`.nav-segment`/`.header-right`/`.header-search-btn` eliminados. La sección activa la deduce `header.js` por `location.pathname` (o `data-active`). (2) **Cintillo** (`js/cintillo.js` + `.giro-countdown` en `css/app.css`): de barra fina de 40px a **tarjeta redondeada "App Store Today"** — superficie `--bg-card`, tinte del color de carrera (`--giro-h` se remide solo por `offsetHeight`), hairline + `--card-shadow`, logo 38, **dos líneas** (título `--font-display` semibold + detalle muted; `<strong>` del detalle en `--accent`), chevron, dots centrados y X arriba-derecha. Sigue **sticky** bajo el header, apilado con `.hoy-sticky` (selector de días), como en la app. La lógica del carrusel (5s, swipe, dots, dismiss por hash) no cambió.

**Startlists tachan abandonos — Fase 7 de resultados in-house (web, 2026-06-09):** en las carreras con **resultados propios de la UCI** (tablas `race_uci_*`), la lista de inscritos (`/inscritos/`) **tacha + atenúa** a los corredores que ya **no siguen en carrera** y **deja de mostrar el aviso de "no se actualiza"** (la lista se mantiene sola). Señal de abandono = campo `irm` (DNF/DNS/OTL/DSQ) de `race_uci_results`, sobre la fila de la **etapa MÁS RECIENTE** del corredor (mayor `stageNumber` entre eventos `classKind='stage'`; un abandono en la et.3 aparece con rank normal en las 1-2); **`irm` es la señal fiable, NO `rank`/`rankText`**; la "Stage General Classification" (`classKind='gc'`) es el GC acumulado del día → NO sirve. Cruce results↔corredor por `globalRiderId` (lo expone `startlist_riders_resolved`). **En vivo desde cliente** (refleja el último volcado del cron, sin paso de build, 0 migraciones): `js/inscritos.js` añade clase `.startlist-rider--out` + `data-rider-dnf="irm|stageNumber"`; el **motivo+etapa va en el tooltip del corredor** (`js/rider-tooltip.js`, línea `.rider-tip__dnf` "Fuera de carrera · ABN · etapa 2"). Módulo compartido nuevo **`js/uci-irm.js`** = fuente única de `IRM_LABELS` (DNF→ABN, DNS→NS, OTL→FC, DSQ→EXP) + `irmLabel()`, **extraído de `js/resultados.js`** (que ahora lo importa — ⚠️ al extraer un módulo, el archivo nuevo y su `import` van en el MISMO commit, o 404 en prod). Con resultados in-house **no se muestra ningún aviso** y se **retiró el `<hr>`** separador bajo los botones Web oficial/Etapas; el "no spoilers" se conserva SOLO en carreras por etapas SIN resultados in-house. Verificado en el Dauphiné 2026 (8/154 tachados) y generaliza (Giro 2026 ya disputado, 33/184). **Las apps NO consumen `race_uci_*` todavía → el tachado en startlists nativas es parte de las apps 3.0** (junto con las páginas de resultados in-house). Ver memoria `project_uci_results_web_build`.

**🏷️ `isDev` RETIRADO DE LAS APPS + barrido de datos (migración 128 ESCRITA, NO APLICADA; 2026-07-18):** `startlist_teams.isDev` añadía **" (Devo)"** al nombre del equipo en la lista de inscritos. Quedó **redundante** cuando cada filial pasó a tener su **ficha propia** en `teams` (nombre y chapa propios): la condición "es filial" vive en el `teamId` canónico. La **063** hizo la fase 1 (limpiar datos) y dejó escrito que una segunda migración haría el `DROP COLUMN` "cuando todas las apps en uso estén actualizadas" — nunca se ejecutó, así que la columna siguió VIVA y LEÍDA por iOS (`StartlistViewModel.swift:129`) y Android (`StartlistTeam.kt:16`) durante año y medio. **Lo destapó un import** (La Périgord Ladies 2026) que la puso a `true` por inferir su uso del nombre de la columna sin leer la skill que lo prohíbe → "UAE Development Team **(Devo)**" en ambas apps. **Barrido de datos:** 18 filas a `false` (2 de ese import + **16 preexistentes** en Alpes Isère Tour ×9, Boucles de la Mayenne ×2, Veenendaal ×2, Carrera de la Paz, París-Troyes, Tour de Lituania — reintroducidas DESPUÉS de la 063); hoy **0 filas con `true`**. De paso se sincronizaron al canónico **6 `teamName` desincronizados**, uno grave: "Visma | Lease a Bike" en Alpes Isère Tour estaba enlazado a `team_devo_visma` (la FILIAL) pero mostraba el nombre del equipo **WorldTour** — sin el "(Devo)" que lo desambiguaba habría quedado como si el Visma WT corriera esa carrera (ahora "Visma | Lease a Bike Development"). **Código (4.0):** el campo sale de los DTO/modelos de ambas apps y `displayName` pasa a ser el nombre base; 0 referencias residuales en web, iOS, Android y panel (el checkbox del panel ya no existía: `docs/memory/enriched-startlists.md` lo documentaba de más y se ha corregido). **⛔ ORDEN OBLIGATORIO — la 128 va DESPUÉS de publicar la 4.0 y de su adopción:** iOS decodifica `isDev` como `Bool` **NO opcional**, así que si el `DROP COLUMN` se aplica antes, PostgREST deja de devolver el campo y **la lista de inscritos NO CARGA EN ABSOLUTO** en las apps <4.0 que hay hoy en las tiendas (Android aguanta: kotlinx con default `false`). Es el mismo patrón de la 125. Skills actualizadas (`cc-nucleo` + `cc-startlists-corredores`: la columna sale de la tabla de campos y del snippet de INSERT → se **omite**, su `DEFAULT FALSE` ya deja el valor bueno).


---

## Features de apps ya implementadas (histórico)

### Pantalla "sin anuncios" para upgraders

**Pantalla "sin anuncios" para upgraders (gate `ads_intro_done` → `ads_intro_v4_done`, 2026-06-07 · re-mostrada en 4.0 el 2026-07-19):** la pantalla `PremiumShowcase` (último paso del onboarding, FASE A) existe desde 2.0 con otro copy ("Novedades 2.0 / Premium"), por lo que los upgraders de 2.0–2.2.x ya tienen `premium_showcase_done = true` y NUNCA habrían visto la pantalla de "sin anuncios". Se introdujo un flag DEDICADO **`ads_intro_done`** (iOS `UserDefaults`, Android `AppPreferences`) que fue el gate REAL de esa pantalla en 2.3; `premium_showcase_done` queda como legacy. Un upgrader (`ads_intro_done = false`) la veía una vez tras actualizar; al cerrarla `markDone()` marcaba los flags. Efecto colateral aceptado: quien probó una instalación fresh de 2.3.0/TestFlight antes de aquel fix la vuelve a ver una vez.
**Re-introducción en 4.0 + limpieza de gates viejos (Android `356` / iOS `1159`, 2026-07-19):** Dani quiere RE-mostrar la pantalla a quien actualiza desde 2.x/3.x a 4.0+. Pero todo el parque 2.3+ ya tiene `ads_intro_done = true`, así que ese flag ya no sirve de gate. Se introduce un flag NUEVO **`ads_intro_v4_done`** (iOS `UserDefaults` `"ads_intro_v4_done"` / Android `AppPreferences.adsIntroV4Done`, key `ads_intro_v4_done`) — **sin poner para nadie todavía** — que pasa a ser el gate VIVO del paso `PremiumShowcase` (`firstPending` iOS / `nextOnboardingStep` Android). Como nace en `false` para todos, la pantalla se dispara una vez tras actualizar a 4.0 (upgraders de 2.x/3.x) y una vez en el onboarding de una instalación fresh 4.0. **⚠️ El nombre de la clave es load-bearing: NO renombrar `ads_intro_v4_done` a `ads_intro_done` — los dispositivos 2.3+ lo tienen persistido en `true` y suprimirían la pantalla.** Futuras oleadas = un nuevo `ads_intro_vN_done`. **Limpieza (mismo commit):** como las versiones 2.3/3.x se lanzaron hace ~3 meses y sus flags ya están saturados a `true` en todo el parque y NADIE los lee, se BORRARON `ads_intro_done`/`adsIntroDone` y `premium_showcase_done`/`premiumShowcaseDone` (flows, setters y keys en `AppPreferences.kt`; escrituras en ambos `markDone`) → `markDone` ahora marca SOLO `ads_intro_v4_done`. Las claves persistidas viejas quedan huérfanas en el store (inofensivo). Colateral aceptado: quien probó una fresh 4.0/TestFlight antes de este cambio vuelve a ver la pantalla una vez. Compilación local NO ejecutada (entorno sin Android SDK/secrets ni Xcode) → se confía en CI/Xcode Cloud. Archivos: iOS `CalendarioCiclismoApp.swift` (`firstPending`) + `PremiumShowcaseOnboardingView.swift` (`markDone`); Android `MainActivity.kt` (`nextOnboardingStep`) + `AppPreferences.kt` (`adsIntroV4Done`) + `PremiumShowcaseOnboardingScreen.kt` (`markDone`).

### Modo Campeonatos, Cintillo, Mini-perfil y Anuncios AdMob

**Modo Campeonatos (apps):** del 22 al 28 de junio el tab **Hoy** se apodera mostrando la rejilla de Campeonatos Nacionales (takeover total, como la web); fuera de esa semana, navegar a esas fechas muestra un botón "Modo Campeonatos". Sin tabs nuevos (pantalla contextual). **Rejilla compacta:** cada país es una tarjeta con sus pruebas en celdas de 4 por fila (8 pruebas → 2 filas, no 8), tinte por género; tap en celda → detalle de etapa. iOS navega **por valor** (`ChampionshipsRoute` + `NavigationLink(value:)`), nunca por destino, para no corromper el `NavigationStack`. Config anual en `ChampionshipsConfig` (iOS `Services/`, Android `util/`) — espejo de `js/campeonatos-config.js`. Carga vía `SupabaseService.loadChampionships()` (iOS) / `CalendarRepository.loadChampionships()` (Android). El año siguiente = editar `ChampionshipsConfig` en ambas plataformas + la web.

**Filtro "Hoy" en la rejilla (web + apps):** la rejilla de Campeonatos tiene filtros de género (Todas/Pro/Masc/Fem). A partir del **24 de junio y hasta el 28 (ambos inclusive)** se antepone un filtro **"Hoy"**, que pasa a ser el **primero y el predeterminado**: muestra solo las pruebas cuya jornada (`dateKey`) coincide con la fecha real del día en curso (no la jornada navegada). Los dos primeros días de campeonatos (22–23) apenas hay pruebas → el filtro es superfluo y **no aparece**; tampoco después del 28. A diferencia de los filtros de género (que restringen *slots* vía `SLOT_FILTERS`), "Hoy" muestra todos los slots pero filtra por fecha. Rango configurable en `TODAY_FILTER_START` (`'2026-06-24'`, fin = `RANGE_END`); helpers `isChampTodayFilterActive()` (web) / `ChampionshipsConfig.isTodayFilterActive()` (iOS/Android), `visibleFilters`/`defaultFilter`. Web: `js/campeonatos.js` (`_campFilter === 'today'`). Apps: `Filter.today`/`TODAY` como primer caso del enum + `ChampionshipCountry.visibleSlots` con rama por fecha. El año siguiente = el rango se actualiza solo al editar las fechas de config.

**Cintillo → Campeonatos:** nuevo `targetType = "championships"` en `today_highlights` (migración `072`). Web (`js/cintillo.js`) enlaza a la página de Campeonatos (`campUrl`); apps abren la pantalla nativa (iOS `ChampionshipsView`, Android `Routes.CHAMPIONSHIPS`). Es un destino sin carrera ni URL (destino fijo); `customTitle`/`customDetail`/`customLogo` opcionales. El `custom` queda solo para web. Panel: radio "Modo Campeonatos" en el editor del cintillo. Las apps renderizan estos slides sin carrera (icono de bandera + título/rango de fechas de config).

**Mini-perfil a sangre + relleno temporal (apps):** paridad con la web. (1) El mini-perfil de elevación (Premium) ya no es un sparkline inline en la columna: se renderiza como **franja a sangre (edge-to-edge) al fondo de la tarjeta**, recortada a las esquinas por `CCCard`. (2) Cuando la jornada tiene `neutralStartTimeUtc`+`estimatedFinishTimeUtc` y no es CRI/CRE, la silueta se pinta **gris** y se **tiñe de izquierda a derecha** según el % de tiempo transcurrido (antes de salida → 0/gris, en curso → parcial, tras llegada → 1/completo), con **auto-refresco cada 60 s** (iOS `TimelineView(.periodic)`, Android `LaunchedEffect`+`delay`). **CRI/CRE → siempre 0% (gris/vacías)** como la web (cada corredor/equipo progresa en un momento distinto). Sin horas y no CRI/CRE → aspecto clásico (tinte completo). Aplica en **Hoy** (`RaceCardView`/`TodayScreen`) y en **competición** (`StageRowView`/`RaceScreen`). La vista de competición pasó de **filas de lista con divisores a tarjetas `CCCard`** (una por etapa) para poder llevar la franja a sangre, unificando el lenguaje visual con Hoy/web. (3) Las etapas ya terminadas de competición muestran los iconos de **Resultados (trofeo) / Revive (TV)** en lugar del chevron y **ocultan el mini-perfil**, igual que en Hoy y la web (`StageRowView`/`StageRow` con `isFinishedMode`; reutilizan `ResultsSheet` iOS / `ResultsDialog` Android y `RaceLogic.shouldShowResults`/`reviveUrl`). Componentes: `MiniElevationProfile` (iOS Canvas / Android Canvas) con params `startTime`/`endTime`/`isTimeTrial`. Espejo de `js/elevation-profile.js` (`buildElevationSparkline` con `progressFraction`) + `_updateProgressCards` (web). Ver `docs/memory/premium.md`.

**AAB Android — solo local:** `cd android-app && ./gradlew :app:bundleRelease`. Requiere `google-services.json` y `secrets.properties` (ambos gitignored). Ver `docs/memory/android-architecture.md`. **Convención (Dani, 2026-07-09): tras generar el AAB, copiarlo SIEMPRE a `~/Downloads/app-release-<versionCode>.aab`** (p. ej. `cp android-app/app/build/outputs/bundle/release/app-release.aab ~/Downloads/app-release-337.aab`) — así Dani lo tiene a mano para subir a Play Console sin buscar en la ruta de build. El nombre lleva el `versionCode` para no pisar builds anteriores.

**Anuncios AdMob (FASE B Android / FASE C iOS — SDK integrado, 2026-06-05):** banner adaptativo inline (sin native/interstitial/rewarded) en **tres** pantallas, **todo gateado por `PremiumService.shouldShowAds`** (NUNCA `featuresUnlocked` — ver política de pricing):
- **Ubicaciones:** Hoy (tras la 3ª tarjeta; si <3, tras la última), detalle de carrera (tras la 3ª etapa; si <3, tras la última — mismo criterio que Hoy, banner intercalado en la lista de etapas, no anclado al fondo) y detalle de etapa (justo debajo del bloque "Schedule"/`TimeSection`; si no hay horario, bajo el header).
- **IDs centralizados con flag test/prod:** `AdsConfig` (Android `data/ads/`, iOS `Services/`). En Debug → unidades de test de Google; en Release → reales. **Ad Unit IDs reales de banner ya creados y puestos** (2026-06-07): `PROD_BANNER_UNIT_ID` Android = `ca-app-pub-7131748907832450/8172741311`, `prodBannerUnitID` iOS = `ca-app-pub-7131748907832450/5546577974` (un bloque banner por app, "Banner Android/iOS - inline", reutilizado en los tres sitios). App IDs reales: Android `~2001481990`, iOS `~8887444226` (en manifest/Info.plist + `AdsConfig`).
- **Consentimiento UMP + init:** `AdsConsentManager`. Android: `UserMessagingPlatform`, disparado desde **`MainActivity`** (UMP necesita un Activity para presentar el formulario; no desde `Application`), init de `MobileAds` solo si `canRequestAds`. iOS: orden estricto **UMP → ATT → `MobileAds.shared.start()`**, disparado desde `AppDelegate.didFinishLaunchingWithOptions`. Ambos gateados por `shouldShowAds` (suscrito → ni UMP, ni ATT, ni init, ni unidades). El **mensaje "European regulations" aún NO está creado** en el panel de AdMob → en runtime UMP falla con "no form configured" y se sigue sirviendo anuncios (no bloquea); crear el mensaje activa el formulario en Europa sin tocar código.
- **Wrapper UI:** Android `ui/ads/AdBanner.kt` (`AndroidView` + `AdView`, `DisposableEffect{destroy}`; alto intrínseco vía `BoxWithConstraints`+`fillMaxWidth`, sin `.height` fija); iOS `Views/Ads/BannerAdView.swift` (`UIViewRepresentable` + `BannerView`) con `AdBannerSlot` que se auto-oculta si `!shouldShowAds`. **Manejo de fallo iOS (1057):** si la petición FALLA (no-fill aleatorio de la unidad real) y nunca llegó a cargar, el slot **colapsa a alto 0** (el `BannerView` queda montado por si el auto-refresco entrega más tarde) y el banner solo acepta toques con anuncio cargado (`.allowsHitTesting(loadedHeight != nil)`). Sin esto, el `BannerView` vacío ocupaba el hueco reservado e interceptaba los toques de la card inmediatamente anterior en Hoy/competición ("la 3ª card no se puede pulsar", aleatorio porque dependía del fill). **Sizing del slot iOS (clave):** NO se fija un alto mágico (antes `.frame(height: 60)` recortaba el banner y se solapaba con las celdas vecinas). El `BannerAdView` tiene un `Coordinator: BannerViewDelegate` que en `bannerViewDidReceiveAd` publica el **tamaño realmente entregado**; `AdBannerSlot` mide el ancho con `GeometryReader`, reserva el alto del `AdSize` adaptativo (`cgSize(for: largeAnchoredAdaptiveBanner(width:))`) hasta que carga y luego usa el alto real. El banner se **centra** (`ZStack` + `.fixedSize`) y el contenedor **recorta** (`.clipped()`) para que un creativo más ancho que el móvil no quede pegado al borde. El `BannerView` solo se monta cuando el ancho real ya se conoce (si no, el `load()` inicial pediría un tamaño degenerado). **El creativo 468×60 / 728×90 que se ve en TestFlight es de la UNIDAD DE TEST de Google: los banners adaptativos NO escalan/recortan creativos (doc oficial) y escalarlos viola la política de AdMob → con la unidad de test puede salir centrado-pero-recortado; la unidad real sirve un creativo dimensionado al ancho pedido y encaja.** No es un bug de la app.
- **Versiones de SDK (clave):** Android `play-services-ads = 24.0.0` (NO 25.x). **Motivo:** 24.1.0+ y toda la 25.x exigen Kotlin 2.1+ (25.x compilada con Kotlin 2.3 → solo la lee un compilador Kotlin 2.3); el proyecto usa Kotlin 2.0.21. 24.0.0 es la última compatible (minSdk 23, compileSdk 34 ✓). UMP declarado aparte (`google-ump = 3.1.0`). iOS SPM `GoogleMobileAds` 13.x + `GoogleUserMessagingPlatform` 3.x (repo `swift-package-manager-google-user-messaging-platform`; módulo a importar = `UserMessagingPlatform`). El SDK iOS 13.x pide **Xcode 26.2+** (local 26.5 OK; verificar la imagen de Xcode Cloud).
- **API moderna iOS 13.x:** sin prefijo `GAD` — `MobileAds.shared` (arranque vía `await MobileAds.shared.start()`, sin `completionHandler:`), `BannerView`, `Request`, `largeAnchoredAdaptiveBanner(width:)` (el antiguo `currentOrientationAnchoredAdaptiveBanner` quedó deprecado en 13.x → sustituido por el "large anchored adaptive"); UMP sin prefijo `UMP` (`ConsentInformation.shared`, `ConsentForm`, `RequestParameters`).
