# Changelog de las apps (Android + iOS)

> Historial completo de versiones de las apps nativas, consolidado el 2026-07-20.
>
> Convención de versiones y reglas de bump: ver `docs/memory/ios-conventions.md` (iOS) y `docs/runbooks/android-release.md` (Android).

## Android (`versionCode` / `versionName`)

**`467` / `4.3.0` = JERARQUÍA VISUAL DEL APOYO:** el anuncio de proyecto amplía el icono de Calendario Ciclismo con corazón al mismo tamaño que la hoja de Amigo. La tarjeta «Código y cuentas públicas» añade un chevron para indicar explícitamente que abre la información de transparencia.**

**`466` / `4.3.0` = PRESENTACIÓN DE AMIGO:** la hoja de apoyo amplía el icono de Calendario Ciclismo con corazón, extiende la tarjeta de ventajas a todo el ancho disponible y fija el fondo de la hoja y de las tarjetas de planes al blanco y gris neutros de la aplicación, sin los tonos de contenedor predeterminados de Material 3.**

**`465` / `4.3.0` = COMPILACIÓN DE AJUSTES:** se restaura el import de recursos de texto de Compose requerido por la pantalla Ajustes para generar correctamente las variantes debug y release. Sin cambio funcional.**

**`464` / `4.3.0` = ONBOARDING DE APOYO SEGÚN INSTALACIÓN:** el anuncio 4.3 utiliza el icono Amigo de CC con corazón. Las instalaciones nuevas explican los costes de mantenimiento y la autoría profesional, sin mostrar «Conoce el cambio»; las actualizaciones conservan la explicación de la retirada de publicidad y el reconocimiento Fundador. La audiencia se fija antes de elegir idioma y se conserva entre relanzamientos. La pantalla «Hazte Amigo» usa el mismo icono, elimina el beneficio redundante «Sin anuncios» y prioriza el icono exclusivo antes de recordar que toda la app es gratuita. Contenido completo en español e inglés. Espejo iOS `1277`.**

**`463` / `4.3.0` = RUTA CANÓNICA DE SOSTENIMIENTO:** los enlaces de Ajustes y onboarding abren la página pública en `/apoyar/`. Espejo iOS `1276`.**

**`462` / `4.3.0` = GRATIS, ABIERTA Y SIN ANUNCIOS:** retirada completa de AdMob, UMP, permisos, espacios publicitarios y seguimiento usado exclusivamente para anuncios. Premium deja de venderse y renovarse; las compras anteriores reciben el icono Fundador permanente. Se incorpora Amigo de Calendario Ciclismo (2,99 €/mes o 17,99 €/año) y aportaciones puntuales (2,99 €, 5,99 € y 11,99 €), siempre voluntarias y sin funciones exclusivas. Ajustes simplifica el bloque de sostenimiento y enlaza la explicación pública; el onboarding de 4.3 se muestra una vez y adapta sus acciones al estado Premium/Amigo. Original, Fundador y Amigo cuentan con iconos diferenciados; al vencer Amigo se recupera el icono elegible anterior. Espejo iOS `1275`.**

**`461` / `4.2.6` = APERTURA INMEDIATA DE AGENDA EN HOY:** la vista Calendario/Mes crea la lista directamente en el día actual, sin recorrer mediante animación todos los días anteriores. El posicionamiento inicial funciona aunque el filtro activo no contenga carreras en el mes; el botón «Hoy» conserva su desplazamiento animado.**

**`460` / `4.2.6` = ACCESO A COMPETICIÓN CON ANCHO ESTABLE:** la hamburguesa de Hoy usa un contenedor real de 16 × 16 dp, sin el mínimo implícito de 48 dp de `IconButton`. Los títulos largos ceden el espacio necesario y ya no desplazan ni recortan el botón.**

**`459` / `4.2.6` = BADGES COMPACTOS EN RESULTADOS:** los badges CRI, CRE y cronoescalada reducen su tipografía y relleno exclusivamente en Resultados para conservar la misma altura de línea que las jornadas sin badge. Espejo iOS `1273`.**

**`458` / `4.2.6` = ACCESO A COMPETICIÓN:** el botón de acceso directo desde Hoy se compacta en un rectángulo de acento de 16 dp con hamburguesa reducida. La fila de nombre y bandera conserva 16 dp también en las vueltas por etapas, evitando que el botón introduzca más interlineado que en las clásicas. Espejo iOS `1272`.**

**`457` / `4.2.6` = BADGES EN MAYÚSCULAS:** Hoy, Jornada y Resultados presentan en mayúsculas los badges de categoría, tipo de etapa, TV, inscritos y orden de salida, igual que la web. El estado Cancelada de Hoy adopta en las tres plataformas la misma caja roja compacta, sin icono y alineada con el resto de badges. Espejo web + iOS `1271`.**

**`456` / `4.2.6` = ARMONIZACIÓN DE HOY Y RESULTADOS:** Hoy y Resultados condensan cada jornada a número de etapa, distancia y desnivel, sin repetir las ciudades. Los badges recuperan el espaciado de la web y dejan de usar iconos de tipo; Resultados reserva el badge para CRI, CRE y cronoescalada. Hoy mantiene el tipo junto a la categoría cuando no hay miniperfil y recupera el acceso directo a Competición en vueltas de varias jornadas. Espejo web + iOS `1270`.**

**`455` / `4.2.5` = SEPARADOR EN HOY:** un divisor vertical separa los filtros de categoría del selector de orden, con el mismo tratamiento visual que la separación entre clasificaciones y filtro de equipos en Resultados.**

**`454` / `4.2.5` = ORDEN DE CARRERAS EN HOY:** la vista Hoy incorpora, junto a los filtros y sobre la lista, el selector de orden por categoría, hora de TV u hora estimada de meta que ya ofrecían web e iOS.**

**`453` / `4.2.5` = FILTROS DE CALENDARIO:** Mes y Temporada parten de Todas cuando el usuario no ha guardado un filtro por defecto, sin mostrar Pro durante la carga inicial. En iOS se desactiva el efecto automático de borde de los scrolls de filtros, cuya capa interceptaba las pulsaciones en dispositivos físicos con iOS 27 Beta; se conservan la barra superior, la posición de año/país y los botones nativos. Espejo iOS `1269`.**

**`452` / `4.2.4` = SIN CAMBIO FUNCIONAL:** versión de marketing sincronizada con la corrección iOS de los filtros de Calendario. Los chips Android ya usan `combinedClickable` y no requieren modificación. Sin AAB nuevo.**

**`452` / `4.2.3` = TV PÚBLICA: RTP1 se suma a la prioridad de televisiones públicas en web, Android e iOS, por delante de Eurosport/HBO Max; la Volta a Portugal ordena además RTP1 antes que los streams de WBD.**

**`450` / `4.2.3` = SOSTENIMIENTO DEL PROYECTO:** la pantalla Sin anuncios se vuelve a mostrar una vez a usuarios no suscritos que actualizan desde 4.2.2 y en instalaciones nuevas, tras el onboarding de notificaciones. Espejo iOS `1248`.**

**`449` / `4.2.3` = RESULTADOS SIN PARPADEO:** Jornada resuelve las clasificaciones nativas junto con el resto de datos, antes de pintar «Resultados» y «Así está la carrera». Los botones ya no aparecen como enlaces FC/PCS para transformarse después en «Ver clasificaciones». Espejo iOS `1247`.**

**`448` / `4.2.2` = FICHAJES, ORDEN DE CATEGORÍAS:** los filtros de equipos siguen el orden WT · PT · WWT · PRW en web, Android e iOS.**

**`447` / `4.2.2` = FICHAJES, TARJETAS Y FICHA:** el distintivo de continuidad incierta se simplifica a «En duda» / «TBC» y queda pegado al nombre. Cada tarjeta mantiene altura fija; la ficha de equipo deja de repetir el año junto a la categoría.**

**`441` / `4.2.2` = COMPILACIÓN:** se propaga el filtro activo de Mes a sus filas de carrera, corrigiendo un error de compilación que impedía generar el APK.**

**`440` / `4.2.2` = FICHAJES, SCROLL Y PARRILLA:** Últimas confirmaciones y los equipos comparten ahora un único scroll, de modo que el feed no queda recortado y todos los equipos son alcanzables. Los equipos se presentan en una parrilla compacta, con chapa y nombre de hasta dos líneas. Espejo web + iOS.**

**`439` / `4.2.2` = MINIPERFILES EN COMPETICIÓN:** las CRI y CRE sin horario de salida/llegada ya usan el mismo color de carrera que una jornada en línea no disputada; con intervalo horario se mantiene el gris, pues no hay un avance único. La vista Hoy conserva su comportamiento actual. Espejo iOS `1246`.**

**`438` / `4.2.1` = FICHAJES:** Corrección del año de contrato en Últimas confirmaciones de los fichajes hacia la próxima temporada. Espejo web + iOS.**

**`437` / `4.2.1` = FICHAJES DE PRÓXIMA TEMPORADA:** Últimas confirmaciones prioriza los movimientos hacia la próxima temporada y deja después los de años más lejanos, también cuando llegan en el mismo lote. Vuelve a mostrar el año de contrato de los fichajes hacia la próxima temporada; los de mitad de temporada conservan su distintivo propio. Espejo web + iOS.**

**`436` / `4.2.1` = FICHAJES MID-SEASON: BADGE Y MERCADO:** el distintivo azul de los fichajes de mitad de temporada adopta las mayúsculas y el espaciado del badge Rumor. Estos movimientos se mantienen en Últimas confirmaciones, pero no se interpretan como llegada ni salida del mercado siguiente: la continuidad o fin de contrato queda determinada únicamente por el contrato real que se cargue. Corregido además el espacio doble antes de la flecha del feed web. Espejo web + iOS `1245`.**

**`435` / `4.2.1` = FICHAJES MID-SEASON + HORA DE TV:** el feed de Últimas confirmaciones identifica con un badge azul «M. Temporada» / «Mid-Season» los movimientos efectivos durante el curso y ya no muestra ahí el año de contrato. En Hoy, las emisiones confirmadas con hora (que no son íntegras) muestran solo la hora, igual que la web. Espejo web + iOS `1244`.**

**`434` / `4.2` = LEYENDA UCI POR MODALIDAD:** el color azul del cajón de información identifica únicamente a los WorldTeams en el ránking masculino y únicamente a los Women's WorldTeams en el femenino, sin citar la categoría contraria. Espejo web + iOS `1243`.**

**`433` / `4.2` = FECHA CANÓNICA DEL RÁNKING UCI:** la actualización comparte en las tres plataformas una presentación con dos puntos, día de la semana y fecha larga, adaptada al idioma («Actualizado: martes, 28 de julio de 2026» / «Updated: Tuesday, 28 July 2026»), también dentro de la información ampliada. Espejo web + iOS `1242`.**

**`432` / `4.2` = RESULTADOS SIN SEPARADOR:** desaparece la línea horizontal entre los botones «Últimos Resultados» / «Ránking UCI» y el contenido de la vista activa; se conservan los separadores internos de las clasificaciones. Espejo iOS `1241`.**

**`431` / `4.2` = INVITACIONES UCI COMPLETAS:** los tres mejores ProTeams aclaran tanto en su explicación como en la leyenda que la invitación obligatoria abarca todo el WorldTour —Grandes Vueltas incluidas— y también todas las pruebas ProSeries. La indicación femenina permanece limitada al Women's WorldTour. Espejo web + iOS `1240`.**

**`430` / `4.2` = RÁNKING UCI COMPACTO:** la fecha de actualización y el botón de información comparten una línea dentro de la vista; desaparece el botón aislado de la barra superior. Las tarjetas se convierten en filas continuas y más densas, los ProTeams fuera del top-30 usan fondo rojo completo y los puntos se redondean a enteros para permanecer siempre en una sola línea. Espejo web + iOS `1239`.**

**`429` / `4.2` = RÁNKING UCI DE EQUIPOS:** Resultados incorpora los botones «Últimos Resultados» y «Ránking UCI». La nueva vista consume una única instantánea semanal sobrescribible de DataRide, enlazada al catálogo canónico de equipos, y permite alternar entre los ránkings masculino y femenino. WorldTeams/WWT usan el azul de las categorías WorldTour; los tres mejores ProTeams masculinos y los dos mejores Women’s ProTeams señalan las invitaciones obligatorias al WorldTour, los ProTeams masculinos cuarto y quinto las invitaciones a ProSeries y los ProTeams fuera del top-30 llevan una franja roja. El botón de información muestra la fecha UCI, la fuente y el carácter provisional de las invitaciones; tocar una fila explica su consecuencia reglamentaria. Espejo web + iOS `1238`.**

**`428` / `4.1.2` = PUNTOS UCI EN RESULTADOS:** cada clasificación muestra automáticamente la columna «UCI» cuando su categoría concede puntos según el reglamento 2026. Admite etapas, lideratos provisionales, generales finales, puntos/montaña de grandes vueltas, las cinco bandas WorldTour por nombre, campeonatos y CRE; conserva centésimas en repartos por equipo o ex-aequo. Espejo web + iOS `1237`.**

**`427` / `4.1.2` = CLASIFICACIONES COMPACTAS + PREPARACIÓN UCI:** Resultados adopta el lenguaje compacto de la web en el selector de etapas, las pestañas y las filas individuales/CRE, conservando el autocentrado horizontal. El contrato de datos y la tabla dejan preparado un slot opcional «UCI» antes de Tiempo/Pts; mientras ninguna fila traiga `uciPoints`, la columna no se crea ni ocupa espacio. Espejo iOS `1237`.**

**`426` / `4.1.1` = PREPARACIÓN DE RELEASE:** nueva build de Android 4.1.1.

**`424` / `4.1.1` = MINIPERFILES POST-CARRERA Y RESPIRACIÓN EN FICHAJES:** los miniperfiles de Hoy y Competición permanecen visibles al entrar en Resultados/Revive y se completan al 100%, también en CRI/CRE; los botones y el resto del modo terminado no cambian. En Mercado de Fichajes se separan 6 dp el título «Últimas confirmaciones» y las cápsulas Fichajes/Renovaciones. Espejo iOS `1236`.

**`423` / `4.1` = ORDEN DEL LIBRO DE RUTA: en Jornada, el Libro de Ruta común a la competición se muestra tras la web oficial y antes de Dorsales, igual que en Competición y web. Espejo iOS `1230`.**

**`422` / `4.1` = FLECHAS CIRCULARES: las flechas de acciones pasan a un control circular de borde, con fundido de solo 24 dp y área táctil conservada. Espejo iOS `1229`.**

**`421` / `4.1` = FLECHAS AÚN MÁS COMPACTAS: la navegación lateral de acciones se estrecha a 40 dp y el fundido ocupa menos espacio. Espejo iOS `1228`.**

**`420` / `4.1` = FLECHAS MÁS DISCRETAS: la navegación de acciones se ancla más al borde y sus fundidos laterales se reducen. Espejo iOS `1227`.**

**`419` / `4.1` = CALENDARIO SIN 2025: Temporada deja de ofrecer años anteriores en el selector; conserva el actual y los futuros publicados.**

**`418` / `4.1` = LIBRO DE RUTA INTERNO: en Competición, el Libro de Ruta se abre con el navegador interno; Notificaciones ya no deja un separador al final de la tira.**

**`417` / `4.1` = FLECHAS DE ACCIONES: las flechas de la tira horizontal llevan directamente al inicio o al final, por lo que desaparecen en un solo toque al alcanzar cada extremo.**

**`416` / `4.1` = NOTIFICACIONES SIEMPRE ACCESIBLES: el control para seguir una jornada o competición se muestra aunque aún no se hayan concedido permisos de notificación. Espejo iOS `1225`.**

**`415` / `4.1` = ACCIONES ARMÓNICAS: las acciones de jornada y competición adoptan las celdas azul tenue de iOS, icono y texto de una línea, separadores y fundidos laterales a toda altura; también Notificaciones y Añadir al calendario. Paridad visual con iOS `1223`.**

**`414` / `4.1` = TEMPORADA 2027: Mes permite escoger el año siguiente antes del 1 de enero, para consultar e introducir el calendario ya publicado. Espejo iOS `1224`.**

**`413` / `4.1` = NOTIFICACIONES + FICHAJES: el panel puede enviar al Mercado de Fichajes o a la ficha de un equipo de su temporada; Android e iOS resuelven los destinos `transfers` y `team/{teamId}`. Espejo iOS `1223`.**

**`412` / `4.1` = RETORNO DE ACCIONES: Android añade la flecha izquierda al avanzar; iOS adopta flechas bidireccionales para tiras largas. Espejo iOS `1220`.**

**`411` / `4.1` = AVANCE DE ACCIONES: las tiras horizontales de documentación muestran una flecha de avance solo cuando existen acciones fuera del área visible; al tocarla desplazan suavemente el contenido y desaparecen al final.**

**`410` / `4.1` = ACCIONES AGRUPADAS: la documentación de jornadas y competición adopta celdas compactas de ancho fijo, icono superior, etiqueta a una línea y separadores; iOS conserva el desplazamiento horizontal nativo conforme a sus pautas. Las duplicidades pasan a «Perfil + Datos»/«Profile + Data» y «Mapa 3D»/«3D Map». Espejo iOS `1218`.**

**`409` / `4.1` = LIBRO DE RUTA + ACCIONES: nuevo asset de competición «Libro de Ruta»/«Technical Guide», accesible desde cualquier etapa y Competición; las acciones pasan a una tira horizontal de ancho fijo para evitar acumulaciones de filas. Las guías grandes no entran en la descarga automática sin conexión. Espejo iOS `1217`.**

**`408` / `4.0.7` = DORSALES: los badges y botones de la lista de salida pasan de «Inscritos/Inscritas» a «Dorsales» en Android, web e iOS; «Lista provisional» se conserva en castellano y pasa a «Provisional Startlist» en inglés. Espejo iOS `1216`.**

**`407` / `4.0.6` = FICHAJES: el aviso de fuentes enlaza en cada plataforma los nombres de los siete periodistas acreditados a sus cuentas de X, como en Datos abiertos. Espejo web + iOS `1215`. Sin cambio de versión de marketing.**

**`406` / `4.0.6` = INSCRITOS: se retira el aviso «Lista al inicio de la competición. No se actualiza después de las etapas (no spoilers)». Las listas ya reflejan los abandonos mediante los resultados propios. Espejo web + iOS `1214`. Sin AAB a propósito.**

**`405` / `4.0.5` = JORNADAS: cuando hay archivo de mapa oficial y mapa interactivo GPX, muestra ambos chips en ese orden, igual que los perfiles.**

**`404` / `4.0.5` = corrección de versión de marketing antes de publicar: sustituye la 4.0.4 preparada por la primera versión pública de este cambio.**

**`403` / `4.0.4` = JORNADAS: el detalle muestra el chip naranja «Sin confirmar» cuando `tvStatus = pending`, incluso si ya existe un canal provisional o Live texto. Reutiliza el lenguaje visual de Hoy.**

**`402` / `4.0.4` = JORNADAS: pull-to-refresh sustituye la instantánea completa de la carrera (jornadas, descripción editorial, carrera, emisiones y assets) y propaga altas, bajas y vaciados del backend sin conservar elementos obsoletos en Room.**

**`401` / `4.0.3` = OFFLINE: «Actualizar ahora» puede sustituir un one-shot automático pendiente o en backoff, evitando que `KEEP` absorba la petición explícita; los disparos automáticos conservan `KEEP` y el cooldown de 30 minutos. Sin cambio de versión de marketing.**

**`400` / `4.0.3` = OFFLINE + SUPABASE: la sincronización automática deja de reemplazar trabajos activos y aplica un cooldown persistente de 30 minutos a reintentos anómalos, incluso tras reiniciar el proceso; «Actualizar ahora» conserva el refresco manual. Las peticiones de Android incluyen versión y build en el User-Agent para poder atribuir futuros picos de API.**

**`399` / `4.0.3` = FICHAJES: la flecha de destino en «Últimas confirmaciones» corrige ligeramente su línea base para quedar centrada verticalmente con corredor y equipo.**

**`398` / `4.0.3` = FICHAJES: el cintillo de Hoy reutiliza el mismo icono `SyncAlt` que la pestaña inferior.**

**`397` / `4.0.3` = FICHAJES: se retira el icono artesanal demasiado macizo y se adopta `SyncAlt`, el símbolo Material limpio más próximo a `arrow.left.arrow.right` de iOS.**

**`396` / `4.0.3` = FICHAJES: el icono inferior usa ahora dos flechas horizontales superpuestas propias, en vez del símbolo Material separado.**

**`395` / `4.0.3` = FICHAJES: el icono inferior recupera las dos flechas horizontales superpuestas, igual que iOS.**

**`394` / `4.0.3` = CARGA DE MARCA: en las secciones Android, iconos y rótulo pasan a una zona central independiente del perfil inferior, igual que en el splash y sin solapamientos.**

**`393` / `4.0.3` = CARGA + FICHAJES: las cargas iniciales de Resultados y Fichajes —incluido el detalle de equipo— usan el perfil animado de marca; la flecha de volver de Fichajes solo aparece al llegar desde el cintillo de Hoy; su icono de pestaña pasa a dos flechas verticales.**

**`392` / `4.0.3` = CARGA DE MARCA: Android completa los cargadores de marca con los iconos de calendario y ciclista sobre el titular, igual que iOS. Espejo iOS `1195`.**

**`391` / `4.0.3` = CARGA DE MARCA: la carga inicial de Clasificaciones adopta también el perfil de ruta animado, anclado al borde inferior en ambas apps. Espejo iOS `1194`.**

**`390` / `4.0.3` = CARGA DE MARCA: los perfiles animados de los estados de carga de pantalla completa dejan de quedar centrados. Ahora se anclan a sangre al borde inferior, ocupando todo el ancho, tanto en Android como en iOS. Espejo iOS `1193`.**

**`389` / `4.0.3` = CARGA DE MARCA: el splash propio de Android e iOS incorpora un perfil de ruta animado con ciclista, manteniendo intacta la pantalla de arranque estática del sistema. El perfil de iOS queda anclado al borde inferior, a sangre por todo el ancho y con el relleno hasta el extremo, igual que en la web. El estado inicial de «Hoy» en Android y los loaders de marca de iOS reutilizan el mismo lenguaje visual; se respeta Reducir movimiento. Espejo iOS `1192`.**

**`388` / `4.0.2` = HOY + AJUSTES: el pull-to-refresh de Hoy conserva las carreras visibles mientras descarga, como Resultados y Fichajes; las respuestas lentas de otro día ya no pueden sobrescribir la jornada seleccionada. En iOS, las recargas manuales, automáticas y al volver a primer plano comparten esa continuidad; Ajustes usa el titular de navegación compacto, igual que Resultados y Fichajes.**

**`387` / `4.0.2` = PLACEHOLDERS: «Sin información extra» se muestra como alerta nativa en Hoy, Mes y Temporada; conserva el acceso a la web oficial cuando existe. Espejo iOS `1191`.**

**`386` / `4.0.2` = FICHAJES: el botón de información incorpora los periodistas y medios que se acreditan como fuentes en Datos abiertos; en web móvil se presenta en un popup, en lugar de desplegarse bajo el título. Espejo web + iOS `1188`.**

**`385` / `4.0.2` = RESULTADOS + MERCADO: el titular del feed pasa a «Últimos Resultados»; Fichajes sustituye el enlace de fuentes por un botón de información junto al título, con la misma explicación de Datos abiertos (tooltip en web de escritorio/tablet y diálogo nativo en las apps); sus fichas de equipo conservan como título superior «Mercado de Fichajes 2027»; el selector de enlaces FirstCycling/PCS de Hoy pasa a `confirmationDialog` nativo en iOS (Android ya usaba `AlertDialog` nativo). Espejo web + iOS `1187`.**

**`384` / `4.0.1` = MERCADO: cada ficha de «Últimas confirmaciones» enlaza al equipo de DESTINO del fichaje, siempre que tenga ficha en el mercado 2027; la fila completa es tocable. Espejo web + iOS `1185`. Incluye el bump de `versionName` sincronizado a 4.0.1.**

**`383` / `4.0` = MERCADO: se elimina el reborde lateral violeta del aviso de continuidad de equipo en duda en su ficha. Se mantienen el fondo y el texto. Espejo web + iOS `1184`.**

**`382` / `4.0` = compatibilidad Google Play: `targetSdk` y `compileSdk` suben a Android 16 / API 36; Google Play Billing pasa a 8.2.1. La actualización de Billing exige Kotlin 2.2, por lo que se actualizan también Kotlin/KSP a 2.2.10, AGP a 8.10.1 y Gradle a 8.11.1. Room pasa a 2.7.2 para compatibilidad con KSP 2. Los tres tests Robolectric se fijan en API 35 (máxima soportada por Robolectric 4.14.1); la app se compila contra API 36.**

**`375` / `4.0` = MERCADO: la FILA ENTERA del corredor es clicable (no solo el nombre) — corrección de `373` a petición de Dani. `PersonRow` pone el `.clickable { onLinkTeam(linkTeamId) }` en el `Modifier` del `Row` (antes del `padding`, para que el ripple cubra todo el ancho) cuando hay `linkTeamId`; el contenido vuelve a ser un único `personLine` (nombre en Medium, SIN accent) — el realce de "clicable" es la fila entera, no el color del nombre; se conserva la truncación a una línea con "…" (`maxLines=1`/`TextOverflow.Ellipsis`) de `374`. Espejo web + iOS `1180`. Solo UI (`TransfersLogic` intacto); compile + tests verdes + AAB en `~/Downloads/app-release-375.aab`. **`374` / `4.0` = MERCADO: corredor + equipo trunca con "…" a una línea en la vista de equipo (`PersonRow`, `maxLines=1`/`TextOverflow.Ellipsis`) — sin doble altura; los badges de contrato/rumor quedan fijos a la derecha. Espejo web (`.tr-row__body` con `text-overflow: ellipsis`) + iOS `1179`. **`373` / `4.0` = MERCADO: el corredor ENLAZA a la ficha del equipo del movimiento (no hay ficha pública de corredor). Regla Dani: quien LLEGA enlaza al equipo del que VENÍA (`fromTeamId`); quien SE MARCHA, al equipo AL QUE VA (`toTeamId`, destino 2027). Solo si ese equipo tiene ficha en el mercado (retirada / destino fuera de las 4 divisiones → sin enlace); Continúan/En duda/Terminan contrato/feed sin enlace. `MovementCard` calcula el `linkTeamId` (set de `data.seasons`) y lo pasa a `PersonRow`. Espejo web + iOS `1178`. Solo UI (`TransfersLogic` intacto → sin tests nuevos). ⚠️ El header de la tabla iba por `371` mientras `build.gradle.kts` estaba en `372` → resincronizado (373). **`371` / `4.0` = MERCADO: contrato VITALICIO — el badge muestra ∞ cuando el año de contrato es 9999 (año centinela `dateTo=9999-12-31`); ordena el primero de «Continúan» (año desc). Espejo web + iOS `1176`. **`370` / `4.0` = MERCADO: en «Llegan», los fichajes CONFIRMADOS van antes que los rumores (apellido dentro de cada grupo); +1 test. Espejo web + iOS `1175`. **`369` / `4.0` = MERCADO: pantalla principal a DOBLE PANEL con scroll independiente («Últimas confirmaciones» ~38% arriba + equipos ~62% abajo, ambos siempre visibles; `Column` con `weight` + `LazyColumn` anidados) + la fecha del feed pasa a «día de la semana + día de mes, SIN año» (`DateFormatting.formatDateWeekdayNoYear`, espejo web/iOS). Espejo web (móvil) + iOS `1174`. **`368` / `4.0` = MERCADO: «Continúan» desempata por APELLIDO (2º factor tras el año), «Llegan» ordenado solo por apellido (antes cronológico inverso). +2 tests. Espejo web + iOS `1173`. **`367` / `4.0` = MERCADO: rótulo «Mercado de Fichajes» con F mayúscula (heading + cintillo) + el feed «Últimas confirmaciones» se corta en 5 fechas distintas U 8 fichajes, lo que antes se alcance (`TransfersLogic.limitedFeed`, `FEED_MAX_DAYS=5`/`FEED_MAX_ITEMS=8`; +2 tests). Espejo web + iOS `1172`. **`366` / `4.0` = MERCADO: «Se marchan» ordenado por categoría del equipo de destino (WT→WWT→PT→PRW→resto), luego alfabético por nombre del equipo, retiros al final; «Terminan contrato» alfabético por apellido. `teamDetail` gana `ridersById`/`categoryByTeamId`/`teamNameById`. Espejo web + iOS `1171`; +2 tests. **`365` / `4.0` = MERCADO: una sección vacía (Continúan/En duda/Terminan contrato/Llegan/Se marchan) NO se muestra —título incluido—; si el equipo no tiene ningún movimiento, un único aviso. Espejo web + iOS `1170`. **`364` / `4.0` = MERCADO: «Continúan» se ordena por año de contrato DESC (2030 → … → sin año al final), alfabético como desempate (`teamDetail` ordena los `StayingRow` por `contractUntil`). Espejo web + iOS `1169`; +1 test. **`363` / `4.0` = MERCADO: sección «Terminan contrato» (fin de contrato sin destino) entre «En duda» y «Llegan»; «En duda» sin badge DUDA (redundante con la sección). `teamDetail` parte las salidas en `contractEnds` (transfer con toTeamName='?') + `departures` (fichaje con destino o retirada); +1 test. Espejo web + iOS `1168`. **`362` / `4.0` = MERCADO: en «Se marchan», una retirada usa la etiqueta con mayúscula «Se retira» (string nueva `transfers_retired`, ≠ `transfers_retires` en minúscula del feed). Espejo web + iOS `1167`. **`361` / `4.0` = MERCADO: CONTINÚAN = solo quien estaba en el equipo en 2026** (`teamDetail` filtra `staying` por `currentTeamId == teamId`; un fichaje de fuera tiene afiliación 2027 pero va a "Llegan", no a "continúan"). `ridersByAffiliation` trae `currentTeamId`; +1 test `stayingOnlyIncludesRidersFrom2026Roster`. Espejo web + iOS `1166`. compile + tests verdes (sin AAB). **`360` / `4.0` = MERCADO: el detalle de equipo lee la plantilla 2027 MATERIALIZADA (`ridersByAffiliation` en `SupabaseService.kt`, year=2027 → contrato del `dateTo` de la afiliación) en vez del roster 2026; orden de secciones continúan→duda→LLEGAN→se marchan (llegan antes); año de contrato como BADGE (`YearBadge`, `PersonRow` + feed row) y contrato SIN "hasta" (`transfers_until` = `%1$d`); separador " · " atenuado entre nombre y movimiento en el feed. Espejo web + iOS `1165`. Ver "🎽 MERCADO — panel equipo-céntrico" arriba. compile + tests verdes (AAB NO generado, Dani no lo quiso). **`359` / `4.0` = el botón «Cómo se hace este proyecto» (Ajustes › Datos abiertos) adopta el aspecto del CTA «Ver clasificaciones» (espejo iOS `1163`).** Antes la sección solo tenía el texto de licencias terminando en «Más detalle en calendariociclismo.app/abierto.html» (SIN botón). Ahora se retira esa coletilla del cuerpo (`settings_licenses_body`, ES+EN) y se añade un **botón relleno de acento** nuevo (`Box` + `background(primary, RoundedCornerShape(3))` + texto `onPrimary` semibold `labelLarge`), calcado del CTA primario de `ResultsButtonsCard` (`StageScreen`). Abre `/abierto.html` o `/en/open/` según `LocaleHolder.shouldShowEnglishContent` con `Intent.ACTION_VIEW`. String nueva `settings_open_project_cta` (ES+EN). `SettingsScreen.kt` (+ imports Box/background/FontWeight/Role/Intent/toUri). Compile/tests NO ejecutados (entorno sin Android SDK/secrets) → se confía en CI; AAB pendiente de generar en local. **`357` / `4.0` = reescritura del COPY de la pantalla "sin anuncios" (`PremiumShowcase`; espejo iOS `1160`).** Se alinea con el tono de las páginas web `about`/`abierto` (gancho de producto + nota de sostenibilidad factual, sin el matiz "proyecto independiente" como súplica). Strings `onboarding_premium_title` ("Sin anuncios, independiente" / "Ad-free and independent"), `onboarding_premium_body` y `onboarding_premium_benefit_support` ("Ayuda a cubrir el coste de los servidores y el mantenimiento") en `values/` + `values-en/`. Solo strings, sin cambios de lógica. **`356` / `4.0` = la pantalla "sin anuncios" (`PremiumShowcase`) se RE-muestra una vez a quien actualiza desde 2.x/3.x a 4.0+ (espejo iOS `1159`; ver "Pantalla sin anuncios para upgraders" arriba).** El gate del paso `PremiumShowcase` en `nextOnboardingStep` (`MainActivity.kt`) pasa de `adsIntroDone` (ya `true` en todo el parque 2.3+) al flag NUEVO `adsIntroV4Done` (`AppPreferences`, key `ads_intro_v4_done`), que nace en `false` para todos → dispara la pantalla una vez tras actualizar. Limpieza en el mismo commit: se BORRARON los gates viejos `adsIntroDone` (2.3) y `premiumShowcaseDone` (2.0), ya saturados y sin lectores → `markDone` (`PremiumShowcaseOnboardingScreen.kt`) marca solo `adsIntroV4Done`. Compile/tests NO ejecutados (entorno sin Android SDK/secrets) → se confía en CI. **`355` / `4.0` = RESULTADOS: el override de país por jornada se aplica aunque la clasificación no traiga `raceDayId` (espejo web + iOS `1158`; ver "🏴 RESULTADOS" arriba).** `UciResultsData` gana la lista `raceDays`; el header (`ResultsScreen`) se resuelve EN MEMORIA por `raceDayId` → `stageNumber` (`daysById`/`daysByStage`, sin fetch `raceDayById`); `ResultsFeedLogic` gana `rdByRaceStage` como fallback en `rdFor`. +2 tests. Compile/tests NO ejecutados (entorno sin Android SDK) → se confía en CI. **`354` / `4.0` = RESULTADOS DE DOBLE SECTOR A/B (etapa partida 3A/3B) — espejo web + iOS `1156`; ver "🔀 RESULTADOS DE DOBLE SECTOR" arriba.** La pantalla de Resultados agrupaba por `stageNumber` entero → 3A y 3B (mismo número) se MEZCLABAN en una sola pestaña. Ahora la clave de agrupación es **sector-consciente** (`UciResultsLogic.resultStageEntryKey` → "final"/"3"/"3A"): cada sector se separa por su `raceDayId`. `stageKeyOf(Int?)` → claves `String`; `ResultsStageSelector` (cápsula "3A"/"3B"), `Routes.results` gana `sfx`, los CTA de la jornada/Hoy/competición pasan `stageSuffix`, y `applyCancelledStages` descarta la etapa cancelada por `raceDayId` (no por número, que en un doble sector se llevaría por delante al sector hermano) + arrastre con sufijo. `UciResultsData` gana `sectorSuffixByRaceDayId`/`sectoredStageNumbers` (de `sectorSuffixMap`). **SIN migración/cambio de esquema** (no toca cómo decodifican las apps 3.2.x en producción). +9 tests en `UciResultsLogicTest`. Compile/tests NO ejecutados (entorno sin Android SDK) → se confía en CI. **`353` / `4.0` = MERCADO: la chapa 2027 OCULTA muestra los COLORES ANTIGUOS (2026) del equipo preexistente, o nada si es nuevo (espejo web + iOS `1155` + mig. 129; ver "🎽 MERCADO" arriba).** `TransfersLogic.badgeSeason(season, prev)` (+3 tests) resuelve la chapa efectiva (publicado → fila 2027; oculto + fila 2026 → colores antiguos; oculto + sin fila 2026 → null); `MarketData.prevSeasonsByTeamId` trae las filas 2026 completas (no solo nombres); `SeasonBadge`/`TeamRow` dejan de auto-gatearse por `badgeVisible` (el gate `!= null` vive en el call site). Compile/tests NO ejecutados (entorno sin Android SDK ni secrets) → se confía en CI; AAB pendiente de generar en local.  **`351` / `4.0` = test `IsDevDropProbeTest`: comprueba ejecutablemente que el modelo Android sobrevive al `DROP COLUMN` de la 128 (kotlinx aplica el default al faltar la clave), el contraste del caso iOS que SÍ rompe. Solo test, sin cambio de comportamiento. **`350` / `4.0` = `isDev` RETIRADO del modelo (espejo iOS `1154`; ver "🏷️ `isDev` RETIRADO" arriba).** `StartlistTeam.kt` pierde el campo y `displayName` devuelve `teamName` tal cual (ya no añade " (Devo)"). Kotlin aguantaría el `DROP COLUMN` sin romper (kotlinx con default `false`), pero se retira igual para que ambas plataformas queden en el mismo estado antes de aplicar la 128. `compileDebugKotlin --rerun-tasks` + `testDebugUnitTest` verdes + AAB en `~/Downloads/app-release-350.aab`. ⚠️ La tabla iba por `347` mientras `build.gradle.kts` estaba en `349` (bumps previos sin actualizarla) → resincronizada aquí. **`347` / `4.0` = MERCADO: la chapa 2027 OCULTA ya no deja hueco — se retira el placeholder circular (espejo de la web e iOS `1151`).** Reportado por Dani al ver la lista en TestFlight: "¿por qué me siguen saliendo esas chapas vacías?". NO era un bug: el círculo gris era el **placeholder deliberado** de la mig. 122 (`.tr-badge-ph` web / `SeasonBadge` else / `TransfersSeasonBadge` else), pensado para que la lista no bailara al ir activando chapas equipo a equipo. Con 55 equipos y casi ningún kit anunciado, 18 círculos vacíos por división se leen como "faltan datos" → **decisión Dani: fuera el hueco**, la fila arranca en el nombre. Trade-off asumido: mientras convivan equipos con y sin chapa, las filas quedan desalineadas entre sí (se recolocan solas según se activen). ⚠️ **GOTCHA de layout, la razón de que el fix no sea "devolver una vista vacía"**: los 4 call sites viven en contenedores con separación declarada (`HStack(spacing:)` iOS, `Row(Arrangement.spacedBy(...))` Android) → un `EmptyView`/composable vacío **gasta el spacing igual** y deja el hueco que se quiere quitar. Por eso el badge se GATEA en el call site (`if season.badgeVisible` Android, `if TransfersSeasonBadge.isVisible(season)` iOS — helper estático nuevo) en vez de invocarlo siempre. La web no sufre esto (`badgeOrPlaceholder` devuelve string vacío y el `gap` del flex no reserva nada para un hijo inexistente) → ahí basta con `return ''` + borrar el CSS `.tr-badge-ph`, que queda muerto. Verificado en navegador contra prod en los dos sentidos: WT sin marcar = 18 filas, 0 placeholders, la fila arranca en el nombre; marcando Alpecin (dato de prueba, **revertido**: BD queda 417×2026 true + 55×2027 false) → chapa de 26 px en la lista y de 44 px en la cabecera de su vista de equipo, 0 placeholders, sin errores de consola. Web 386 tests + Android compile + tests + AAB en `~/Downloads/app-release-347.aab`. **`346` / `4.0` = MERCADO: la chapa 2027 pasa a OPT-IN ESTRICTO en el cliente (espejo de la web e iOS `1150`).** Reportado por Dani: "creo que la chapa 2027 se muestra como opt-out, debería ser opt-in". Diagnóstico: **en producción NO había ningún equipo mostrando chapa 2027 sin marcar** — las 55 filas `team_seasons` 2027 tienen `badgeVisible=false` (la siembra de la 122 lo pasó explícito) y las 417 de 2026 `true`. Lo que SÍ era opt-out es el **fallback cuando el dato falta**: el default de deserialización era `val badgeVisible: Boolean = true` → una fila creada sin nombrar la columna (SQL ad-hoc, seeding futuro; la columna en BD es `NOT NULL DEFAULT true`) o un `select` que no la pida nacería/leería VISIBLE. FIX (decisión Dani: **NO tocar el default de la BD**, solo los clientes): default a `false` → "no sé" = "no la pinto" (enseñar un kit 2027 inventado es peor que no enseñar ninguno). ⚠️ El default vive en el modelo `TeamSeason`, que es COMPARTIDO (render temporal de startlists/resultados), pero `badgeVisible` **solo lo lee Fichajes** (`SeasonBadge`) — verificado con grep en las 3 plataformas → no afecta a startlists ni a resultados, que además tienen fallback a `teams`. El panel ya era opt-in y no se toca (`loadTeamSeason27` deja el checkbox desmarcado sin fila; `saveTeamSeason27` escribe siempre el valor del checkbox). Verificado end-to-end en navegador contra prod: WT = 18 equipos, 0 chapas / 18 huecos neutros; marcando `badgeVisible=true` en Alpecin (dato de prueba, **revertido**: BD queda 417×2026 true + 55×2027 false) → 1 chapa + 17 huecos, sin errores de consola. compile + tests verdes + AAB en `~/Downloads/app-release-346.aab`. **`345` / `4.0` = MERCADO: dudas (corredor + equipo) + fecha ocultable (mig. 123; espejo de la web e iOS `1149`).** Ver "🔁 MERCADO — DUDAS" arriba para las reglas. `TransfersLogic`: `confirmedFeed` exige `dateVisible`; `teamDetail` separa las renovaciones EN DUDA a un bucket propio (`DoubtRow`/`TeamDetail.doubtful`), las saca de "continúan" y NO les aplica el contrato de la duda; secciones reordenadas a continúan → **en duda** → se marchan → llegan. Modelos: `RiderTransfer.dateVisible` (default true) + `TeamSeason.continuityDoubt`. UI: `DoubtBadge` violeta (`TRANSFERS_DOUBT_COLOR` `#8B5CF6`, ≠ ámbar del rumor), chip "Continuidad en duda" en la fila de equipo y `TeamDoubtNotice` en su detalle; strings ES+EN. Las queries no cambian (`select {}` trae las columnas nuevas). **7 tests nuevos** en `TransfersLogicTest` (22 verdes): feed sin fecha oculta, oculta que sí cuenta en el equipo, feed sin dudas, duda saca de continúan, duda no pisa contrato, renovación confirmada sí, salida gana a la duda, orden por apellido. compile + tests verdes + AAB en `~/Downloads/app-release-345.aab`. **`344` / `4.0` = la ETAPA CANCELADA ya NO muestra el trofeo de resultados en las cards de Hoy ni de competición (paridad iOS `1148`).** Reportado por Dani: "en la vista Hoy en una etapa cancelada me estás mostrando la copa cuando los resultados en una etapa cancelada son IRRELEVANTES". CAUSA: el gate de la card es `hasInhouse || RaceLogic.shouldShowResults(...)`. `shouldShowResults`/`noIdsAndPastDeadline` SÍ excluyen la cancelada (`if (rd.isRestDay || rd.isCancelledDay) return false`), pero **`hasInhouse` la colaba por la otra rama del `||`**: `inhouseStagesForDays` solo filtraba `rowCount > 0`, sin mirar `isCancelledDay` → si el cron volcó filas ANTES de la cancelación (o la jornada hereda por `stageNumber`), la jornada entraba en el mapa y encendía el trofeo. Verificado contra prod: 4 jornadas canceladas con filas volcadas (Qinghai E6 con 5+23 filas, otra E7 con 41, otra E3 con 124) → todas mostraban copa. FIX en el MAPA, no en el call site (lo comparten Hoy y competición, y **ambas tenían el bug**: mismo gate en `TodayScreen`/`RaceScreen`): `inhouseStagesForDays(raceId, days, cancelledDayIds)` salta las jornadas canceladas (`if (dayId in cancelledDayIds) continue`); los 2 call sites pasan el set. Con el trofeo apagado, el **Revive** también se apaga solo (`reviveUrl` cuelga de `showResults || noIdsAndPastDeadline`, y ambos excluyen la cancelada) — coherente: en una cancelada no hay carrera que revivir. ⚠️ **NO se toca el CTA de la FICHA** (`hasInhouseResults = has || rd.isCancelledDay` en `StageScreen`, de `342`/`343`): ahí la página de resultados SÍ es relevante (aviso de cancelación + generales ARRASTRADAS de la etapa anterior). La distinción es card (irrelevante, no hubo clasificación) vs ficha (explica la cancelación). Verificado: `compileDebugKotlin --rerun-tasks` + `testDebugUnitTest` verdes. **Sin AAB a propósito** (Dani). **`343` / `4.0` = ETAPA CANCELADA en Resultados: pestaña "Etapa" con aviso + generales ARRASTRADAS de la etapa anterior (paridad web + iOS `1146`).** Ver la sección "Resultados de una ETAPA CANCELADA" arriba para la regla de producto y el detalle. Lógica pura `UciResultsLogic.applyCancelledStages` (+6 tests, incl. doble sector); `loadResultsData` carga las jornadas SIEMPRE y sintetiza antes del guard de Empty; campos sintéticos `isCancelledStage`/`carriedFromStage` (`@Transient`); avisos `CancelledStageNotice`/`CarriedStandingsNotice` + string `results_carried_standings` ES+EN. Verificado en emulador contra prod con la E6 del Qinghai. **`342` / `4.0` = recupera el CTA "Ver clasificaciones" y el "Revive" de la ficha de una etapa cancelada** (regresión mía: `hasInhouseResults` dependía de tener clasificaciones volcadas —una cancelada no las tiene ni las tendrá— y el corte de TV se llevaba por delante el Revive, que en una cancelada en carrera es vídeo REAL de lo que sí se disputó, curado con `showInRevive`). Ahora `hasInhouseResults = has || isCancelledDay` y el Revive sobrevive sin esperar al T+30 (en una cancelada ya no hay directo que esperar). **`341` / `4.0` = ETAPA CANCELADA: se retiran perfil, horarios y TV/Live Texto (paridad web + iOS `1144`).** Cazado por Dani con la etapa 6 del Tour of Magnificent Qinghai (cancelada en carrera por meteorología): la card seguía pintando el horario y el badge de TV, y la ficha ofrecía el chip "Live texto" de una etapa que no se corrió. Una jornada cancelada NO se corre ni se emite → fuera todo lo que anuncie o siga la carrera; la ficha SIGUE siendo accesible (recorrido, perfil, rutómetro, mapa: describen la etapa que estaba trazada) y un aviso la encabeza. Cambios: `TodayScreen` oculta `TVBadge` y la columna de tiempos con `isCancelledDay`; `StageScreen` oculta `TimeSection` y `BroadcastSection`, filtra el asset `live_text` de los chips (espejo del filtro de `buildActionButtons` en la web) y añade el banner "Etapa cancelada"/"Carrera cancelada" (strings `race_cancelled` ES+EN nuevas) en `StageInfoBlock` (compartido con el perfil de elevación). El mini-perfil ya se ocultaba. **Fix de alineación (cazado por Dani en la captura):** el texto "Cancelada" quedaba ALTO respecto a los badges vecinos — `FlowRow` alinea al BORDE SUPERIOR y `CategoryBadge`/`StageTypeBadge` llevan fondo + `padding(vertical = 2.dp)` propio mientras el aviso es texto pelado → `Modifier.align(Alignment.CenterVertically)` sobre el texto en `TodayScreen` y `RaceScreen`. ⚠️ `FlowRow(itemVerticalAlignment = …)` NO existe en Compose BOM 2024.12.01 (el proyecto) → usar `Modifier.align` dentro del scope. Verificado en emulador Pixel 9a contra prod con la etapa real: banner + sin horario + sin TV + sin Live texto + "Cancelada" centrada; compile + tests verdes. **AAB NO generado a propósito** (Dani: "aún no se va a aplicar en Android"). **2ª pasada (mismo día, feedback de Dani):** (a) **la jornada cancelada NO se atenúa** — se retira el `alpha(0.5f)` de `RaceScreen` (fila de etapa) y de `MonthScreen` (que solo lo aplica ya a la CARRERA cancelada, `race.isCancelled`, que sí sigue atenuada: no se corre en absoluto). El aviso rojo ya lo dice y atenuarla la hacía parecer deshabilitada teniendo ficha accesible; (b) **la fila de etapa cancelada vuelve a ser PULSABLE** en la vista de competición (`isInteractive` pasa a `!rd.isRestDay`; el `onClick` del call site ya no la filtra) → recupera su chevron y abre su ficha, igual que en la web. Solo el DESCANSO queda inerte (no tiene ficha). Verificado en emulador: chevron visible + tap abre la ficha. **`339` / `4.0` = Fichajes: el equipo de ORIGEN de un movimiento se pinta con su nombre de la temporada EN CURSO (2026), no con el del mercado (2027) — paridad web + iOS `1141`.** Reportado por Dani: "el equipo del que viene un corredor debería llamarse como se llama en 2026". `TransfersLogic.teamLabel` resolvía SIEMPRE contra `team_seasons[2027]` con `teams` de fallback → un corredor que sale de un equipo renombrado para 2027 aparecía saliendo del nombre NUEVO, que aún no existe. Hoy no se nota (las 55 filas 2027 nacieron copiando la identidad 2026 → 0 divergencias, y `teams` == 2026 por el trigger `sync_team_to_season`): es un bug LATENTE que se activa al primer renombre de sponsor. FIX: `MarketData.teamNamePrev` (mapa del año anterior, lo carga `loadTransfersMarket` con `api.teamSeasons(season - 1)` en paralelo y degrada a vacío si falla) + `TransfersLogic.TeamSide` (FROM/TO) en `teamLabel`, con fallback cruzado al otro año y `teams` solo si el equipo no está en ninguno. Call sites FROM: retirada + origen del fichaje (`TransfersScreen`) y la lista "llegan" (`TransfersTeamScreen`); el destino y las renovaciones siguen en 2027. 2 tests nuevos en `TransfersLogicTest` (14 verdes) + compile + AAB en `~/Downloads/app-release-339.aab`. Verificado end-to-end en navegador contra prod con un renombre 2027 temporal (revertido). **`338` / `4.0` = APPS 4.0 — pestaña FICHAJES (mercado 2027, 3ª posición) sustituye a Buscar (archivado en `archive/buscador-apps-2026/`; deep links `search` caen a Hoy) + onboarding SIN paso de modo offline (borrado `OfflineOnboardingScreen.kt` + strings; la función sigue en Ajustes). Piezas: `Routes.TRANSFERS`/`TRANSFERS_TEAM`, `ui/transfers/TransfersScreen+TransfersTeamScreen` (solo-online, patrón ResultsFeed), `util/TransfersLogic` (+11 tests), modelos `RiderTransfer`/`TeamSeason.badgeVisible`/`RiderProfile.contractUntil`, queries `riderTransfers`/`ridersByCurrentTeam`, `loadTransfersMarket` en el repo, cintillo nativo `targetType='transfers'` → pestaña. `raceDaysForSearch`/`searchRaceDays` podadas (raceDaysInRange se conserva — Mes/Temporada). Ver "🔁 MERCADO DE FICHAJES" arriba. compile+tests verdes + AAB en ~/Downloads/app-release-338.aab. ⚠️ Worktree: copiar `local.properties`/`secrets.properties`/`google-services.json` del checkout principal para compilar.** **`337` / `3.2.13` = "Así está la carrera" (CTA de resultados de la etapa anterior, en la jornada) abre en la clasificación GENERAL (GC), no en la clasificación de etapa (paridad iOS `1139` + web).** Pedido por Dani: el CTA lleva a la GC de la etapa anterior — lo que uno quiere ver de "cómo va la carrera" es la general, no el orden de meta de esa etapa. La ruta de Resultados gana un parámetro de clasificación inicial. **Android**: `Routes.RESULTS` pasa a `results/{raceId}?stage={stage}&class={class}` + `Routes.results(raceId, stage, classKind)`; `AppNavHost` añade el navArg `class`; `ResultsScreen`/`ResultsContent` reciben `initialClassKind` y siembran `activeClassKind` con esa clasificación SOLO si seguimos en la etapa que se abrió (al cambiar de etapa cae a la primera, `stage`) y si existe (degrada con gracia); `StageScreen` navega con `classKind = "gc"` en el CTA "Así está la carrera" (el CTA de la etapa ACTUAL sigue abriendo en su etapa). **Web**: `js/jornada.js` + `js/race-data-modal.js` añaden `#gc` a la URL in-house SOLO del CTA de la etapa anterior (`resultados.js` ya seleccionaba pestaña por hash `#gc`, con fallback a la primera). **iOS** (`1139`): `ResultsRoute`/`ResultsView` ganan `classKind`/`initialClassKind`. Verificado en navegador con el Tour 2026 E4 (`#gc` → pestaña General activa `res-tab--active`, GC acumulada con tiempos absolutos + gaps, 0 errores de consola); `compileDebugKotlin` + `testDebugUnitTest` verdes + AAB (22.8 MB). **`334` / `3.2.12` = fix del ENLACE del badge de TV en directo (Hoy/Competición): una emisión YA EN DIRECTO gana el enlace a una de MAYOR tier que aún no ha empezado (paridad iOS `1136` + web).** Bug reportado por Dani con la etapa 4 del Tour de Francia: la etapa estaba LIVE (Eurosport ya emitiendo) pero el badge enlazaba a RTVE (mayor tier pero aún sin empezar) → clic a una emisión inaccesible. CAUSA: `selectedBroadcast` (`TVBadge.kt`) elegía el enlace SOLO por tier (`broadcastLinkPriority` + `sortOrder`), ignorando si esa emisión de mayor tier ya estaba en el aire; la HORA mostrada sí usaba la más temprana, así que el badge decía "Live" pero enlazaba a la que no había arrancado. FIX: el comparador de `selectedBroadcast` antepone `isBroadcastLive` (startTime ≤ ahora) → entre emisiones EN DIRECTO manda el tier; sin ninguna en directo se conserva el tier puro (pre-carrera un YouTube programado sigue ganando el enlace). Espejo web (`js/broadcast-priority.js` nueva `pickBadgeBroadcast`, usada por `js/app.js` Hoy + `js/race-assets.js` Competición/Campeonatos; 5 tests nuevos en `broadcastPriority.test.js`) e iOS (`selectedBroadcast` en `TVBadge.swift` con `isBroadcastLive`). Sin `.swift` nuevos → NO requiere setup. `compileDebugKotlin` + tests verdes + AAB (33.5 MB). **`333` / `3.2.11` = en CRI/CRE, "salida neutralizada"/"meta estimada" (sin sentido cuando cada corredor/equipo sale y llega en un momento distinto) se sustituyen por "salida 1º corredor/equipo" y "meta último corredor/equipo" (paridad iOS `1135` + web).** `TimeSection` (`ui/stage/StageScreen.kt`) recibe ahora también `race` (antes solo `rd`) y elige la etiqueta por `rd.primaryType`: `itt` → `stage_label_start_first_rider`/`_female` + `stage_label_finish_last_rider`/`_female` (género por `race.isFemale`), `ttt` → `stage_label_start_first_team`/`stage_label_finish_last_team`, resto → las genéricas de siempre (`stage_label_start_neutralized`/`stage_label_estimated_finish`). Strings nuevas ES+EN. Web: `js/shared.js` gana `startFinishLabels(rd, race)` (misma lógica que ya tenían `jornada.js`/`race-data-modal.js`, ahora también en `mapa-pub.js`/`perfil-pub.js`, que mostraban las etiquetas genéricas sin importar CRI/CRE) + wording ES corregido de "ciclista" a "corredor"/"corredora" (`startFirstRider(F)`/`finishLastRider(F)` en `js/i18n.js`; EN ya era neutro, sin cambios). Solo build/tests locales (sin Gradle/Xcode en el entorno) → verificado por lectura de código + suite web verde. **`332` / `3.2.10` = fix de jornadas que desaparecían de Mes/Calendario y del buscador cuando una carrera cae en un año con >1.000 `race_days` publicados (paridad iOS `1134` + web).** Bug reportado por Dani: en Mes no salía NINGUNA etapa del Tour de Francia 2026 salvo la etapa 10 y los dos descansos. Verificado con SQL directo contra la BD: los 23 `race_days` del Tour (21 etapas + 2 descansos) están perfectamente publicados y completos — NO era un dato mal introducido. CAUSA real: `raceDaysInRange`/`raceDaysForSearch` (`SupabaseService.kt`) piden TODO el año de golpe (`refreshRange` desde `MonthScreen`, año completo) sin `.range()` ni `.order()`; PostgREST aplica un tope server-side de 1.000 filas por request que un `.limit()` más alto NO evita (el mismo tope que ya documentaba `panel.js` para `riders_men`/`riders_women` — ver ese comentario). 2026 ya supera las 1.000 jornadas publicadas → la respuesta se trunca en silencio, y como no hay `ORDER BY`, el recorte no sigue la fecha: puede tocar carreras enteras de mitad de temporada (el bloque de 23 filas del Tour cayó casi entero fuera de las primeras 1.000 del orden natural de escaneo, dejando solo 3). Peor aún en Android: `refreshRange` alimenta `deleteByDateRangeNotIn(from, to, keepIds)` → con la respuesta truncada, esa purga BORRABA de Room las jornadas correctamente cacheadas que no vinieron en la página recortada (el fix de `331` para huérfanos por borrado real se convertía en vector de borrado por truncamiento). FIX: `raceDaysInRange` pagina en chunks de 1.000 vía `range()` en bucle hasta agotar el rango, ordenando por `id` (clave única — paginar por `dateKey`, no único, puede saltar/duplicar filas en el borde de página); `raceDaysForSearch` pasa a delegar en `raceDaysInRange` (antes usaba `limit(5000)` creyendo evitar el tope, pero el servidor lo recorta a 1.000 igual). Sin cambios en Room/DAOs. Ver también fix idéntico en `raceDays(from:to:)`/`raceDaysForSearch` de iOS y en `js/buscar.js` (el único punto del WEB que pedía el año completo de golpe; Mes/Temporada web ya cargaban por ventanas de 1-3 meses, a salvo). Compile local NO verificado (entorno sin acceso a Google Maven) → revisar en CI antes de publicar. **`331` / `3.2.9` = las jornadas borradas en el backend ya se propagan a la caché local (Room).** Bug reportado por Dani: en Android salía el "Campeonato de Eslovenia CRI sub23 femenino" (una jornada que nunca existió en la BD / se había borrado) y solo desaparecía al borrar el almacenamiento de la app. CAUSA: `refreshDay`/`refreshRange` (`data/repository/CalendarRepository.kt`) solo hacían `raceDaysDao.upsertAll(...)` (INSERT REPLACE por `id`) → actualizaban/insertaban las jornadas que llegan, pero **nunca borraban las que el backend deja de devolver** para esa fecha; una jornada cacheada y luego borrada en origen quedaba huérfana en Room hasta la purga a 21 días (`deleteStale`) o hasta que el usuario borrase datos a mano — y si su fecha caía fuera de la ventana de sync offline, nunca envejecía. Para broadcasts/assets ya había `deleteByRaceDayIds` previo; faltaba el equivalente para `race_days`. FIX: dos queries nuevas en `RaceDaysDao` — `deleteByDateNotIn(dateKey, keepIds)` y `deleteByDateRangeNotIn(from, to, keepIds)` (`DELETE ... WHERE dateKey(=/BETWEEN) AND id NOT IN (:keepIds)`; con `keepIds` vacío Room emite `NOT IN ()` = siempre verdadero → purga todas las de esa fecha, que es lo correcto si el backend no devuelve ninguna) — cableadas en `refreshDay` (por `dateKey`) y `refreshRange` (por rango) justo tras el upsert. Los broadcasts/assets huérfanos de una jornada borrada son inofensivos (nunca se leen sin su jornada padre) y los barre `deleteStale` a 21 días → no se tocan. Verificado: `compileDebugKotlin` (el KSP de Room valida el SQL nuevo) + `testDebugUnitTest` verdes + `bundleRelease` (AAB 33.5 MB). `versionName` sube a `3.2.9` con el bump. **`329` / `3.2.8` = hora REAL del pie de puerto en la guía de horarios de paso (campo `footTimeUtc`; espejo iOS `1132` + web).** El pie de cada puerto (`climb_foot`) no tenía hora propia: la guía lo INTERPOLABA linealmente por km entre anclas → en un tramo llano→cima le aplicaba la velocidad del llano (pie del Tourmalet a ~32 km/h subiendo un HC, irreal). Ahora `profileSummits[i].footTimeUtc` (hora del rutómetro de ASO, que SÍ lista el pie) se usa como ancla real. Cambio mínimo en `util/SimplifiedGuide.kt` (1 línea: el `climb_foot` usa `s.footTimeUtc` en vez de `null`/estimado) + campo `footTimeUtc: String? = null` en `data/model/ElevationProfile.kt` (`ProfileSummit`, aditivo, `ignoreUnknownKeys`). Render intacto (el `*` solo si `isEstimated`, y el glyph del pie ya es `↗`). Test nuevo `footTimeUtcUsedAsRealAnchor` en `SimplifiedGuideTest`; suite verde + AAB (versionCode 329). Datos: se pueblan los `footTimeUtc` del Tour de Francia 2026 (jornadas en línea) desde los roadbooks de ASO (columna media). **`328` / `3.2.7` = doble sector en orden cronológico (Hoy+Mes) + auto-avance de medianoche + fix del switch rosado (espejo iOS `1121` + web).** (1) **Doble sector**: dos jornadas de la misma carrera el mismo día (etapa en línea + CRI, o sector A/B) se ordenan SIEMPRE por hora de salida (`neutralStartTimeUtc`) y, si falta, por el sufijo A/B (que `annotateDoubleSectors` ya asigna en orden cronológico) — en TODOS los filtros. Desempate añadido en `RaceLogic.byCategory` (Hoy) y `byCategoryWithRaceMap` (Mes), justo tras la categoría UCI. (2) **Auto-avance de medianoche**: el día por defecto sigue siendo la fecha local; si la app queda abierta y cruza la medianoche local (latido 60 s + `ON_RESUME`) avanza sola al día nuevo, SOLO si se está viendo "hoy" (no si se navegó a otro día) — `TodayViewModel.advanceIfNewLocalDay()` (con `lastTodayKey`) cableado en `TodayScreen`. (3) **Switch rosado**: el toggle "Estadísticas de uso" salía rosado porque el `Switch` de M3 sin `colors` tira de roles no sobreescritos por el tema → `SwitchDefaults.colors(...)` con el azul de marca. Verificado: `compileDebugKotlin` + `test` verdes + AAB (versionCode 328). `versionName 3.2.7` sin tocar. **`327` / `3.2.7` = RETIRADA de la ficha de corredor (reducción de scope; espejo de iOS `1120` + web).** Se eliminó por completo la pantalla nativa de ficha de corredor y todos sus enlaces: borrados `ui/rider/RiderScreen.kt`, `util/RiderProfileLogic.kt` + su test; quitada la ruta `Routes.RIDER`/`rider()` y su `composable` en `AppNavHost`; en Startlist/Resultados/Buscador el nombre del corredor pasa a TEXTO (sin `onRiderTap`/navegación). El **buscador deja de listar corredores** (solo carreras): `SearchHit` simplificado, fuera `searchableRiders()`/`SearchRiderRow`. El **gate `isPublicTeamCategory`/`currentTeamCategory` se retiró en cascada** (solo gateaba el enlace de ficha): fuera `TeamCategorySlim`, `curTeamCategoryById` (`StartlistData`), `currentTeamCategory` (`ResolvedRider`), `RiderProfileLogic`. De `UciResultsLogic` se quitaron `riderLinkEligible`/`riderId` (NO se tocó `buildIndividualRows`/`collapseTtt`/`teamsInClass`/`byDorsal`/`byRider`/`byTeamOverride`). Podadas además queries muertas de `SupabaseService` que solo alimentaban la ficha (`riderById`/`teamById`/`riderUciResults`/`creStageRows`/`teamCategories`…). **SE CONSERVAN**: chapas de equipo, banderas, filtro por equipo, CRE colapsada, **tachado de abandonos** (usa `globalRiderId` por dorsal), `enrichRidersByGlobalId` (fallback de equipo/bandera en CN sin startlist) y `Team.category` (override de temporada). Verificado: `compileDebugKotlin` + `test` (debug+release) verdes + `bundleRelease` (AAB 33.5 MB). `versionName 3.2.7` sin tocar (decisión Dani). **`326` / `3.2.7` = Resultados in-house: override MANUAL de equipo desde el panel (paridad web + iOS `1119`; mig. 112).** El equipo de cada fila de Resultados se resolvía SIEMPRE por dorsal (→ startlist) o por `globalRiderId` (→ equipo actual); no había forma de fijarlo a mano cuando el dorsal no casaba. Nueva columna `race_uci_results.teamId` = OVERRIDE, que el panel fija por fila y GANA a esa cascada en el render. `UciResultsLogic.buildIndividualRows`/`collapseTtt`/`teamsInClass` aceptan un mapa `byTeamOverride` (teamId → `Team`) y lo anteponen a `byDorsal`/`byRider`; `RaceUciResultRow.teamId` nuevo; `CalendarRepository.enrichTeamsByIds` lo construye desde `teams` (espejo de `enrichOverrideTeams` web); `ResultsTable` lo enriquece on-demand en el `.task` de carga. Tests nuevos en `UciResultsLogicTest` (override gana al dorsal, sin override cae al dorsal, override sin startlist/ficha, `teamsInClass` con override, CRE colapsada con override); suite verde + AAB. (`versionName` subido a `3.2.7` con el bump +0.0.1 al cerrar; `325`/`3.2.6` previo fue solo bump de `versionName`, 3.2.5 ya en producción.) **`324` / `3.2.5` = Resultados in-house: fallback por `globalRiderId` para los Campeonatos Nacionales SIN startlist (espejo 1:1 de iOS `1117` + la web, que ya lo tenía).** El equipo y la bandera de cada corredor en Resultados se resolvían SOLO por dorsal contra la startlist curada (`buildByDorsal`). En los CN sin startlist con dorsales casables —España: filas de resultado con `bib` NULL y startlist con dorsales a 0; Francia: sin startlist— las filas salían peladas (sin chapa ni bandera), mientras que Italia (cuyos dorsales SÍ casan) mostraba chapa + bandera. La web ya rescata esos casos con un fallback por `globalRiderId` (`byRider` → `riders_*.nationality`/`currentTeamId`); se porta a la app. `UciResultsLogic.buildIndividualRows`/`collapseTtt`/`teamsInClass` aceptan un mapa `byRider` (globalRiderId → `ResolvedRider`) y caen a él cuando la fila no casa por dorsal (`byDorsal` mantiene la prioridad). `CalendarRepository.enrichRidersByGlobalId(ids)` construye ese mapa desde `riders_men`/`women` + su equipo ACTUAL (`SupabaseService.ridersByIds`; nombre + chapa + categoría para el gate de la ficha) — espejo de `enrichRiders` en `js/resultados.js`. `ResultsTable` enriquece on-demand los `globalRiderId` que no casan por dorsal (en el `LaunchedEffect` de carga de filas) y los pasa a la tabla individual y a la CRE. El gate del enlace a la ficha (WT/WWT/PT/PRW por el equipo ACTUAL) se respeta igual con el fallback. Tests nuevos en `UciResultsLogicTest` (fallback, prioridad de `byDorsal`, fila sin ficha, `teamsInClass` por `byRider`); suite verde + AAB regenerado. (El `versionCode` venía de `323`; la entrada `323` no estaba detallada en esta tabla.) **`322` / `3.2.4` = fix del huso horario en el orden de salida (CRI/CRE).** La conversión de la hora local de la carrera a la del usuario usaba `race_days.date`, que está **NULL** en 15 de las 36 jornadas con orden de salida (los CN del 25-jun: Irlanda, Suiza, Noruega, Francia, GB) → con `date` vacío, `raceLocalInstant` devolvía null y se mostraba la **hora cruda sin convertir** (un usuario en España veía la hora irlandesa). FIX de raíz en las 3 plataformas: usar `dateKey` (campo canónico, NUNCA null) con `date` de respaldo vía `effectiveDate = dateKey ?: date`. Android: `StartOrderRaceDay.effectiveDate` (`data/model/StartOrderEntry.kt`) + `dateKey` añadido al `select` de `SupabaseService.startOrderRaceDay` + 3 call sites en `StartOrderScreen.kt`. Espejo web (`js/orden-salida.js`, `rdDate = rd.dateKey || rd.date`) e iOS (`StartOrderRaceDay.effectiveDate` + `dateKey` al select de `StartOrderViewModel`). Además se rellenó `date = dateKey` en BD en esas 15 jornadas (parche de datos inmediato; el código ya no depende de ello). Verificado: web en navegador (Irlanda CRI masc → 20:43 en hora de España) + Android compila + AAB. **`321` (sin subir) / `320` / `3.2.3` = tanda de arreglos de Campeonatos Nacionales + badge de TV (TODO bajo 3.2.3, SIN bump intermedio; iteraciones reportadas por Dani).** **(c) el badge de TV NO se muestra cuando TODA la emisión es de fuera de la región del usuario** (un usuario de España veía "TV" en los CN de Bélgica/Países Bajos): el badge salía del flag `tvStatus='confirmed'/'confirmed_time'` (cobertura confirmada, AGNÓSTICO de región) aunque el único broadcast estuviera filtrado por región (Bélgica `country='BE'`, NL `country='NL'` → invisibles para España). El badge recibía la lista YA filtrada y no distinguía "sin TV" de "TV pero toda fuera de mi región". FIX: señal `regionBlocked` = había broadcasts y NINGUNO sobrevive al filtro regional → se suprime el badge "TV" genérico (con 0 broadcasts en absoluto el badge sigue saliendo de `tvStatus`: cobertura sin canal aún). `TVBadge.kt` (`buildLabel` con param `regionBlocked`); espejo iOS (`TVBadge.swift`) y web (`tvBadge`/`tvBadgeCard` con param `regionBlocked`, flag `rd._tvBlocked` calculado en el filtro de `app.js`/`competicion.js`/`campeonatos.js`). **(d) el horario del badge = la emisión accesible que ANTES empieza** (desacoplado del enlace): si una emisión global (ALL) empieza antes que las de tu grupo, su hora manda (España CN: Aragón TV ALL 10:00 UTC = "TV 12:00", aunque el enlace gane TDP Play/Eurosport a las 11:30 UTC). El ENLACE sigue por prioridad de tier (YouTube > redes > pública > resto); solo se desacopla la hora MOSTRADA. Cubre también el caso previo (enlace YouTube sin hora → caía a la más temprana). `TVBadge.kt`/`.swift` (refBroadcast/first/refBroadcastSec = la más temprana accesible) + web `tvBadgeCard`/`tvBadge` (refTs = la más temprana). **(e) la rejilla de Campeonatos usa los resultados in-house NATIVOS** (trofeo → `ResultsScreen`) cuando existen — el trofeo **SUSTITUYE** a FC/PCS (no se une a ellos); sin in-house, la celda cae a FC/PCS: `CalendarRepository.inhouseStageKeys(raceIds)` (1 query por lote, clave `raceId#stage|final`), `ChampionshipsViewModel.inhouseKeys` → `EventCell` (trofeo `Routes.results`); espejo iOS (`inhouseStageKeys` + `ChampionshipResultsRoute` → `ResultsView`) y web (`campeonatos.js` carga `loadInhouseStageSet` → trofeo `openResultsModal` → `/resultados/`; ya era if/else). Los CN raramente tienen in-house (suelen ser solo FC/PCS), pero un volcado PDF/UCI lo activa. **Base (a)/(b) = Campeonatos Nacionales: botón de resultados en pruebas terminadas + badge de TV coherente con la región (reportado por Dani en iOS).** (1) **Resultados**: las pruebas de CN ya terminadas NO mostraban el botón FC/PCS porque `RaceLogic.isRaceConcluded` exigía `estimatedFinishTimeUtc` y **TODOS los CN lo tienen NULL** (no hay hora de meta curada) → nunca "concluían". Ahora `isRaceConcluded` es espejo FIEL de la `isRaceConcluded` EXPORTADA de `js/race-data-modal.js` (que NO lleva ese guard): cae al fallback de `dateKey` 18:00 UTC de `raceTimeCheck` → al terminar aparecen FC/PCS (los CN sí tienen `fcId`/`pcsSlug`). El ÚNICO consumidor de `isRaceConcluded` en las apps es la rejilla de Campeonatos. (2) **TV por región**: la celda de Campeonatos (`EventCell` en `ChampionshipsScreen.kt`) pasaba `item.broadcasts` SIN filtrar → un usuario de España veía la TV del campeonato de Bélgica (`country='BE'`); ahora filtra por `regionPreference` antes del badge (`regionBroadcasts`). Además `TVBadge.kt` (race cards) calculaba label/icono/"hay TV"/hora de referencia sobre los broadcasts CRUDOS (solo el ENLACE iba filtrado) → mostraba "TV 14:00" de una emisión inaccesible para la región; ahora TODO el badge usa la lista filtrada, igual que la web (que pre-filtra en cada entrada con `filterBroadcastsByRegion`). Tests `RaceLogicTest` actualizados (sin hora de meta: `dateKey` pasado→concluida, futuro→no). **La web ya era correcta** en ambos puntos (su `isRaceConcluded` exportada cae al date y pre-filtra broadcasts en `app.js`/`competicion.js`/`campeonatos.js`/`jornada.js`/`race-data-modal.js`). El detalle de etapa (`StageScreen`) ya filtraba por región. **`319` / `3.2.2`** = (sesión previa) logo del cintillo de Campeonatos (globo) + ocultar CN sueltas en Mes. **`318` = deep-link de App Link (calendario/web) resuelve el SLUG → id antes de navegar** (bug reportado por un usuario de Android: al tocar una etapa guardada en el calendario salía "Jornada no encontrada"). CAUSA: `assetlinks.json` + `autoVerify` + `pathPattern="/jornada/.*"` hacen que `https://calendariociclismo.app/jornada/<slug>/` abra la APP (no el navegador); `parseIntent` (`MainActivity.kt`) lo convertía en `DeepLink.Stage(<slug>)` y `StageScreen.loadStageData` trataba ese slug como un **id de Room** (`raceDaysDao.getById`, que es un UUID) → nunca casaba → `error(stage_label_route_unknown)` = "Jornada no encontrada". Lo mismo afectaba a `/competicion/<slug>/` (`Race(slug)` → `raceById(slug)` con `eq("id", slug)`). **iOS no sufre este bug** (NO declara `applinks:` para el dominio → el enlace abre en Safari/web, que SÍ resuelve slugs). FIX: dos variantes nuevas `DeepLink.RaceSlug`/`StageSlug` que **solo** produce la rama del App Link HTTPS de `parseIntent` (las push y el widget siguen con `Race`/`Stage` por id, intactos); el handler resuelve slug → id real contra Supabase con `CalendarRepository.raceIdForSlug`/`raceDayIdForSlug` (envuelven los helpers `raceBySlug`/`raceDayBySlug` que YA existían sin usar) y navega con el id (las pantallas no cambian: ya funcionan con ids). ⚠️ **GOTCHA clave**: la resolución (suspende, va a red) NO puede correr dentro del `LaunchedEffect(pendingDeepLink.value)` que invoca `handleDeepLink` — ese effect se cancela en cuanto se limpia `pendingDeepLink` a `null`, abortando la query a medias con `CancellationException` que `runCatching` se tragaba → `id=null` → fallback a Hoy. Solución: helper `resolveSlugAndNavigate` que lanza la resolución en **`lifecycleScope`** (ligado a la Activity, sobrevive a la recomposición) + los resolvers **rethrowean `CancellationException`** (no la tragan). Slug inexistente → cae a Hoy (mejor que el error muerto). Verificado en emulador Pixel 9a con el intent real: `/jornada/vuelta-a-suiza-2026-etapa-5/` → pantalla de etapa; `/competicion/vuelta-a-suiza-2026/` → lista de etapas; slug inexistente → Hoy sin crash. Tocados: `MainActivity.kt`, `DeepLink.kt`, `CalendarRepository.kt`. **NOTA — Universal Links en iOS (mismo comportamiento, para llevar gente a las apps) están IMPLEMENTADOS pero APARCADOS** en la rama `claude/ios-universal-links` (origin): el AASA debe servirse desde el apex con `application/json`, pero el apex va en GitHub Pages (sin extensión = `octet-stream`, iOS lo rechaza) y NO se puede proxiar por Cloudflare (constraint VPS/LaLiga); Apple no sigue redirects del AASA. Retomar cuando se resuelva el hosting del AASA en el apex (ver esa rama + `docs/runbooks/ios-universal-links.md`). **`317` / `versionName 3.2.1` = el mapa del recorrido respeta la PREFERENCIA de tema de la app** (no solo el ajuste del sistema): `RouteMapScreen.kt` usaba `isSystemInDarkTheme()` → con la app en "Oscuro" y el sistema en claro, el mapa cargaba el estilo CLARO (`liberty`) mientras el resto de la app iba oscura (cazado por Dani en el emulador tras la migración a OpenFreeMap). Ahora resuelve `darkTheme` igual que `MainActivity` (`app.preferences.themePreference` SYSTEM/LIGHT/DARK + `isSystemInDarkTheme()`) → el estilo del mapa (`liberty`/`dark`) sigue el tema REAL de la app. Bug **PREEXISTENTE** (ya pasaba con MapTiler), solo lo destapó la migración. **`316` = mapa del recorrido: MapTiler → OpenFreeMap SIN CLAVE** (MapTiler invalidó la key por uso; ver "🔑→∅ Mapa del recorrido" arriba). `MapTilerConfig.kt` sin `API_KEY`, `styleUrl{Light,Dark}` → `tiles.openfreemap.org/styles/{liberty,dark}`; `MapLibreView` intacto. **`315` / `versionName 3.2` = mapa del recorrido NATIVO (MapLibre) + popups de pin** (commit `5f9559139b`; entrada no detallada en esta tabla). **`314` / `versionName 3.1.2` = Temporada agrupa los Campeonatos en UNA card (paridad iOS `1108` + web `js/temporada.js`):** la vista de Temporada listaba cada Campeonato Nacional suelto (España CRI masc/fem, Francia línea fem…); ahora se colapsan en una única fila sintética `SeasonChampionshipsRow` (globo + título + badge CN, espejo de `MonthChampionshipsRow`) intercalada por fecha (`ChampionshipsConfig.RANGE_START` = 22-jun) en el mes de Campeonatos, que navega a `Routes.CHAMPIONSHIPS`. En `ui/season/SeasonScreen.kt`: `filtered` excluye `ChampionshipsConfig.isChampionship`, flags `hasChampionships`/`championshipsMonth`, `MonthPage` recibe `showChampionships`/`championshipsMonth`/`onChampionshipsClick` e inyecta la fila en su posición ordenada; las CN sueltas se excluyen también del selector de país (`availableCountries`). **Mes ya era la subvista por defecto de Calendario** (`AppPreferences.calendarSubview` → `else -> "month"`) — sin cambios, solo verificado. El overlap de la tab bar y el botón País → cápsula de `1108` son SOLO iOS (la `NavigationBar` Material 3 de Android no es flotante translúcida; su Temporada no tiene el botón País con ese estilo). Bump `versionName` a 3.1.2 para paridad con iOS. **`313` = barra de clasificaciones de Resultados: separador + chevron de scroll (paridad iOS `1103`):** la fila de pestañas (Etapa/General/Puntos/Montaña/Jóvenes/Equipos) scrollea en horizontal y quedaba pegada al filtro "Todos los equipos" sin nada que indicara que había más clasificaciones ocultas tras él. Dos retoques en `ResultsClassTabsBar` (`ui/results/ResultsComposables.kt`): (1) **divisor vertical** (`VerticalDivider`, `outlineVariant`) entre las pestañas y el dropdown de equipos; (2) **botón chevron** (círculo `surfaceVariant` + `KeyboardArrowRight`) anclado al borde derecho de las pestañas (overlay en un `Box`, NO scrollea con el contenido), visible SOLO cuando `scrollState.canScrollForward`, que al tocarlo hace `animateScrollTo(maxValue)`. **Descartado un primer intento de degradado de desvanecido al color del fondo** (mismo color → señal invisible, lo cazó Dani) → chevron explícito en su lugar. iOS = espejo: `onScrollGeometryChange` controla el chevron `chevron.right` (`ScrollViewReader` + `scrollTo(.trailing)`) + `Divider`. String `results_more_classifications` (ES+EN). Solo apps (la web tiene su propio layout). **`311` = Resultados, dos retoques (paridad web + iOS `1101`):** (1) **las generales del ÚLTIMO día se duplican bajo la última etapa** — las clasificaciones finales (`stageNumber` null: GC/puntos/montaña/jóvenes/equipos) se inyectan TAMBIÉN en el grupo de la última etapa numerada, además de seguir en su pantalla **'F'** (que se conserva intacta). NO se vuelcan dos veces (es solo presentación: mismas filas por id de clasificación; si la etapa ya trae una clasificación del mismo tipo, manda la final, que es la oficial del último día). Espejo en `ResultsScreen.kt` (`stagesByKey`: inyecta `grouped[null]` en la clave numérica máxima), web `js/resultados.js` (grupo `stagesByNum`) e iOS `ResultsView.swift` (`stagesByKey`). (2) **el filtro por equipos se ordena alfabéticamente** (case-insensitive) en `UciResultsLogic.teamsInClass` — antes iba por orden de aparición del primer corredor; la web ya lo hacía (`localeCompare`). Tests nuevos en `UciResultsLogicTest`/`UciResultsLogicTests`. (El `versionCode` venía de `310`; entradas 307–310 sin documentar en esta tabla.) **`306` = una CRI con ex aequo reales ya NO se pinta como CRE en Resultados** (paridad web + iOS `1097`): la heurística `UciResultsLogic.isTttStage` colapsaba por equipos cualquier etapa con ≥3 puestos compartidos (rama estructural `sharedRanks >= 3`) — y una CRI puede tener empates legítimos (varios corredores con el mismo tiempo al cronómetro → mismo puesto). Cazado en el **Tour de Beauce 2026 etapa 4** (CRI de Québec, 122 corredores, empates en los puestos 49/51/88): se pintaba como crono por equipos (cabecera "Equipo", filas colapsadas). FIX: guard al inicio de `isTttStage` — si la jornada es `primaryType='itt'` **o** la etapa `raceType='ITT'`, devuelve `false` de entrada (una CRI nunca es CRE, pase lo que pase con la estructura). Nuevo parámetro `stageRaceType` en la firma (Android/iOS) cableado desde `stage.raceType` en los call sites (`ResultsComposables.kt` / `ResultsComponents.swift`). Espejo en las 3 plataformas: web (`js/resultados.js`), Android (`util/UciResultsLogic.kt`), iOS (`Services/UciResultsLogic.swift`). Tests nuevos en `UciResultsLogicTest`/`UciResultsLogicTests`. **`305` = la etiqueta del corredor en el buscador distingue género** (paridad web + iOS `1096`): "Corredora" (de `riders_women`) vs "Corredor" (de `riders_men`); en inglés ambas → "Rider" (sin distinción). Señal `SearchableRider.isFemale` (la fija `searchableRiders()` según la tabla de origen) → `SearchRiderRow` elige `search_rider_tag_female`/`search_rider_tag`. **`304` = el buscador (tab Buscar) incluye CORREDORES con ficha** (paridad web + iOS `1095`): los corredores cuyo equipo ACTUAL es top-división (WT/WWT/PT/PRW = `RiderProfileLogic.isPublicTeamCategory`, el mismo gate de la ficha) son buscables por nombre y **se mezclan por relevancia** con las carreras en un solo listado (cap 12). Piezas: `SupabaseService.searchableRiders()` (carga `teams` id+name+category → filtra públicos → `riders_men`/`riders_women` por `currentTeamId in (públicos)`, ~1.300 filas; cacheado en `CalendarRepository.searchableRiders()`), `RiderProfile.otherNames` añadido + modelo `SearchableRider`. `SearchScreen.kt`: `sealed SearchHit` (RaceHit/RiderHit), scoring por substring `scoreText()` (espejo de `score()` web — inicio 3 / tras espacio 2 / contiene 1), fila `SearchRiderRow` (bandera+nombre+equipo+etiqueta "Corredor") → `Routes.rider(id)`. String `search_rider_tag` (ES+EN). El matching de carreras (nombre/ubicación/descr) NO cambia. Ver memoria `project_search_riders`. **`303` = CCMA (TV3/Esport3/3Cat) y EITB (ETB) entran en el tier de TV pública del badge de TV en directo** (paridad web + iOS `1093`; salto 300→303 = el `versionCode` ya iba por 302 al integrar tras la tanda de Campeonatos): el tier 2 deja de ser solo RTVE.es → ahora **RTVE.es + `ccma.cat`/`3cat.cat` + `eitb.eus`/`eitb.tv`** (por delante del resto de cadenas; YouTube y otras redes sociales siguen por encima). En `RaceLogic.broadcastLinkPriority(url)` (+ tests `RaceLogicTest`). **`299` = prioridad del enlace del badge de TV en directo** (Hoy/Competición; paridad web + iOS `1086`): el badge enlaza a la mejor emisión por tier **YouTube > otras redes sociales (FB/IG/X/TikTok/Twitch/Kick) > TV pública (RTVE/CCMA/EITB) > resto de cadenas**; **Eurosport / HBO Max pasan a ser "una cadena más"** (antes ocupaban un tier propio por delante del resto). Fuente única `RaceLogic.broadcastLinkPriority(url)` (espejo de `js/broadcast-priority.js` web e iOS), cableada en `selectedBroadcast` de `TVBadge.kt`. ⚠️ `x.com` anclado con `//`/`.` para no capturar `play.max.com`. Tests en `RaceLogicTest`. Ver `docs/memory/live-badge.md`. **`298` = el pull-to-refresh de Resultados ahora SÍ recarga las filas** (paridad con iOS `1085`, mismo commit): las filas de cada clasificación las carga el propio `ResultsTable` (`ui/results/ResultsComposables.kt`) por `loadResultRows(stage.id)`, NO `loadResultsData` → el `PullToRefreshBox` recargaba cabecera/etapas/equipos pero NO las filas visibles (el `LaunchedEffect(stage.id)` no se re-disparaba al no cambiar la etapa). Fix: un **token de recarga** (`reloadToken`) que se incrementa en cada pull-to-refresh (`ResultsScreen`), se pasa por `ResultsContent` hasta `ResultsTable` y entra en la clave del `LaunchedEffect(stage.id, reloadToken)`; el `remember(stage.id)` de `rows` NO se re-clava → la tabla anterior permanece visible mientras recarga (sin parpadeo del spinner). **`295` = el filtro "Pro" (y Masc/Fem) incluye las CN élite en Hoy/Buscar** (paridad web + iOS `1082`): las pruebas de Campeonatos Nacionales élite (masc+fem) NO entraban en el filtro Pro; ahora sí, y las **sub23 quedan fuera** de Pro/Masc/Fem (igual que las 1.2U/2.2U). Masc/Fem respetan el género de la prueba. La señal élite-vs-sub23 y el género se deducen del nombre (con respaldo en `gender`), misma lógica que `slot()`: helpers nuevos `ChampionshipsConfig.isU23Championship()` + `isFemaleChampionship()`, cableados en `RaceLogic.matchesCategory` (usado por Hoy + Buscar) con una rama propia para `uciCategory=="CN"` ANTES del blacklist genérico (uwt/wwt no aplican a CN → false). Mes/Temporada no se tocan (ahí las CN se colapsan en su fila sintética). Espejo web (`js/campeonatos-config.js` `isU23Championship`/`isFemaleChampionship` → `js/app.js` `applyAgendaFilters` + `matchesCategoryFilter`) e iOS (`ChampionshipsConfig` → `RaceLogic.matchesCategory`). Tests en `RaceLogicTest`/`ChampionshipsConfigTest` (Android), `campeonatosConfig.test.js` (web), `RaceLogicTests`/`ChampionshipsConfigTests` (iOS). **`294` = sort interno de la categoría CN en Hoy y Mes (Agenda)** (paridad web + iOS `1081`): cuando dos jornadas son Campeonatos Nacionales (`uciCategory == "CN"`) se ordenan entre sí por (1) país según `ChampionshipsConfig.COUNTRY_ORDER` (el mismo de la página de Campeonatos), (2) **LÍNEA siempre por delante de TODA la CRI**, (3) elite masc → elite fem → sub23 masc → sub23 fem. Distinto del orden de columnas de la página (que intercala línea/CRI por categoría → `SLOT_ORDER`). Pieza única `ChampionshipsConfig.compare(rA, rdA, rB, rdB)` (devuelve `null` si alguna no es CN, número si decide el orden, 0 ⇒ desempate al comparador genérico) + `countryIndex()` (ausentes al final) + `CN_SLOT_RANK` (línea 0-3, cri 4-7, reutiliza `slot()`), cableada al principio de los DOS comparadores de día tras el guard de placeholders: `RaceLogic.byCategory` (Hoy) y `byCategoryWithRaceMap` (Mes). Espejo web (`js/campeonatos-config.js` `compareChampionships` → `js/app.js` `_sortByCategory` + `js/calendario-mes.js` `sortDayRaces`) e iOS (`ChampionshipsConfig.compare` → `RaceLogic.sortByCategory` + `MonthViewModel`). Tests en `ChampionshipsConfigTest` (Android), `campeonatosConfig.test.js` (web), `ChampionshipsConfigTests` (iOS). Solo afecta a la semana de campeonatos (22-28 jun; fuera de ahí no hay carreras CN). **`291` = etiquetado GA4 de etapa en Resultados/Perfil/Orden de salida** (paridad iOS `1078`): tres pantallas que son DE una etapa concreta no mandaban a Firebase los params `stage_name`/`race_name` (caían en `(not set)` en el panel "etapas más vistas"). (a) **Resultados** (`ResultsScreen.kt`) ahora re-emite `screen_view` en cada cambio de etapa del selector (`LaunchedEffect(activeStageKey, headerRaceDay)`) con `stage_name`+`race_day_id`+`race_name` derivados del `headerRaceDay` ya resuelto, en vez de un único log al cargar con solo `race_id`/`race_name`. (b) **ElevationProfileScreen** añade `stage_name`+`race_name` (clave del effect ampliada a `raceDay, race` para no perder `race_name` que se resuelve en el mismo LaunchedEffect). (c) **StartOrderScreen** añade `stage_name` vía `ready.data.fullRaceDay?.stageLabel`. Todos usan `stageLabel` como `stage_name`, MISMA fuente que `stage_detail` → las vistas de la misma etapa se agrupan. Lado lectura: la edge function `ga-analytics` (`top_stages`) ahora filtra `(not set)`/`''` en `stage_name` (espejo del filtro de `top_pages`/`top_screens`) → cambio RETROACTIVO que limpia el histórico al desplegar. **`289`–`290` = swipe de cambio de día "sigue el dedo" PROBADO Y REVERTIDO** (feedback Dani en TestFlight): el intento de que el contenido siguiera al dedo en vivo (`289`) tuvo una **fluidez grotesca en dispositivo** (animar la lista pesada de Hoy por frame caía frames) → `290` revierte el código al snap anterior (`git revert` del commit del swipe). `TodayScreen.kt` vuelve al gesto previo (decide horizontal, consume, anima al soltar con `animateNavigation`; sin seguir el dedo). La versión **web SÍ se quedó** (su `initDaySwipe` va fino; el problema era solo nativo). versionCode sube a 290 (la 289 ya se quemó, no se reusa). **`288` / `versionName 3.1.1` = tooltip/indicación de abandono más limpia** (feedback Dani, paridad web + iOS `1075`): el motivo bajo el nombre del corredor tachado en Inscritos ya NO antepone "Fuera de carrera ·" (redundante con el tachado) y un **prólogo** dice "prólogo" en vez de "etapa 0". Strings `startlist_dnf_reason_stage`/`_reason` sin el prefijo + nueva clave `startlist_dnf_reason_prologue` (ES+EN); `StartlistScreen.kt` ramifica `stageNumber == 0` → prólogo. Espejo web = `js/i18n.js`/`i18n/en.json` (`startlist.dnfReason*` + `dnfReasonPrologue`) + `js/rider-tooltip.js`; iOS = `StartlistView.swift`. **`287` = cabecera de la ficha de corredor en caja** (feedback Dani en dispositivo): sin TopAppBar — la flecha de retroceso va INTEGRADA en la `CCCard` del hero (patrón estético `StageInfoHeaderCard` de Resultados/perfil; mismos datos), como primer item del scroll igual que el perfil de elevación. Solo Android (iOS usa la flecha del sistema en Resultados → su ficha ya era consistente). **`286` / `versionName 3.1.0` = APPS 3.1** (espejo iOS `1068`, mismo commit): (1) **pestañas**: Hoy · **Resultados** (trofeo, feed nativo) · **Calendario** (fusión Mes+Temporada con toggle en el TopAppBar, subvista persistida en DataStore `calendarSubview`; los deep links `month`/`season` fijan la subvista y abren Calendario) · Buscar · Ajustes. (2) **Feed nativo** `ui/resultsfeed/ResultsFeedScreen` + lógica pura `util/ResultsFeedLogic` (port 1:1 de `js/resultados-feed.js`, 13 tests): etapas+clásicas+generales FINALES delante de su etapa, orden canónico del día con hora POR CARRERA-DÍA, fechas NULL de PDFs por jornada, ganador canónico (CRE→EQUIPO por startlist), fallback FC/PCS con `ResultsDialog`, cargar más 14d, filas estilo card (RaceLogo+bandera+badges+tinte). (3) **Ficha de corredor** `ui/rider/RiderScreen` + `util/RiderProfileLogic` (15 tests) + ruta `rider/{riderId}`: header sin avatar (futuro foto), gate ESTRICTO por equipo actual, contador de días (DNS no cuenta), bloques inversos por fecha efectiva (fecha·bandera·nombre sin año), un día = resultado+recorrido, General SOLO definitiva (chips de general por etapa), CRE variantes B y **C** (un día como gc, fix `6140f1a`), victorias doradas; enlaces desde startlist y resultados (incl. sub-filas CRE) **gateados por el equipo ACTUAL del corredor** (`currentTeamCategory` en `ResolvedRider`/`StartlistData`, query slim `teamCategories`, fail-closed — paridad con el fix devo web `9a3d1d12fd6`). Suite **238 tests** verde tras el merge con `6140f1a`. **`285` = startlist ordenada por el dorsal del PRIMER corredor** (paridad con web e iOS `1067`, mismo commit): `startlist_teams.sortOrder` es el orden de inserción del panel ("al tuntún") → el orden canónico lo imponen los dorsales EN CLIENTE: equipos por su mínimo dorsal > 0, sin dorsal al final conservando sortOrder (startlist entera sin dorsales = orden del panel, sin cambio); nada que tocar en panel/BD. `util/StartlistLogic.teamsByFirstDorsal` (+5 tests `StartlistLogicTest`) cableado en `CalendarRepository.loadStartlistData`; espejo web en `js/inscritos.js` (el PDF comparte el array → hereda) e iOS en `StartlistViewModel.fetchTeams`. Ver `docs/memory/enriched-startlists.md` § "Orden de equipos". **`284` / `versionName 3.0.2` = release de la ocultación de "Individual"** (3.0.1 ya estaba EN PRODUCCIÓN → estos cambios salen como 3.0.2; sin cambios de código respecto a `283`, solo bump de `versionCode` + `versionName`, mismo patrón que `277`/3.0). **`282`–`283` = ocultación cosmética del equipo ficticio "Individual"** (paridad con la web y con iOS `1064`–`1065`): `resolve_uci_startlist` (mig. 084) siembra ese equipo (`teamId` NULL, nombre 'Individual') cuando la fila de resultados UCI no trae equipo; ahora en Resultados esas filas salen SIN equipo/chapa y el filtro por equipo no lo lista (`CalendarRepository.buildByDorsal` vacía el `teamName`), y en la startlist nativa su tarjeta va SIN cabecera y no cuenta en "X equipos" (sus corredores sí se listan y suman en corredores). Detección = `StartlistTeam.isIndividualPlaceholder` (`teamId == null` + nombre 'individual' case-insensitive); espejo web = `isIndividualPlaceholderTeam` en `js/shared.js` (resultados + inscritos + PDF de inscritos, mismo commit). `283` remata el caso límite startlist 100% ficticio (3 carreras en prod, p. ej. Rutland-Melton): con `teamCount == 0` el header nativo omite el bloque "Equipos" y en web/PDF la línea de stats y la description SEO omiten "0 equipos". **`281` = CRI con diferencias sobre tiempos truncados en Resultados** (paridad con la web `d960724e61d` y con iOS `1063`; espec Dani: `20:52.99/20:53.00/20:53.05 → 20'52" / +1" / m.t.`): el tiempo oficial se trunca a segundos ENTEROS; gap = floor(propio)−floor(ganador) (truncar CADA tiempo, no el gap: 20:53.00 es +1", no +0"); gap publicado CON décimas (Gila "+36.98") → floor(ganador+gap)−floor(ganador) = +37"; ganador de CRI en notación de prensa truncada (`secondsToPressTime`: 20'52", ≥1h H:MM:SS); el resto fluye por el MISMO pipeline de gaps/m.t. que una etapa en línea (incluido el m.t. dinámico del filtro por equipo). Gate `UciResultsLogic.isIttStage` (RaceTypeCode `ITT` de `race_uci_stages.raceType` — columna nueva en el modelo — O `primaryType='itt'` de la jornada, que cubre las CRI de UN DÍA con bloque final sin raceType y las mal etiquetadas tipo Gila); la GC del día y las acumuladas NO cambian. Implementación: `buildIndividualRows` pasa a tiempos Double vía `tttToSeconds` (espejo del `timeToSeconds` decimal de la web — el parser entero no entendía "20:52.99"), overload `secondsToGap(Double)` con floor, `cleanTimeText` recorta decimales enteros fuera, `formatGap` decimal-aware. 12 tests nuevos (espejo Swift) con los casos verificados en web: Mayenne 6'36"/+1"/+6"/m.t./+7" · Estonia 1'04"/+2"/m.t./+3" · Romandía 3'35"/+6"/m.t./+7" · Gila 32'30"/+37"/+52" · Giro et.10 45'53"/+1'53". **`280` = icono de notificación (barra de estado) rediseñado:** el vector "bicicleta simplificada" de `ic_notification.xml` estaba roto (la rueda trasera renderizaba como borrón sólido con muescas y el cuadro era un paralelogramo flotante → en el móvil parecía una mancha). Sustituido por la **bici del logo de marca** (la misma de launcher/web/favicon, a trazo, grid 24×24) + `setColor(notification_accent)` en el builder del FCM service (antes el icono salía gris en la cabecera de la notificación desplegada; el azul `#1A73E8` del manifest solo aplicaba a las auto-construidas por el SDK en background). Comparativa antes/después: `docs/android-ic-notification-antes-despues.png`. **`279` = pestaña Equipos de Resultados con chapa + nombre canónico** (paridad con iOS `1061` y con la web `eb1cdbb2ec3`): las filas de la clasificación de equipos (`classKind='teams'`, `riderDisplay` crudo de la fuente, `bib` NULL) se casan por NOMBRE contra los equipos canónicos de la startlist — `UciResultsLogic.normalizeTeamName`/`findMatchingTeam` (port de `js/shared.js`: NFD sin acentos + stopwords + `nameAliases` multilínea + fallback de contención ≥4 chars), `Team.nameAliases` nuevo en el modelo, `UciResultsData.raceTeams` (lo emite `buildByDorsal`, que ya no early-return sin riders) → chapa `TeamBadgeComposable` + nombre del catálogo en `ResultsRow` (la chapa ya no se gatea por `showTeam`). Tests espejo Kotlin+Swift (4 nuevos, casos reales del ARA 2026 — 22/22 casan). **`278` / `3.0.1` = autoscroll del selector de etapas en Resultados** (paridad con iOS `1060`). En etapas avanzadas de una Gran Vuelta la cápsula activa quedaba fuera de pantalla; `ResultsStageSelector` (`ui/results/ResultsComposables.kt`) pasó de `Row`+`horizontalScroll` a `LazyRow`+`rememberLazyListState` y centra la cápsula activa con `animateScrollToItem(index, scrollOffset=-(viewport/2 - itemHalf))` en un `LaunchedEffect(activeKey)` (mismo patrón que el selector de días de `TodayScreen`). Resincroniza `versionName` a `3.0.1` con iOS. **`277` / `versionName 3.0` (EN MAIN) = salto de versión mayor a 3.0 (apps 3.0 = resultados in-house UCI nativos + tachado de abandonos + UI sticky/unificada). Sin cambios de código respecto a `276`; solo bump de `versionCode` + `versionName`. iOS alcanzó 3.0 el mismo día (build `1056`) → sincronía de `versionName` restaurada.** `276` = (a) **buscador → jornada en carreras de un día**: un resultado de carrera de un día navegaba a competición (lista de etapas, sin sentido); ahora a su jornada única (`SearchResult.oneDayRaceDayId`). iOS tenía el MISMO bug — resuelto en iOS 3.0 (`1056`). (b) Mergeado el WIP de km/fecha en idioma de CONTENIDO (`DateFormatting.formatDateLongContent` + cabecera de etapa en `StageScreen`; el `273` "reservado" ya está aquí) + refinamientos de `UciResultsLogic` (`isAbandonIrm`, `secondsToTimeText`, ABD como variante de DNF) + `uci-results-upsert.mjs`.** `275` = UI de resultados/jornada: (a) **cabeceras sticky** — en Inscritos y Resultados el bloque de carrera queda FIJO arriba (en Resultados también el selector de etapa + pestañas de clasificación/filtro de equipo, con colchón inferior para que las filas no rocen el filtro al scrollear); patrón = sacar el header del `LazyColumn` a un `Column` fijo + `LazyColumn` `weight(1f)`. (b) **"Así está la carrera" usa in-house** — si la etapa ANTERIOR tiene clasificaciones propias, el CTA primario va a la pantalla nativa de esa etapa (FC/PCS de respaldo); espejo de `_prevHasInhouse`/`resultsButtonsHtml` en jornada.js. (c) **Bloque "Resultados" unificado** en UNA tarjeta (CTA in-house + "También en" + FC/PCS), antes dos tarjetas; se extrae `ResultsButtonsCard` compartido (Resultados + Así está la carrera), se eliminan `ResultsCtaSection`/`ResultsSection`. (d) chip de startlist EN "Riders"→"Startlist".** `274` = 2 bugfixes: (1) el separador de miles del desnivel seguía el locale del CHROME (`LocaleHolder.current`) en vez del idioma de CONTENIDO → un dispositivo en inglés con app en ES veía "+2.500 m" (punto) en vez de coma; ahora `RaceDay.elevationGainFormatted` + `ElevationProfileScreen.formatAlt` usan `shouldShowEnglishContent`, igual que `distanceFormatted` (test `RaceDayFormattingTest`). (2) Pulsar el logo/cabecera de una carrera de un día en la jornada navegaba a la pantalla de competición (lista de etapas) — sin sentido sin etapas; `onRaceTap` ahora se cablea solo si `race.isStageRace` en `StageScreen` + `ElevationProfileScreen` (paridad con iOS).** `273` reservado para el WIP de km/fecha i18n (sin commitear). **`272` = Fase 7 apps: resultados in-house UCI + tachado de abandonos (ver "Fase 7 apps" abajo).** `271` = render temporal de equipos. `271` arrastra todo lo de 2.3.0. _En producción 3.0.1 (2026-06-11)._ ~67% de usuarios venían de 2.2.x. `269` re-sube la misma `versionName 2.3.0` (Play lo permite con `versionCode` distinto) con el fix del gate de la pantalla "sin anuncios" para upgraders (ver abajo). **Guía simplificada de horarios** (despliegue inline en detalle de etapa, ver `docs/memory/simplified-guide.md`). **FASE B de anuncios (AdMob — SDK integrado):** `play-services-ads` + UMP; banner adaptativo inline gateado por `shouldShowAds` en **tres** sitios (Hoy tras la 3ª tarjeta, detalle de carrera tras la 3ª etapa, detalle de etapa bajo "Schedule"); consentimiento UMP (`AdsConsentManager`) disparado desde `MainActivity` (necesita Activity), init de `MobileAds` solo si `shouldShowAds`. **Ad Unit ID real de banner puesto** (`/8172741311`) tras flag `AdsConfig` (Debug = test de Google, Release = real). Verificado en emulador (banner de test visible con Premium OFF). Arrastra FASE A + todo lo de 2.2.x.


## iOS (`CURRENT_PROJECT_VERSION` / `MARKETING_VERSION`)

**`1278` / `MARKETING_VERSION 4.3.0` = AVISO DE SOSTENIMIENTO TRAS EL USO:** el diálogo local que aparece tras 30 pantallas de contenido y 7 días deja de ofrecer «Sin anuncios» y de afirmar que una aportación elimina publicidad. Presenta la aplicación como gratuita y sin anuncios, abre las formas de apoyo mediante Amigo o aportaciones puntuales y actualiza su evento interno de Premium a sostenimiento. Android ya tenía el copy correcto.**

**`1277` / `MARKETING_VERSION 4.3.0` = ONBOARDING DE APOYO SEGÚN INSTALACIÓN:** el anuncio 4.3 utiliza el icono Amigo de CC con corazón. Las instalaciones nuevas explican los costes de mantenimiento y la autoría profesional, sin mostrar «Conoce el cambio»; las actualizaciones conservan la explicación de la retirada de publicidad y el reconocimiento Fundador. La audiencia se fija antes de elegir idioma y se conserva entre relanzamientos. La pantalla «Hazte Amigo» usa el mismo icono, elimina el beneficio redundante «Sin anuncios» y prioriza el icono exclusivo antes de recordar que toda la app es gratuita. Contenido completo en español e inglés. Espejo Android `464`.**

**`1276` / `MARKETING_VERSION 4.3.0` = AJUSTES VISUALES DE SOSTENIMIENTO:** la pantalla Amigo recupera el fondo general de la aplicación y presenta el icono CC con corazón. El botón principal del onboarding amplía su altura y ancho táctil.**

**`1275` / `MARKETING_VERSION 4.3.0` = GRATIS, ABIERTA Y SIN ANUNCIOS:** retirada completa de Google Mobile Ads, UMP, ATT publicitario, SKAdNetwork y espacios de banner. Premium deja de venderse y renovarse; las compras anteriores reciben el icono Fundador permanente. Se incorpora Amigo de Calendario Ciclismo (2,99 €/mes o 17,99 €/año) y aportaciones puntuales (2,99 €, 5,99 € y 11,99 €), siempre voluntarias y sin funciones exclusivas. Ajustes simplifica el bloque de sostenimiento y enlaza la explicación pública; el onboarding de 4.3 se muestra una vez y adapta sus acciones al estado Premium/Amigo. Original, Fundador y Amigo se rehacen por capas en Icon Composer para Liquid Glass; al vencer Amigo se recupera el icono elegible anterior. Espejo Android `462`.**

**`1274` / `MARKETING_VERSION 4.2.6` = CARDS UNIFICADAS EN RESULTADOS:** las filas que abren el modal de FirstCycling/ProCyclingStats adoptan la misma presentación y el mismo chevron derecho que las clasificaciones nativas. Espejo web; Android ya aplicaba esta presentación.**

**`1273` / `MARKETING_VERSION 4.2.6` = BADGES COMPACTOS EN RESULTADOS:** los badges CRI, CRE y cronoescalada reducen su tipografía y relleno exclusivamente en Resultados para conservar la misma altura de línea que las jornadas sin badge. Espejo Android `459`.**

**`1272` / `MARKETING_VERSION 4.2.6` = ACCESO A COMPETICIÓN:** el botón de acceso directo desde Hoy se compacta en un rectángulo de acento de 16 puntos con hamburguesa reducida, sin alterar el interlineado de la tarjeta. Espejo Android `458`.**

**`1271` / `MARKETING_VERSION 4.2.6` = BADGES EN MAYÚSCULAS:** Hoy, Jornada y Resultados presentan en mayúsculas los badges de categoría, tipo de etapa, TV, inscritos y orden de salida, igual que la web. El estado Cancelada de Hoy adopta en las tres plataformas la misma caja roja compacta, sin icono y alineada con el resto de badges. Espejo web + Android `457`.**

**`1270` / `MARKETING_VERSION 4.2.6` = ARMONIZACIÓN DE HOY Y RESULTADOS:** Hoy y Resultados condensan cada jornada a número de etapa, distancia y desnivel, sin repetir las ciudades. Los badges recuperan el espaciado de la web y dejan de usar iconos de tipo; Resultados reserva el badge para CRI, CRE y cronoescalada. Hoy mantiene el tipo junto a la categoría cuando no hay miniperfil y recupera el acceso directo a Competición en vueltas de varias jornadas. Espejo web + Android `456`.**

**`1250` / `MARKETING_VERSION 4.2.5` = FILTROS DE CALENDARIO CON BOTÓN NATIVO:** corrección de `1249`. Mes y Temporada eliminan por completo el estilo primitivo basado en `Gesture`, que bajo iOS 27 seguía perdiendo el toque frente al paginador. Los chips usan ahora la acción estándar de `Button`; tocar de nuevo el filtro activo conserva la configuración del filtro predeterminado. Sin selección previa, ambas vistas parten de Todas en iOS y Android. Espejo Android `453`.**

**`1249` / `MARKETING_VERSION 4.2.4` = FILTROS DE CALENDARIO PULSABLES:** Mes y Temporada sustituyen los reconocedores de toque y pulsación larga en competición por un botón con gesto exclusivo. El selector Todas · Pro · WT · WWT · Masc · Fem vuelve a responder bajo iOS 27 Beta 5 y conserva la fijación por pulsación larga. Android no presenta el defecto estructural.**

**`1247` / `MARKETING_VERSION 4.2.3` = RESULTADOS SIN PARPADEO:** Jornada incorpora el gate de clasificaciones nativas a su carga inicial. «Resultados» y «Así está la carrera» se presentan ya en su versión definitiva, sin sustituir después los enlaces FC/PCS por «Ver clasificaciones». Espejo Android `449`.**

**`1246` / `MARKETING_VERSION 4.2.2` = MINIPERFILES EN COMPETICIÓN:** las CRI y CRE sin horario de salida/llegada ya usan el mismo color de carrera que una jornada en línea no disputada; con intervalo horario se mantiene el gris, pues no hay un avance único. La vista Hoy conserva su comportamiento actual. Espejo Android `439`.**

**`1243` / `MARKETING_VERSION 4.2` = LEYENDA UCI POR MODALIDAD:** el color azul del cajón de información identifica únicamente a los WorldTeams en el ránking masculino y únicamente a los Women's WorldTeams en el femenino, sin citar la categoría contraria. Espejo web + Android `434`.**

**`1242` / `MARKETING_VERSION 4.2` = FECHA CANÓNICA DEL RÁNKING UCI:** la actualización comparte en las tres plataformas una presentación con dos puntos, día de la semana y fecha larga, adaptada al idioma («Actualizado: martes, 28 de julio de 2026» / «Updated: Tuesday, 28 July 2026»), también dentro de la información ampliada. Espejo web + Android `433`.**

**`1241` / `MARKETING_VERSION 4.2` = RESULTADOS SIN SEPARADOR:** desaparece la línea horizontal entre los botones «Últimos Resultados» / «Ránking UCI» y el contenido de la vista activa; se conservan los separadores internos de las clasificaciones. Espejo Android `432`.**

**`1240` / `MARKETING_VERSION 4.2` = INVITACIONES UCI COMPLETAS:** los tres mejores ProTeams aclaran tanto en su explicación como en la leyenda que la invitación obligatoria abarca todo el WorldTour —Grandes Vueltas incluidas— y también todas las pruebas ProSeries. La indicación femenina permanece limitada al Women's WorldTour. Espejo web + Android `431`.**

**`1239` / `MARKETING_VERSION 4.2` = RÁNKING UCI COMPACTO:** la fecha de actualización y el botón de información comparten una línea dentro de la vista; desaparece el botón aislado de la barra superior. Las tarjetas se convierten en filas continuas y más densas, los ProTeams fuera del top-30 usan fondo rojo completo y los puntos se redondean a enteros para permanecer siempre en una sola línea. Espejo web + Android `430`.**

**`1238` / `MARKETING_VERSION 4.2` = RÁNKING UCI DE EQUIPOS:** Resultados incorpora los botones «Últimos Resultados» y «Ránking UCI». La nueva vista consume una única instantánea semanal sobrescribible de DataRide, enlazada al catálogo canónico de equipos, y permite alternar entre los ránkings masculino y femenino. WorldTeams/WWT usan el azul de las categorías WorldTour; los tres mejores ProTeams masculinos y los dos mejores Women’s ProTeams señalan las invitaciones obligatorias al WorldTour, los ProTeams masculinos cuarto y quinto las invitaciones a ProSeries y los ProTeams fuera del top-30 llevan una franja roja. El botón de información muestra la fecha UCI, la fuente y el carácter provisional de las invitaciones; tocar una fila explica su consecuencia reglamentaria. Espejo web + Android `429`.**

**`1237` / `MARKETING_VERSION 4.1.2` = CLASIFICACIONES COMPACTAS + PUNTOS UCI:** Resultados adopta el lenguaje compacto de la web en el selector de etapas, las pestañas y las filas individuales/CRE, conservando el autocentrado horizontal. La columna opcional «UCI» muestra los puntos reglamentarios —incluidas las centésimas de CRE y ex-aequo— sin reservar espacio en clasificaciones que no puntúan. Espejo web + Android `428`.**

**`1236` / `MARKETING_VERSION 4.1.1` = MINIPERFILES POST-CARRERA Y RESPIRACIÓN EN FICHAJES:** los miniperfiles de Hoy y Competición se mantienen al mostrar Resultados/Revive, con el relleno completado al 100% (incluidas CRI/CRE); los iconos y el resto del modo terminado se mantienen. Mercado de Fichajes gana 6 pt entre «Últimas confirmaciones» y Fichajes/Renovaciones. Espejo Android `424`.**

**`1235` / `MARKETING_VERSION 4.1` = TIRA DE ACCIONES: corrige el recorrido sobrante tras el último botón (el marcador invisible ya no ocupa una celda), mide por separado ambos bordes para ocultar cada flecha al alcanzarlo y recupera el desplazamiento nativo fluido con `HStack`.**

**`1234` / `MARKETING_VERSION 4.1` = ORDEN DEL LIBRO DE RUTA: Jornada extrae la guía técnica de la lista genérica para situarla, sin duplicados, tras la web oficial y antes de Dorsales. Competición ya respetaba este orden. Espejo Android `423`.**

**`1229` / `MARKETING_VERSION 4.1` = FLECHAS CIRCULARES: las flechas de acciones pasan a un control circular de borde, con fundido de solo 24 pt y área táctil conservada. Espejo Android `422`.**

**`1228` / `MARKETING_VERSION 4.1` = FLECHAS AÚN MÁS COMPACTAS: la navegación lateral de acciones se estrecha a 40 pt y el fundido ocupa menos espacio. Espejo Android `421`.**

**`1227` / `MARKETING_VERSION 4.1` = FLECHAS MÁS DISCRETAS: la navegación de acciones se ancla más al borde y sus fundidos laterales se reducen. Espejo Android `420`.**

**`1226` / `MARKETING_VERSION 4.1` = FLECHA IZQUIERDA CON FUNDIDO: el regreso de la tira de acciones usa un degradado explícito a toda altura, con el fondo de tarjeta opaco en el borde y sin rebote más allá del final. En Competición, Notificaciones no deja un separador final.**

**`1225` / `MARKETING_VERSION 4.1` = NOTIFICACIONES SIEMPRE ACCESIBLES: el control para seguir una jornada o competición se muestra aunque aún no se hayan concedido permisos de notificación. Espejo Android `416`.**

**`1224` / `MARKETING_VERSION 4.1` = TEMPORADA 2027: Mes y Temporada permiten escoger el año siguiente antes del 1 de enero, para consultar e introducir el calendario ya publicado. Espejo Android `414`.**

**`1223` / `MARKETING_VERSION 4.1` = NOTIFICACIONES + FICHAJES: el panel puede enviar al Mercado de Fichajes o a la ficha de un equipo de su temporada; Android e iOS resuelven los destinos `transfers` y `team/{teamId}`. Espejo Android `413`.**

**`1222` / `MARKETING_VERSION 4.1` = FLECHAS CON DEGRADADO: la ocultación de acciones bajo las flechas adopta el fundido lateral de la web.**

**`1221` / `MARKETING_VERSION 4.1` = FLECHAS DE ACCIONES: las bandas de navegación lateral son opacas para ocultar por completo las celdas que quedan detrás.**

**`1219` / `MARKETING_VERSION 4.1` = ACCIONES ARMONIZADAS: las celdas de documentación usan el mismo tinte de marca suave y separadores, incluidas Notificaciones y Añadir al calendario.**

**`1216` / `MARKETING_VERSION 4.0.7` = DORSALES: los badges y botones de la lista de salida pasan de «Inscritos/Inscritas» a «Dorsales» en iOS, web y Android; «Lista provisional» se conserva en castellano y pasa a «Provisional Startlist» en inglés. Espejo Android `408`.**

**`1215` / `MARKETING_VERSION 4.0.6` = FICHAJES: el aviso de fuentes presenta enlaces pulsables, en los nombres de los siete periodistas acreditados, a sus cuentas de X de Datos abiertos. Espejo web + Android `407`. Sin cambio de versión de marketing.**

**`1214` / `MARKETING_VERSION 4.0.6` = INSCRITOS: se retira el aviso de «no spoilers»; las listas ya reflejan los abandonos mediante los resultados propios. Espejo web + Android `406`. Sin AAB a propósito.**

**`1213` / `MARKETING_VERSION 4.0.5` = JORNADAS: cuando hay archivo de mapa oficial y mapa interactivo GPX, muestra ambos chips en ese orden, igual que los perfiles.**

**`1212` / `MARKETING_VERSION 4.0.5` = corrección de versión de marketing antes de publicar: sustituye la 4.0.4 preparada por la primera versión pública de este cambio.**

**`1211` / `MARKETING_VERSION 4.0.4` = JORNADAS: el detalle muestra el chip naranja «Sin confirmar» cuando `tvStatus = pending`, incluso si ya existe un canal provisional o Live texto. Reutiliza el componente de Hoy.**

**`1210` / `MARKETING_VERSION 4.0.4` = JORNADAS: tras el pull-to-refresh, iOS sustituye la respuesta remota completa y reconstruye las secciones derivadas (incluida la descripción editorial), para reflejar cambios, inclusiones, eliminaciones y campos vaciados sin conservar contenido anterior. Espejo Android `402`.**

**`1209` / `MARKETING_VERSION 4.0.3` = CARGA DE MARCA: el bloque central (iconos, rótulo y puntos) y el perfil inferior pasan a ocupar franjas independientes, evitando cualquier solapamiento. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1196` / `MARKETING_VERSION 4.0.3` = CARGA DE MARCA: iconos, rótulo y puntos recuperan su posición centrada original; el perfil de ruta se mantiene anclado abajo. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1195` / `MARKETING_VERSION 4.0.3` = CARGA DE MARCA: los iconos de calendario y ciclista vuelven a aparecer sobre el titular de los cargadores con perfil animado. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1191` / `MARKETING_VERSION 4.0.2` = PLACEHOLDERS: «Sin información extra» se presenta como alerta nativa centrada en Hoy, Mes y Temporada, con la web oficial como acción opcional. Espejo Android `387`. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1190` / `MARKETING_VERSION 4.0.2` = RESULTADOS: el selector FC/PCS se presenta como alerta nativa centrada, en vez de un `confirmationDialog` o popup flotante. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1189` / `MARKETING_VERSION 4.0.2` = RESULTADOS: el selector de enlaces FirstCycling/PCS vuelve al popup personalizado anterior, a petición de Dani. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1188` / `MARKETING_VERSION 4.0.2` = FICHAJES: el diálogo de información cita también las fuentes periodísticas de Datos abiertos. Espejo web + Android `386`. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1187` / `MARKETING_VERSION 4.0.2` = RESULTADOS + MERCADO: titular «Últimos Resultados», información nativa de Fichajes junto al título, ficha de equipo con título superior de Mercado y selector nativo de FirstCycling/PCS al tocar el trofeo de una carrera terminada. Espejo web + Android `385`. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1186` / `MARKETING_VERSION 4.0.1` = FIX DE COMPILACIÓN: `TransferFeedRowView.rowCard` declara el `return` explícito que SwiftUI requiere tras sus variables locales; sin él, Xcode no podía inferir el tipo opaco (`some View`). Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1185` / `MARKETING_VERSION 4.0.1` = MERCADO: cada ficha de «Últimas confirmaciones» abre el equipo de DESTINO del fichaje cuando tiene ficha de mercado; la tarjeta completa es tocable. Espejo web + Android `384`. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1184` / `MARKETING_VERSION 4.0` = MERCADO: se elimina el reborde lateral violeta del aviso de continuidad de equipo en duda en su ficha, conservando fondo y texto. Espejo web + Android `383`. Sin `.swift` nuevos → NO requiere `./setup.sh`.**

**`1180` / `MARKETING_VERSION 4.0` = MERCADO: la FILA ENTERA del corredor es tocable (no solo el nombre) — corrección de `1178` a petición de Dani. `personRow` envuelve el `HStack` en un `Button` (`.plain`) con `.contentShape(Rectangle())` cuando hay `linkTeamId` → toda la fila navega a `linkedTeamRoute`; el contenido vuelve a ser un único `personLine` (nombre + "· equipo" atenuado, SIN accent), conservando la truncación a una línea (`.lineLimit(1)`/`.truncationMode(.tail)`) de `1179`. Espejo web + Android `375`. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode) → se confía en Xcode Cloud (Android, el espejo, compila + tests verdes + web verificada en navegador). **`1179` / `MARKETING_VERSION 4.0` = MERCADO: corredor + equipo trunca con "…" a una línea en la vista de equipo (`personRow`, `.lineLimit(1)`/`.truncationMode(.tail)`) — sin doble altura. Espejo web + Android `374`. **`1178` / `MARKETING_VERSION 4.0` = MERCADO: el corredor ENLAZA a la ficha del equipo del movimiento (no hay ficha pública de corredor). Regla Dani: quien LLEGA enlaza al equipo del que VENÍA (`fromTeamId`); quien SE MARCHA, al equipo AL QUE VA (`toTeamId`). Solo si ese equipo tiene ficha en el mercado; Continúan/En duda/Terminan contrato/feed sin enlace. `TransfersTeamView` gana su PROPIO `navigationDestination(item: $linkedTeamRoute)` (es una hoja empujada por `TransfersView`, que no declaraba este destino) + push por valor `TransfersTeamRoute`. Espejo web + Android `373`. Solo UI (`TransfersLogic` intacto). ⚠️ El header iba por `1176` mientras el proyecto estaba en `1177` → resincronizado (1178). **`1176` / `MARKETING_VERSION 4.0` = MERCADO: contrato VITALICIO — `YearBadge` muestra ∞ cuando el año es 9999 (centinela); ordena el primero de «Continúan». Espejo web + Android `371`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1175` / `MARKETING_VERSION 4.0` = MERCADO: en «Llegan» los fichajes CONFIRMADOS van antes que los rumores (apellido dentro de cada grupo); +1 test. Espejo web + Android `370`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1174` / `MARKETING_VERSION 4.0` = MERCADO: pantalla principal a DOBLE PANEL con scroll independiente (feed ~38% arriba + equipos ~62% abajo, `GeometryReader` + alturas fraccionales) + fecha del feed «día de la semana + día de mes, SIN año» (`DateFormatting.formatDateWeekdayNoYear`). Espejo web + Android `369`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1173` / `MARKETING_VERSION 4.0` = MERCADO: «Continúan» desempata por APELLIDO (2º factor tras el año), «Llegan» ordenado solo por apellido. +2 tests. Espejo web + Android `368`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1172` / `MARKETING_VERSION 4.0` = MERCADO: rótulo «Mercado de Fichajes» con F mayúscula + el feed se corta en 5 fechas U 8 fichajes (`TransfersLogic.limitedFeed`; +2 tests). Espejo web + Android `367`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1171` / `MARKETING_VERSION 4.0` = MERCADO: «Se marchan» ordenado por categoría del destino (WT→WWT→PT→PRW→resto) → nombre del equipo → retiros al final; «Terminan contrato» alfabético por apellido. `teamDetail` gana `ridersById`/`categoryByTeamId`/`teamNameById`; +2 tests. Espejo web + Android `366`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1170` / `MARKETING_VERSION 4.0` = MERCADO: una sección vacía NO se muestra (título incluido); equipo sin movimientos → aviso único. Espejo web + Android `365`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1169` / `MARKETING_VERSION 4.0` = MERCADO: «Continúan» ordenado por año de contrato DESC (2030 → … → sin año al final), alfabético como desempate. Espejo web + Android `364`; +1 test. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1168` / `MARKETING_VERSION 4.0` = MERCADO: sección «Terminan contrato» (fin de contrato sin destino) entre «En duda» y «Llegan»; «En duda» sin badge DUDA. `teamDetail` gana `contractEnds` + `isContractEnd`; +1 test. Espejo web + Android `363`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1167` / `MARKETING_VERSION 4.0` = MERCADO: en «Se marchan» la retirada usa la etiqueta con mayúscula «Se retira»/«Retires» (el feed la mantiene en minúscula, mid-sentence). Espejo web + Android `362`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1166` / `MARKETING_VERSION 4.0` = MERCADO: CONTINÚAN = solo quien estaba en el equipo en 2026** (`teamDetail` filtra `staying` por `currentTeamId == teamId`; un fichaje de fuera tiene afiliación 2027 pero va a "Llegan"). `ridersByAffiliation` trae `currentTeamId`; +1 test. Espejo web + Android `361`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1165` / `MARKETING_VERSION 4.0` = MERCADO: el detalle de equipo lee la plantilla 2027 MATERIALIZADA (`ridersByAffiliation` en `SupabaseService+Transfers.swift`, year=2027 → contrato del `dateTo` de la afiliación; `TransferRider.withContractUntil`) en vez del roster 2026; orden de secciones continúan→duda→LLEGAN→se marchan (llegan antes); año de contrato como BADGE (`YearBadge` en `TransfersTeamView`/`TransfersView`) y contrato SIN "hasta" (año pelado); separador " · " atenuado entre nombre y movimiento en el feed. Espejo web + Android `360`. Ver "🎽 MERCADO — panel equipo-céntrico" arriba. Sin `.swift` nuevos → NO requiere `./setup.sh`. Parse-check OK (sin Xcode → Xcode Cloud). **`1164` / `MARKETING_VERSION 4.0` = fix de alineación del header «Datos abiertos» en Ajustes.** El icono + título de la sección quedaban con **doble sangría** (más a la derecha que el resto de secciones): `aboutSection` ponía `.padding(.horizontal)` en el `VStack` EXTERIOR mientras que `sectionHeader` ya añade el suyo → el header sumaba dos. El resto de secciones dejan el VStack exterior sin padding y lo aplican por hijo. FIX: se retira el `.padding(.horizontal)` del VStack exterior y se pasa a la tarjeta (`.ccCardSurface().padding(.horizontal)`), como las demás. Bug PREEXISTENTE (no introducido por el botón de `1163`). Solo iOS: en Android cada título de sección va DENTRO de su `CCCard` (`Section`), sin header externo → no hay desalineación. `SettingsView.swift`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1163` / `MARKETING_VERSION 4.0` = el botón «Cómo se hace este proyecto» (Ajustes › Datos abiertos) adopta el aspecto del CTA «Ver clasificaciones» (espejo Android `359`).** Antes era un enlace de texto en color acento con una flecha ↗ dentro de la tarjeta; ahora es un **botón relleno de acento** (texto blanco semibold, ancho completo, `RoundedRectangle(cornerRadius: 3)`), calcado del primario de `ResultsButtonsCard` en `StageDetailView`. La nota de licencias (flag-icons/OSM/Esri/AWS) se conserva como párrafo encima; el `Link` envuelve solo el botón. `SettingsView.swift`, `aboutSection`. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode) → se confía en Xcode Cloud. **`1162` / `MARKETING_VERSION 4.0` = fix de coloración del botón «País» en la vista Temporada (toolbar).** Reportado por Dani con captura: el pill «País» salía SÓLIDO azul con contenido blanco (aspecto de estado ACTIVO) aunque no hubiera país elegido (`activeCountry == "all"`), en vez de su estado inactivo (accent-dim 14 % + globo/texto azul). CAUSA: en iOS 26 el toolbar envuelve el trigger de un `Menu` en un botón de **Liquid Glass** con tinte de acento propio, que PISA el `.background`/`.foregroundStyle` del label → el estado inactivo nunca se veía (el pill «2026» solo *parecía* correcto porque su estado objetivo es siempre sólido). FIX: `.buttonStyle(.plain)` en los dos `Menu` del toolbar (Año + País) de `SeasonView.swift` para que la cápsula personalizada mande sobre el cristal automático. **Android NO está afectado**: su `PillButton` pinta fondos Compose crudos (`primary@15%`/`surfaceVariant`) sin capa de cristal que los pise → el estado inactivo se distingue bien; sin cambios → sin bump de `versionCode`. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode) → se confía en Xcode Cloud. **`1160` / `MARKETING_VERSION 4.0` = reescritura del COPY de la pantalla "sin anuncios" (`PremiumShowcase`; espejo Android `357`).** Se alinea con el tono de las páginas web `about`/`abierto` (gancho de producto + nota de sostenibilidad factual, sin el matiz "proyecto independiente" como súplica). Título "Sin anuncios, independiente" / "Ad-free and independent", cuerpo y viñeta de apoyo ("Ayuda a cubrir el coste de los servidores y el mantenimiento") en `PremiumShowcaseOnboardingView.swift` (inline `LocaleService.t`). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1159` / `MARKETING_VERSION 4.0` = la pantalla "sin anuncios" (`PremiumShowcase`) se RE-muestra una vez a quien actualiza desde 2.x/3.x a 4.0+ (espejo Android `356`; ver "Pantalla sin anuncios para upgraders" arriba).** El gate del paso `premiumShowcase` en `OnboardingStep.firstPending` (`CalendarioCiclismoApp.swift`) pasa de `"ads_intro_done"` (ya `true` en todo el parque 2.3+) al flag NUEVO `"ads_intro_v4_done"` (`UserDefaults`), que nace en `false` para todos → dispara la pantalla una vez tras actualizar. Limpieza en el mismo commit: se BORRARON las escrituras de los gates viejos `"ads_intro_done"` (2.3) y `"premium_showcase_done"` (2.0), ya saturados y sin lectores → `markDone` (`PremiumShowcaseOnboardingView.swift`) marca solo `"ads_intro_v4_done"`. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode) → se confía en Xcode Cloud. **`1158` / `MARKETING_VERSION 4.0` = RESULTADOS: el override de país por jornada se aplica aunque la clasificación no traiga `raceDayId` (espejo web + Android `355`; ver "🏴 RESULTADOS" arriba).** `UciResultsData` gana la lista `raceDays`; el header (`ResultsView.refreshHeaderRaceDay`) se resuelve EN MEMORIA por `raceDayId` → `stageNumber` (sin fetch `raceDayById`); `ResultsFeedLogic` gana `rdByRaceStage` como fallback en `rdFor`. +2 tests. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1157` / `MARKETING_VERSION 4.0` = FIX DE BUILD: `BundledLogos` pasa a REFERENCIA DE CARPETA (`project.yml` `type: folder` + `.pbxproj` con UNA entrada de carpeta en vez de ~500 `PBXFileReference`) + fetch de logos no bloqueante en `ci_scripts/ci_pre_xcodebuild.sh` (`python3` desde `races.logoUrl`).** El build de Xcode Cloud fallaba con `The file logo_<hash>.webp couldn't be opened because there is no such file` desde que los logos se movieron a `.gitignore` (repo público): el checkout quedaba con `BundledLogos/` vacío mientras el `.pbxproj` seguía listando cada logo. Sin `.swift` nuevos, pero ⚠️ **cambio estructural del proyecto** → si se regenera con `setup.sh`/xcodegen, verificar que `- BundledLogos` (exclude) + el source `type: folder` siguen en `project.yml`. Ver "🖼️ LOGOS DEL BUNDLE" arriba + `docs/memory/race-data.md`. **`1156` / `MARKETING_VERSION 4.0` = RESULTADOS DE DOBLE SECTOR A/B (etapa partida 3A/3B) — espejo 1:1 de Android `354` + web; ver "🔀 RESULTADOS DE DOBLE SECTOR" arriba.** `ResultsStageKey` (enum stage(Int)|final) → clave `String` sector-consciente vía `UciResultsLogic.resultStageEntryKey`; `stagesByKey`/`stageKeys` separan 3A/3B por `raceDayId`; `ResultsStageSelector` pinta "3A"/"3B"; `ResultsRoute` gana `stageSuffix` (los CTA de la jornada/Hoy/competición lo pasan) + `ResultsView.initialStageSuffix`; `applyCancelledStages` descarta por `raceDayId` (no por número) + arrastre con sufijo; `UciResultsData` gana `sectorSuffixByRaceDayId`/`sectoredStageNumbers`; `RaceUciStage.carriedFromSuffix` (sintético, fuera de CodingKeys). **SIN migración/cambio de esquema.** +9 tests. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile NO ejecutado (entorno sin Xcode) → se confía en Xcode Cloud. **`1155` / `MARKETING_VERSION 4.0` = MERCADO: la chapa 2027 OCULTA muestra los COLORES ANTIGUOS (2026) del equipo preexistente, o nada si es nuevo (espejo web + Android `353` + mig. 129; ver "🎽 MERCADO" arriba).** `TransfersLogic.badgeSeason(for:prev:)` (+3 tests) resuelve la chapa efectiva (publicado → fila 2027; oculto + fila 2026 → colores antiguos; oculto + sin fila 2026 → nil); `MarketData.prevSeasonsByTeamId` trae las filas 2026 completas; `TransfersSeasonBadge` deja de auto-gatearse (el gate `if let` vive en el call site). Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode) → se confía en Xcode Cloud.  **`1154` / `MARKETING_VERSION 4.0` = `isDev` RETIRADO del DTO y del modelo de vista (espejo Android `350`; ver "🏷️ `isDev` RETIRADO" arriba).** `StartlistTeamDTO` pierde el campo y su `CodingKeys`, `StartlistTeamWithRiders` también, y `displayName` pasa a ser `globalTeam?.name ?? teamDTO.teamName` (ya no añade " (Devo)"). ⚠️ **Esta build es el prerrequisito de la migración 128**: mientras circulen apps <4.0, el `Bool` NO opcional del DTO viejo hace que el `DROP COLUMN` tumbe la lista de inscritos entera en esas versiones. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode: `xcode-select` → CommandLineTools) → parse-check OK + 0 referencias residuales; Android, el espejo, compila + tests verdes. ⚠️ La tabla iba por `1151` mientras `project.yml`/`project.pbxproj` estaban en `1153` (bumps previos sin actualizarla) → resincronizada aquí. **`1151` / `MARKETING_VERSION 4.0` = MERCADO: la chapa 2027 OCULTA ya no deja hueco — se retira el placeholder circular (espejo 1:1 de Android `347` + web; ver esa celda para el diagnóstico y el gotcha del spacing).** `TransfersSeasonBadge` pierde la rama `else` del `Circle()` y gana el helper estático **`isVisible(_:)`**, que consultan los 2 call sites (`teamRow` en `TransfersView`, cabecera en `TransfersTeamView`) para NO montar la vista cuando no hay chapa — en un `HStack(spacing:)` un `EmptyView` gastaría el hueco igual. Sin `.swift` nuevos → **NO requiere `./setup.sh`**. Compile local NO ejecutado (entorno sin Xcode: `xcode-select` → CommandLineTools) → parse-check OK en los 2 archivos; se confía en Xcode Cloud (Android, el espejo, compila + tests verdes, y la web está verificada en navegador contra prod). **`1150` / `MARKETING_VERSION 4.0` = MERCADO: la chapa 2027 pasa a OPT-IN ESTRICTO en el cliente (espejo 1:1 de Android `346` + web; ver esa celda para el diagnóstico completo).** `TransfersSeasonBadge` (`TransfersView.swift`): `season.badgeVisible ?? true` → `== true`, así un `badgeVisible` ausente cuenta como oculta. El modelo `TeamSeason.badgeVisible` sigue siendo `Bool?` (no se toca); `badgeVisible` solo lo lee Fichajes. Sin `.swift` nuevos → **NO requiere `./setup.sh`**. Compile local NO ejecutado (entorno sin Xcode: `xcode-select` → CommandLineTools) → parse-check OK; se confía en Xcode Cloud (Android, el espejo, compila + tests verdes, y la web está verificada en navegador contra prod). **`1149` / `MARKETING_VERSION 4.0` = MERCADO: dudas (corredor + equipo) + fecha ocultable (mig. 123; espejo 1:1 de Android `345` + web).** Ver "🔁 MERCADO — DUDAS" arriba y la celda de Android para el detalle. `TransfersLogic` espejo (`DoubtRow`/`TeamDetail.doubtful`, `confirmedFeed` con `dateVisible`); `RiderTransfer.dateVisible` con **init propio** (`decodeIfPresent ?? true` → un select que omita la columna no tumba la pantalla) + init de memberwise explícito para tests; `TeamSeason.continuityDoubt: Bool?` (misma convención que `badgeVisible`); `DoubtBadge` (con `text` opcional para el chip "Continuidad en duda" de la lista) + `teamDoubtNotice` + `Color.transfersDoubt` `#8B5CF6`. ⚠️ `DoubtBadge` usa `LocaleService.shared.t` porque vive en una `View` (`@MainActor`) — en `Models/`/`Services/` seguiría siendo `LocaleService.t` ([[feedback_ios_localeservice_nonisolated_in_models]]). **7 tests nuevos** en `TransfersLogicTests` (espejo de los de Android). Sin `.swift` nuevos → **NO requiere `./setup.sh`**. Compile local NO ejecutado (entorno sin Xcode: `xcode-select` → CommandLineTools) → parse-check OK en los 6 archivos; se confía en Xcode Cloud (Android, el espejo, compila + 22 tests verdes). **`1148` / `MARKETING_VERSION 4.0` = la ETAPA CANCELADA ya NO muestra el trofeo de resultados en las cards de Hoy ni de competición (espejo 1:1 de Android `344`; ver esa celda para el diagnóstico completo).** `SupabaseService.inhouseStagesForDays(raceId:days:cancelledDayIds:)` salta las jornadas canceladas (`if cancelledDayIds.contains(dayId) { continue }`); `TodayView.loadInhouseMap` y `RaceDetailView` pasan el set. El Revive se apaga solo con el trofeo; el CTA de la FICHA (`hasInhouseResults = has || rd.isCancelledDay` en `StageDetailView`) NO se toca. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode utilizable) → parse-check OK en los 3 archivos; Android, el espejo, compila + tests verdes. **`1147` / `MARKETING_VERSION 4.0` = la ETAPA CANCELADA vuelve a ser PULSABLE en Hoy y en Mes (SOLO iOS; Android ya era correcto).** Reportado por Dani: "en Hoy las etapas canceladas siguen sin ser clicables como sí lo son en Competición". CAUSA: `TodayView.raceItemView` (línea 623) y `MonthView` (línea 450) gateaban con `isRestDay || isCancelledDay` → renderizaban la card PELADA (sin `NavigationLink`) para ambos casos, mezclando dos cosas distintas: el DESCANSO es inerte de verdad (no tiene ficha que abrir), pero la cancelada CONSERVA su ficha (recorrido, perfil, rutómetro, mapa, documentación). Es el punto ciego del commit `daa99853e94` ("pulsable en competición"), que aplicó esta misma regla a `RaceDetailView`/`RaceScreen` en ambas plataformas pero NUNCA tocó Hoy ni Mes. FIX = el gate pasa a `isRestDay` solo, en los dos sitios (mismo comentario que la vista de competición). **Android NO necesitaba cambios**: sus cards de Hoy/Mes no tienen chevron por card y son clicables incondicionalmente; su `onClick` solo desvía al modal de placeholder para filas sintéticas (`isPlaceholder`/`editorialStatus=='placeholder'`) y para `race.isCancelled` (la CARRERA entera no se corre → no hay ficha) → una JORNADA cancelada ya caía a `navigate(Routes.stage(...))`. La rama de `race.isCancelled` (modal) sigue ganando en iOS: se evalúa antes que la navegación. Sin cambios en los labels de VoiceOver (`stageRowLabel`/`monthRowLabel` ya anuncian "etapa cancelada") ni en el resto de gates de `isCancelledDay` (mini-perfil, badge de inscritos, horarios, TV/Live Texto, badge de tipo, fila de metadatos de Mes), que son correctos y ya tienen paridad con Android. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode utilizable) → parse-check OK en ambos archivos + suite web verde; se confía en Xcode Cloud. **`1146` / `MARKETING_VERSION 4.0` = ETAPA CANCELADA en Resultados: pestaña "Etapa" con aviso + generales ARRASTRADAS de la etapa anterior (espejo 1:1 de Android `343` + web).** Ver la sección "Resultados de una ETAPA CANCELADA" arriba. `UciResultsLogic.applyCancelledStages` (+6 tests espejo de Android, incl. doble sector); `loadResultsData` carga las jornadas SIEMPRE y sintetiza antes del `guard stages.isEmpty`; campos sintéticos `isCancelledStage`/`carriedFromStage` (fuera de `CodingKeys`); avisos `cancelledStageNotice`/`carriedStandingsNotice` en `ResultsTableView` (el `.task` corta antes de pedir filas del marcador). Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode) → parse-check OK; Android, el espejo, compila + tests verdes y está verificado en emulador. **`1145` / `4.0` = recupera el CTA "Ver clasificaciones" y el "Revive" de la ficha de una etapa cancelada** (regresión mía; ver la celda de Android `342`). **`1144` / `MARKETING_VERSION 4.0` = ETAPA CANCELADA: se retiran horarios y TV/Live Texto + banner de aviso (espejo 1:1 de Android `341` + web).** Ver la celda de Android para el diagnóstico completo. `StageDetailView`: `timeSection` no pinta horario si `isCancelledDay` (los `hasStart`/`hasFinish` lo gatean y los `if let` internos lo comprueban), `broadcastSection` vacía sus broadcasts, `documentationChips` filtra el asset `live_text` (`viewModel.sortedAssets`) y `StageInfoHeader` gana el banner "Etapa cancelada"/"Carrera cancelada" (por `race?.isOneDay`; `LocaleService.t` nonisolated — es un `struct` de modelo, ver [[feedback_ios_localeservice_nonisolated_in_models]]). `RaceCardView`: `TVBadge` gateado en sus DOS call sites (layout horizontal y vertical), `timesColumn` vacío y la línea "Salida HH:MM" del layout vertical oculta. **Fix de alineación (paridad con Android):** el `FlowLayout` propio colocaba TODOS los subviews en la `y` del tope de la fila → un texto pelado ("Cancelada") quedaba alto junto a un `CategoryBadge` con fondo+padding; ahora `arrange` recentra cada fila sobre su altura definitiva (`centerRow`). Beneficia a cualquier fila de alturas mixtas (el `FlowLayout` es compartido — lo usa también la fila del feed de `1069`). **2ª pasada (mismo día, feedback de Dani):** (a) **la jornada cancelada NO se atenúa** — fuera el `.opacity(0.5)` por `isCancelledDay` de `RaceCardView` (Hoy), `MonthView` y `StageRowView`; los dos primeros lo conservan SOLO para la CARRERA cancelada (`race.isCancelled`); (b) **la fila de etapa cancelada vuelve a ser PULSABLE** en competición: `RaceDetailView` la envuelve en su `NavigationLink` (el gate pasa a `isRestDay` solo) y el `accessibilityHint` de `StageRowView` vuelve a anunciarla. Solo el DESCANSO queda inerte. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode utilizable) → parse-check OK; se confía en Xcode Cloud (Android, el espejo, compila + tests verdes y está verificado en emulador: chevron + tap abren la ficha). ⚠️ La tabla saltó de `1142` a `1144`: el repo ya iba por `1143` sin entrada documentada. **`1142` / `MARKETING_VERSION 4.0` = FIX DE COMPILACIÓN: el cintillo del mercado de fichajes rompía el build de iOS (`TodayHighlight.swift:68`).** Cazado por Dani en Xcode Cloud: `Call to main actor-isolated instance method 't' in a synchronous nonisolated context` + `Main actor-isolated static property 'shared' can not be referenced from a nonisolated context`. CAUSA: el título del cintillo `targetType='transfers'` (mig. 122, commit `c7f8b6545c7`) se resolvía con `LocaleService.shared.t(...)` — la variante de INSTANCIA, aislada al `@MainActor` — desde `TodayHighlightView.title`, que es una propiedad calculada SÍNCRONA y no aislada de un `struct` (los modelos no son `@MainActor`). `LocaleService` ya tiene desde antes la variante `nonisolated static func t(_:_:)` (lee el idioma de UserDefaults) documentada como "para usar desde modelos y servicios sin @MainActor", y el PROPIO archivo ya la usaba 13 líneas más abajo (`LocaleService.t("Hoy", "Today")` en `detail`) — la línea del mercado se escapó con `.shared`. FIX = 1 línea: `LocaleService.shared.t` → `LocaleService.t`. Barrido del proyecto: queda 1 solo `.shared.t(` (`TransfersTeamView.swift:216`), legítimo — vive en una `View` SwiftUI, que sí es `@MainActor`. ⚠️ REGLA: en `Models/` y `Services/` usar SIEMPRE `LocaleService.t(...)`/`LocaleService.isEnglish` (nonisolated); `.shared.t(...)` solo dentro de vistas. NOTA: este error venía del commit del mercado inicial, NO del `1141` (la regla de nombres por temporada); el parse-check de aquella sesión no lo detecta porque es un fallo de aislamiento de actores (type-check), no de sintaxis — de ahí que solo saliera en Xcode Cloud. **`1141` / `MARKETING_VERSION 4.0` = Fichajes: el equipo de ORIGEN se pinta con su nombre de la temporada EN CURSO (2026), no con el del mercado (2027) — espejo 1:1 de Android `339` + web (ver esa celda para el diagnóstico completo).** `MarketData.teamNamePrev` (lo carga `loadTransfersMarket` con `async let prevSeasonsTask = try? teamSeasons(year: season - 1)` → `?? []`, degrada a vacío sin tumbar la pantalla) + `enum TeamSide { case from, to }` en `TransfersLogic.teamLabel`, con fallback cruzado y `teams` solo para equipos sin fila en ninguna temporada. Call sites `.from`: retirada + origen del fichaje (`TransfersView`) y la lista "llegan" (`TransfersTeamView`). 2 tests nuevos en `TransfersLogicTests` (espejo de Android). Sin `.swift` nuevos → **NO requiere `./setup.sh`**. Compile local NO ejecutado (entorno sin Xcode utilizable) → parse-check OK en los 5 archivos tocados; se confía en Xcode Cloud (Android, el espejo, compila + 14 tests verdes). **`1140` / `MARKETING_VERSION 4.0` = APPS 4.0 — espejo 1:1 de Android `338`: pestaña FICHAJES (`arrow.left.arrow.right`, value 2; Calendario pasa a value 3 y el `tabMap` de NotificationManager se reindexa — `transfers`:2, `search`→0 para pushes antiguos) sustituye a Buscar (borrados `SearchView.swift`/`SearchViewModel.swift`, archivados en `archive/buscador-apps-2026/`; `AccessibilityID.tabTransfers` reemplaza a `tabSearch` y AccessibilityTests actualizado) + onboarding SIN paso offline (borrado `OfflineOnboardingView.swift` + case del enum). Piezas nuevas: `Models/RiderTransfer.swift` (+`TransferRider`), `Services/TransfersLogic.swift` (+`TransfersLogicTests`), `Services/SupabaseService+Transfers.swift`, `Views/Transfers/TransfersView+TransfersTeamView` (push por valor `TransfersTeamRoute`), `TeamSeason.gender/badgeVisible`; cintillo nativo `targetType='transfers'` → conmuta a la pestaña vía `pendingDeepLink=.tab(2)`. **Requiere `./setup.sh`** (hecho; scheme `CalendarioAnalytics.xcscheme` restaurado tras regenerar). ⚠️ Worktree: xcodegen necesita `Config/Supabase.xcconfig` del checkout principal. Compile local NO ejecutado (sin Xcode utilizable) → parse-check OK en los 16 archivos tocados; se confía en Xcode Cloud.** **`1139` / `MARKETING_VERSION 3.2.13` = "Así está la carrera" (CTA de resultados de la etapa anterior, en la jornada) abre en la clasificación GENERAL (GC), no en la clasificación de etapa (espejo 1:1 de Android `337` + web).** La ruta de Resultados gana la clasificación inicial: `ResultsRoute` y `ResultsView` ganan `classKind`/`initialClassKind`; `applyInitialSelection` siembra `activeClassKind` con la pedida si existe en la etapa, si no la primera (`stage`) — degradación con gracia; `StageDetailView` construye la ruta del CTA "Así está la carrera" con `classKind: "gc"` (el CTA de la etapa ACTUAL sigue en su etapa) y pasa `initialClassKind: route.classKind` al `NavigationLink → ResultsView`. Los demás call sites de `ResultsRoute`/`ResultsView` (Hoy, competición, feed, Campeonatos) omiten el parámetro (default `nil` → abren en `stage`, sin cambios). Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode utilizable) → se confía en Xcode Cloud; Android (espejo) compila + tests verdes + AAB. **`1136` / `MARKETING_VERSION 3.2.12` = fix del ENLACE del badge de TV en directo (Hoy/Competición): una emisión YA EN DIRECTO gana el enlace a una de MAYOR tier que aún no ha empezado (espejo 1:1 de Android `334` + web).** Bug reportado por Dani con la etapa 4 del Tour de Francia (LIVE con Eurosport emitiendo pero el badge enlazaba a RTVE, aún sin empezar). `selectedBroadcast` (`TVBadge.swift`) antepone ahora `isBroadcastLive(_:)` (startTime ≤ ahora) al tier/sortOrder: entre emisiones EN DIRECTO manda el tier, sin ninguna en directo se conserva el tier puro. La HORA del badge (la más temprana) no cambia. Espejo web (`pickBadgeBroadcast`) y Android. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode utilizable) → se confía en Xcode Cloud; Android (espejo) compila + tests verdes. **`1135` / `MARKETING_VERSION 3.2.11` = en CRI/CRE, "salida neutralizada"/"meta estimada" se sustituyen por "salida 1º corredor/equipo" y "meta último corredor/equipo" (espejo 1:1 de Android `333` + web).** `StageDetailView.swift` gana `startLabel(_:)`/`finishLabel(_:)` (por `rd.primaryType` + `viewModel.race?.isFemale`) que sustituyen a los literales fijos "Salida neutralizada"/"Meta estimada" en `timeSection`; misma cascada a los `accessibilityLabel`. Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (sin Xcode en el entorno) → verificado por lectura de código; espejo exacto de Android `333`. **`1134` / `MARKETING_VERSION 3.2.10` = fix de jornadas que desaparecían de Mes/Calendario y del buscador cuando una carrera cae en un año con >1.000 `race_days` publicados (espejo 1:1 de Android `332` + web).** Bug reportado por Dani: en la vista de Mes no salía NINGUNA etapa del Tour de Francia 2026 salvo la etapa 10 y los dos descansos. Verificado con SQL directo contra la BD: los 23 `race_days` del Tour (21 etapas + 2 descansos) están perfectamente publicados y completos — NO era un dato mal introducido. CAUSA real: `raceDays(from:to:)`/`raceDaysForSearch` (`SupabaseService.swift`) piden TODO el año de golpe (`MonthViewModel.loadYear`) sin `.range()` ni `.order()`; PostgREST aplica un tope server-side de 1.000 filas por request que un `.limit()` más alto NO evita (el mismo tope que ya documentaba `panel.js` para `riders_men`/`riders_women`). 2026 ya supera las 1.000 jornadas publicadas → la respuesta se trunca en silencio, y como no hay `ORDER BY`, el recorte no sigue la fecha: puede tocar carreras enteras de mitad de temporada (el bloque de 23 filas del Tour cayó casi entero fuera de las primeras 1.000 del orden natural de escaneo, dejando solo 3 — confirmado reproduciendo el mismo recorte con SQL). FIX: `raceDays(from:to:)` pagina en chunks de 1.000 vía `.range(from:to:)` en bucle hasta agotar el rango, ordenando por `id` (clave única — paginar por `dateKey`, no único, puede saltar/duplicar filas en el borde de página); `raceDaysForSearch` pasa a delegar en `raceDays(from:to:)` (antes usaba `.limit(5000)` creyendo evitar el tope, pero el servidor lo recorta a 1.000 igual). Mismo fix en Android (`raceDaysInRange`/`raceDaysForSearch`) y en `js/buscar.js` (el único punto del WEB que pedía el año completo de golpe; Mes/Temporada web ya cargaban por ventanas de 1-3 meses, a salvo). Sin `.swift` nuevos → NO requiere `./setup.sh`. Compile local NO ejecutado (entorno sin Xcode utilizable) → se confía en Xcode Cloud. ⚠️ La tabla saltó de `1121` a `1133`/`3.2.9`: el repo ya iba por `1133`/`MARKETING_VERSION 3.2.9` sin entrada documentada (bump previo sin detallar en esta tabla) — resincronizado aquí. **`1132` / `MARKETING_VERSION 3.2.8` = hora REAL del pie de puerto en la guía de horarios de paso (campo `footTimeUtc`; espejo Android `329` + web).** El pie de cada puerto se interpolaba linealmente (velocidad del llano aplicada a la subida → irreal). Ahora `ProfileSummit.footTimeUtc` (hora del rutómetro de ASO) es ancla real. 1 línea en `Services/SimplifiedGuide.swift` (el `climb_foot` usa `s.footTimeUtc`) + `let footTimeUtc: String?` en `Models/ElevationProfile.swift` (aditivo, `Codable` automático ignora campos extra) + helper de test `summit(...)` con el parámetro nuevo + test `test_footTimeUtcUsedAsRealAnchor`. Sin `.swift` nuevos → NO requiere `./setup.sh`. ⚠️ La tabla saltó de `1121` a `1132`: el repo real iba por `1131` (Xcode Cloud quemó builds con pushes web/Android entre medias); gana Xcode Cloud → resincronizado a `1132`. Compile local NO ejecutado (entorno sin Xcode utilizable) → se confía en Xcode Cloud; Android (espejo) compila + tests verdes. **`1121` / `MARKETING_VERSION 3.2.7` = doble sector en orden cronológico (Hoy+Mes) + auto-avance de medianoche (espejo Android `328` + web).** (1) **Doble sector**: dos jornadas de la misma carrera el mismo día se ordenan por hora de salida (`neutralStartTimeUtc`) y, si falta, por el sufijo A/B — desempate añadido en `RaceLogic.sortByCategory` (Hoy) y en el sort de `MonthViewModel` (Mes), tras la categoría UCI. (2) **Auto-avance de medianoche**: `TodayViewModel.advanceIfNewLocalDay()` (con `lastTodayKey`) cableado en `TodayView` (en `scenePhase == .active` y en el ciclo de refresco de 300 s, ANTES del guard `isToday`); avanza al nuevo "hoy" local solo si se está viendo hoy. (El switch rosado era solo de Android.) Sin `.swift` nuevos → **NO requiere `./setup.sh`**. Compile local NO ejecutado (entorno sin Xcode utilizable) → se confía en Xcode Cloud; balance/parse OK y Android (espejo) compila + tests verdes. **`1120` / `MARKETING_VERSION 3.2.7` = RETIRADA de la ficha de corredor (reducción de scope; espejo de Android `327` + web).** Borrados `Views/Rider/RiderView.swift` (con `RiderRoute`), `Services/RiderProfileLogic.swift`, `Services/SupabaseService+Rider.swift` (con `SearchableRider`/`RiderProfileInfo`/`searchableRiders()`) y `Tests/RiderProfileLogicTests.swift`. En Startlist/Resultados/Buscador el corredor pasa a TEXTO (fuera `riderRoute`/`navigationDestination`→RiderView/`onRiderTap`). El **buscador solo lista carreras** (`SearchHit` reducido a `.race`). Gate retirado en cascada: `currentTeamCategory` (`ResolvedRider`), `SlimTeamCategory`/`teamCategoriesByIds`, `riderLinkEligible`/`riderId` de `UciResultsLogic` (NO se tocó `buildIndividualRows`/`collapseTtt`/`teamsInClass`/`byDorsal`/`byRider`/`byTeamOverride`). **SE CONSERVAN** chapas, banderas, filtro por equipo, CRE colapsada, override de equipo, **tachado de abandonos** (`globalRiderId`), `enrichRidersByGlobalId`. `CURRENT_PROJECT_VERSION 1119→1120` (4 en `project.pbxproj` + 2 en `project.yml`); `MARKETING_VERSION 3.2.7` sin tocar. **Requiere `./setup.sh`** (4 `.swift` borrados → proyecto regenerado; ⚠️ `setup.sh`/xcodegen NO declara el scheme manual `CalendarioAnalytics.xcscheme` en `project.yml` → lo borra al regenerar; se restauró desde `origin/main`, vigilar en futuros `setup.sh`). **Compile/test local NO ejecutado** (entorno: `xcode-select` apunta a CommandLineTools, sin Xcode utilizable) → **se confía en Xcode Cloud** al pushear; parse-check (`swift-frontend -parse`) y verificación de símbolos OK, 0 referencias residuales. **`1119` / `MARKETING_VERSION 3.2.7` = Resultados in-house: override MANUAL de equipo desde el panel (espejo 1:1 de Android `326` + la web; mig. 112).** Nueva columna `race_uci_results.teamId` = OVERRIDE de equipo, fijada por fila en el panel, que GANA a la resolución por dorsal/globalRiderId en el render. `UciResultsLogic.buildIndividualRows`/`collapseTtt`/`teamsInClass` aceptan un mapa `byTeamOverride` (`[String: Team]`) y lo anteponen a `byDorsal`/`byRider`; `RaceUciResultRow.teamId` nuevo (Codable: en CodingKeys + decoder + ambos inits); `SupabaseService.enrichTeamsByIds(_:)` lo construye desde `teams` (espejo de `enrichOverrideTeams` web); `ResultsTableView` lo enriquece on-demand en el `.task` de carga. Tests nuevos en `UciResultsLogicTests` (espejo de los 5 de Android). Sin `.swift` nuevos → **NO requiere `./setup.sh`**. **`1117` / `MARKETING_VERSION 3.2.5` = Resultados in-house: fallback por `globalRiderId` para los Campeonatos Nacionales SIN startlist (la web ya lo tenía; Android = espejo `324`).** El equipo y la bandera de cada corredor en Resultados se resolvían SOLO por dorsal contra la startlist curada (`buildByDorsal`). En los CN sin startlist con dorsales casables —España: filas con `bib` NULL y startlist con dorsales a 0; Francia: sin startlist— las filas salían peladas, mientras que Italia (dorsales que SÍ casan) mostraba chapa + bandera. Se porta el fallback `byRider` de la web: `UciResultsLogic.buildIndividualRows`/`collapseTtt`/`teamsInClass` aceptan un mapa `byRider` (globalRiderId → `ResolvedRider`) y caen a él cuando la fila no casa por dorsal (`byDorsal` con prioridad); `SupabaseService.enrichRidersByGlobalId(_:)` lo construye desde `riders_men`/`women` + equipo ACTUAL (nombre + chapa + categoría para el gate de la ficha). `ResultsTableView` enriquece on-demand los `globalRiderId` sin dorsal (en el `.task` de carga) y los pasa a la tabla individual y a la CRE. Tests nuevos en `UciResultsLogicTests`. Sin `.swift` nuevos → **NO requiere `./setup.sh`**. **`1116` / `MARKETING_VERSION 3.2.5` = release del sort de Campeonatos Nacionales en el feed de Resultados** (el cambio de código ya estaba en `1115`/`e6811edaddc` bajo `3.2.4`, marcado "se enviará en una versión posterior" → ahora se envía como 3.2.5; espejo de Android `322` + web `js/resultados-feed.js`). El feed nativo "Últimos resultados" (`ResultsFeedLogic.compareEntries`) no aplicaba el orden canónico de Campeonatos Nacionales; ahora invoca `ChampionshipsConfig.compare` tras el agrupamiento por carrera y antes del ranking de grandes vueltas → país según `COUNTRY_ORDER`, luego línea masc/fem/sub23m/sub23f, luego CRI masc/fem/sub23m/sub23f (solo `uciCategory='CN'`; `compare` devuelve nil para el resto). El `rd` da el `primaryType` para deducir línea/CRI; se llama solo si ambos `rd` no son nil (las CN son de un día → siempre tienen `rd`; la general final, única entrada sin `rd`, nunca es CN). Hoy y Mes ya lo aplicaban (`RaceLogic.byCategory`/`MonthViewModel`); el feed era el único hueco. El panel es solo web. Test nuevo en `ResultsFeedLogicTests` (espejo del de Android). Sin cambios de código respecto a `1115` (solo bump de `CURRENT_PROJECT_VERSION` + `MARKETING_VERSION`) → **NO requiere `./setup.sh`**. **`1114` / `MARKETING_VERSION 3.2.4` = fix del huso horario en el orden de salida (CRI/CRE)** (espejo 1:1 de Android `322` + web): la conversión de hora local de la carrera → hora del usuario usaba `raceDay.date`, que está **NULL** en 15 de las 36 jornadas con orden de salida (los CN del 25-jun: Irlanda/Suiza/Noruega/Francia/GB) → `raceLocalInstant` devolvía null y se mostraba la **hora cruda sin convertir**. FIX de raíz: `StartOrderRaceDay.effectiveDate` = `dateKey ?? date` (`Models/StartOrderEntry.swift`; `dateKey` es canónico y nunca null) + `dateKey` añadido al `.select(...)` de `StartOrderViewModel`, usado en `shouldConvertTime`/`convertedTime`/`tzOffsetLabel`. Espejo Android (`StartOrderScreen.kt` + `SupabaseService` select) y web (`js/orden-salida.js`). Además `date` rellenado en BD en esas 15 jornadas. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1113` / `MARKETING_VERSION 3.2.3` = tanda de arreglos de Campeonatos Nacionales + badge de TV (TODO bajo 3.2.3, SIN bump intermedio; espejo 1:1 de Android `320`).** Sobre la base (a)/(b) (botón de resultados en CN terminados + TV por región en la celda, detallado abajo): **(c)** el badge de TV NO se muestra cuando TODA la emisión es de fuera de la región (`regionBlocked` en `TVBadge.swift` → `label` ""; con 0 broadcasts sigue saliendo de `tvStatus`); **(d)** el horario del badge = la emisión accesible que ANTES empieza, desacoplado del enlace (si una emisión global ALL empieza antes que las de tu grupo, su hora manda; España CN: Aragón TV ALL → "TV 12:00" aunque el enlace gane TDP/Eurosport a las 13:30); el enlace sigue por tier; **(e)** la rejilla de Campeonatos abre los resultados in-house NATIVOS (`inhouseStageKeys` + `ChampionshipResultsRoute` → `ResultsView`) cuando existen, con FC/PCS de respaldo. Espejo Android/web. Sin `.swift` nuevos → NO requiere `./setup.sh`. **Detalle base (a)/(b):** (1) **Resultados**: `RaceLogic.isRaceConcluded` exigía `estimatedFinishTimeUtc != nil` y los CN lo tienen NULL → nunca concluían → la celda nunca mostraba FC/PCS. Ahora es espejo FIEL de la `isRaceConcluded` exportada de la web (sin ese guard): cae al fallback de `dateKey` 18:00 UTC de `raceTimeCheck`. Único consumidor en la app = la rejilla de Campeonatos (`ChampionshipsView`). (2) **TV por región**: `ChampionshipEventCell` filtra `item.broadcasts` por `RegionService.shared.current.allowedBroadcastGroups` (nueva `regionBroadcasts`) antes de `hasTvInfo`/`championshipTvState`. Y `TVBadge.swift` (race cards) usaba los broadcasts CRUDOS para label/icono/`hasBroadcasts`/`earliestTimedBroadcast` (solo el `selectedBroadcast` del enlace iba filtrado) → un usuario de España veía "TV 14:00" de una emisión solo de Bélgica; ahora TODO el badge usa `regionBroadcasts`. Tests `RaceLogicTests` actualizados (sin hora de meta: dateKey pasado→true, futuro→false). El detalle de etapa (`StageDetailViewModel`) ya filtraba por región. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1111` / `MARKETING_VERSION 3.2.1` = la última card de Hoy ya NO queda tapada por la tab bar flotante (bug post-3.2 reportado por Dani; VERIFICADO en simulador iPhone 17 Pro iOS 26.3).** En una sesión previa (entradas `1104`–`1108`) se retiró el mecanismo de medición-y-padding y se dejó SOLO `.ignoresSafeArea(.container, edges: .bottom)` sobre el `contentArea` de Hoy. Eso da el efecto traslúcido (las cards pasan bajo la barra al desplazarse ✓) pero **el último ítem quedaba tapado y no clicable**: a diferencia de Mes/Temporada (que cuelgan de un `TabView(.page)` y SÍ reciben el content inset del sistema), en Hoy la envoltura `.offset(x:)` + `.clipped()` del swipe de día ROMPE la cadena por la que UIKit inyectaría ese inset. **Medido con overlay + `print` de depuración en simulador:** `safeAreaInsets.bottom` del `ScrollView` interno = **0.0** CON y SIN `.ignoresSafeArea`, e igual el del `.background` externo; la barra flotante de iOS 26 TAMPOCO modifica el safe area de la window (solo el home indicator, 34pt). ⚠️ La afirmación de `1108` de que "el sistema añade SOLO el content inset para que el último ítem la libre" era **FALSA para esta jerarquía** — Mes/Temporada libran el último ítem por el `TabView(.page)`, NO por el `.ignoresSafeArea`. FIX: nuevo `TabBarInsetReader` (`UIViewRepresentable` privado al final de `TodayView.swift`) que recorre la jerarquía UIKit de la window hasta la `UITabBar` y publica su **altura real** (`tabBar.frame.height` = **83pt** en 17 Pro = 49 barra + 34 home indicator; **0 si no hay tab bar** → tests/iPad) en un `@State bottomBarInset`; se aplica como `.padding(.bottom, 8 + bottomBarInset)` al `LazyVStack` de la lista de carreras → la última card queda COMPLETA sobre la barra al final del scroll, conservando el traslúcido de las intermedias. El reader re-mide en `didMoveToWindow`/`layoutSubviews` (rotación) y solo escribe el binding si cambió >0.5pt (sin ciclos de layout). El `.ignoresSafeArea` SE CONSERVA (da el traslúcido). Solo se tocó `TodayView.swift` → **NO requiere `./setup.sh`**. **⚠️ Mes/Temporada (`MonthView`/`SeasonView`) NO se tocaron** — siguen con su `.ignoresSafeArea` sobre el `TabView(.page)`, que ahí sí funciona; si en el futuro reaparece el corte en esas vistas, aplicarles el mismo `TabBarInsetReader`. Ver memoria [[project_ios_tabbar_overlap_inset]]. **`1108` / `MARKETING_VERSION 3.1.2` = overlap de la tab bar flotante en Hoy/Mes/Temporada RESUELTO DE VERDAD + Campeonatos colapsados en Temporada + botón País cápsula (todo VERIFICADO en simulador, iPhone 17 Pro iOS 26.5, con capturas).** (1) **Overlap — el problema real era el CONTRARIO de lo que documentaban `1104`–`1107`:** no era que la barra tapase el contenido, sino (a) un HUECO de 83pt entre la última card y la barra (= el `padding(.bottom)` que metía el mecanismo de medición) y (b) que las cards **se cortaban en seco** en el borde de la barra en vez de pasar **traslúcidas por debajo** (efecto Liquid Glass), como sí hace Resultados. CAUSA: en Hoy el `ScrollView` cuelga del frame `.frame(maxHeight:.infinity).offset().clipped()` del swipe de día, y en Mes/Temporada del `TabView` paginado `.tabViewStyle(.page)` — ambas capas RECORTAN el scroll en el borde inferior del área segura → el contenido no llega bajo la barra. FIX (lo que faltaba): **`.ignoresSafeArea(.container, edges: .bottom)`** sobre el `contentArea` de Hoy (después de `.clipped()`) y sobre el `.page` TabView de Mes/Temporada → el scroll llega al borde inferior de la pantalla, las cards pasan traslúcidas bajo la barra Y el sistema añade SOLO el content inset para que el último ítem la libre. Se **ELIMINÓ por completo** el mecanismo de medición manual (`captureBottomSafeInset` + `enum TabBarMetrics` de `CCCard.swift`, los `@State tabBarInset` + `.padding(.bottom, tabBarInset)` de las 3 vistas, y el `import UIKit` de `CCCard.swift`): ese padding era el origen del hueco. **Verificado en simulador**: cards de Hoy/Mes/Temporada pasando traslúcidas bajo la barra, y al fondo del scroll el último ítem la libra sin hueco. (2) **Temporada — Campeonatos colapsados en UNA card** (espejo de Mes y de `js/temporada.js`): `SeasonViewModel.filteredRaces` excluye `uciCategory=="CN"` y expone `hasChampionships`/`championshipsMonth`/`championshipsSortDate`; `SeasonView` inyecta una única fila sintética `SeasonChampionshipsRow` (globo + título + badge CN) intercalada por fecha (`ChampionshipsConfig.rangeStart` = 22-jun) en el mes de Campeonatos y navega a `ChampionshipsView` (nuevo `navigationDestination(for: ChampionshipsRoute.self)` en SeasonView); las CN sueltas ya no se listan y se excluyen del selector de país (`availableCountries`). Espejo Android en `SeasonScreen.kt` (`SeasonChampionshipsRow` + filtro CN + inyección por fecha → `Routes.CHAMPIONSHIPS`). (3) **Botón «País» → `Capsule()`** (antes `RoundedRectangle(cornerRadius: 3)`, se veía boxy junto al pill «2026»); conserva estado activo/inactivo (tenue accent-dim cuando «Todos los países» → azul sólido al elegir país). Decisión Dani: "solo la forma". (4) **Calendario abre en MES por defecto** (ya era así en iOS `@AppStorage("calendar_subview")="month"` y Android `else -> "month"`; verificado). Sin `.swift` nuevos → NO requiere `./setup.sh`. **⚠️ Documentación corregida — `1104`–`1107`:** aquellas entradas dieron por bueno el mecanismo de medición-y-padding y llegaron a afirmar que `1106` "resolvía" el overlap y que el corte a media lista era "normal" — **era FALSO**: el padding creaba el hueco y el corte NO era el efecto traslúcido. El fix correcto es el `.ignoresSafeArea` de `1108`. (Histórico de los intentos descartados: `1104` GeometryReader en `.background` daba inset 0; `1105` GeometryReader envolvente; `1106` fallback UIKit `TabBarMetrics`; `1107` reescritura a UIKit puro.) **`1103` = barra de clasificaciones de Resultados: separador + chevron de scroll (espejo 1:1 de Android `313`):** la fila de pestañas scrollea en horizontal y quedaba pegada al filtro "Todos los equipos" sin pista de que había más clasificaciones ocultas. Dos retoques en `ResultsClassTabsBar` (`Views/Results/ResultsComponents.swift`): (1) **`Divider`** (height 22) entre las pestañas y el `Menu` de equipos; (2) **botón chevron** (`chevron.right` en círculo `tertiarySystemBackground`) en `.overlay(alignment:.trailing)` del `ScrollView`, visible solo cuando `onScrollGeometryChange` detecta que `contentOffset.x + containerSize.width < contentSize.width` (queda contenido por ver); al tocarlo, `ScrollViewReader.scrollTo(última pestaña, anchor:.trailing)`. **Descartado un primer intento de degradado al color del fondo** (señal invisible, lo cazó Dani). Android usa el chevron equivalente con `canScrollForward`. ⚠️ `onScrollGeometryChange` es iOS 18+ (target ya 18.0). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1101` = Resultados, dos retoques (espejo 1:1 de Android `311` + web):** (1) **las generales del ÚLTIMO día se duplican bajo la última etapa** — `stagesByKey` (`ResultsView.swift`) inyecta las clasificaciones finales (`.final`, stageNumber nil: GC/puntos/montaña/jóvenes/equipos) TAMBIÉN en la clave de la última etapa numerada (`.stage(maxNum)`), conservando intacta la pantalla **'F'**. No se vuelcan dos veces (solo presentación; si la etapa ya trae el mismo tipo, manda la final). (2) **el filtro por equipos se ordena alfabéticamente** (`localizedCaseInsensitiveCompare`) en `UciResultsLogic.teamsInClass` — antes iba por orden de aparición. Test nuevo en `UciResultsLogicTests`. Sin `.swift` nuevos → NO requiere `./setup.sh`. (El repo venía de `1100`; entradas 1098–1100 sin documentar en esta tabla.) **`1097` = una CRI con ex aequo reales ya NO se pinta como CRE en Resultados** (espejo 1:1 de Android `306` + web): guard al inicio de `UciResultsLogic.isTttStage` — si `raceDayPrimaryType == "itt"` o `stageRaceType == "ITT"`, devuelve `false` (una CRI nunca es CRE aunque tenga ≥3 puestos compartidos). Nuevo parámetro `stageRaceType` cableado desde `stage.raceType` en los dos call sites de `ResultsComponents.swift`. Cazado en el Tour de Beauce 2026 etapa 4 (CRI de Québec con empates en 49/51/88). Test nuevo en `UciResultsLogicTests`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1096` = la etiqueta del corredor en el buscador distingue género** (espejo 1:1 de Android `305` + web): "Corredora" (de `riders_women`) vs "Corredor" (de `riders_men`); en inglés ambas → "Rider". Señal `SearchableRider.isFemale` (la fija `searchableRiders()` según la tabla) → `SearchRiderRow`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1095` = el buscador (tab Buscar) incluye CORREDORES con ficha** (espejo 1:1 de Android `304` + web): corredores de equipo ACTUAL top-división (WT/WWT/PT/PRW = `RiderProfileLogic.isPublicTeamCategory`) buscables por nombre y **mezclados por relevancia** con las carreras (un listado, cap 12). `SupabaseService.searchableRiders()` (teams id+name+category → públicos → `riders_men`/`riders_women` por `in(currentTeamId)`, ~1.300), `RiderProfileInfo.otherNames` añadido + struct `SearchableRider`. `SearchViewModel`: `enum SearchHit` (race/rider) + `scoreText()` (espejo de `score()` web) + carga riders con `async let`. `SearchView`: fila `SearchRiderRow` (bandera+nombre+equipo+"Corredor") → **push por valor** `navigationDestination(item: $riderRoute)` con `RiderView` (regla iOS, igual que StartlistView). Sin `.swift` nuevos → NO requiere `./setup.sh`. Tests: build + 264 OK. Ver memoria `project_search_riders`. **`1093` = CCMA (TV3/Esport3/3Cat) y EITB (ETB) entran en el tier de TV pública del badge de TV en directo** (espejo 1:1 de Android `303` + web; Xcode Cloud escupió `1092` con este contenido al integrar tras la tanda de Campeonatos + autocommits → repo fijado a `1093` para que el siguiente disparo case): el tier 2 deja de ser solo RTVE.es → ahora **RTVE.es + `ccma.cat`/`3cat.cat` + `eitb.eus`/`eitb.tv`** (por delante del resto de cadenas; YouTube y otras redes sociales siguen por encima). En `RaceLogic.broadcastLinkPriority(_:)` (+ tests `RaceLogicTests`). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1086` = prioridad del enlace del badge de TV en directo** (Hoy/Competición; espejo 1:1 de Android `299` + web): el badge enlaza a la mejor emisión por tier **YouTube > otras redes sociales (FB/IG/X/TikTok/Twitch/Kick) > TV pública (RTVE/CCMA/EITB) > resto de cadenas**; **Eurosport / HBO Max pasan a ser "una cadena más"** (antes ocupaban un tier propio por delante del resto). Fuente única `RaceLogic.broadcastLinkPriority(_:)` (espejo de `js/broadcast-priority.js` web y Android), cableada en `selectedBroadcast` de `TVBadge.swift`. ⚠️ `x.com` anclado con `//`/`.` para no capturar `play.max.com`. Tests en `RaceLogicTests`. Sin `.swift` nuevos → NO requiere `./setup.sh`. Ver `docs/memory/live-badge.md`. **`1085` = el pull-to-refresh de Resultados ahora SÍ recarga las filas** (paridad con Android `298`, mismo commit): las filas de cada clasificación las carga el propio `ResultsTableView` (`Views/Results/ResultsComponents.swift`) por `loadResultRows(stageRef: stage.id)`, NO `loadResultsData` → el `.refreshable` recargaba cabecera/etapas/equipos pero NO las filas visibles (el `.task(id: stage.id)` no se re-disparaba al no cambiar la etapa). Fix: un **token de recarga** (`rowsReloadToken`) que se incrementa en el closure del `.refreshable` (`ResultsView`), se pasa a `ResultsTableView` y entra en la clave del `.task(id: "\(stage.id)#\(reloadToken)")`; `rows` (@State) NO se resetea a nil → la tabla anterior permanece visible mientras recarga (sin parpadeo del spinner). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1082` = el filtro "Pro" (y Masc/Fem) incluye las CN élite en Hoy/Buscar** (espejo 1:1 de Android `295` + web, ver esa celda): `ChampionshipsConfig.isU23Championship`/`isFemaleChampionship` + rama `cat=="CN"` en `RaceLogic.matchesCategory` (élite→Pro/género, sub23 fuera). Tests en `RaceLogicTests`/`ChampionshipsConfigTests`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1081` = sort interno de la categoría CN en Hoy y Mes (Agenda)** (espejo 1:1 de Android `294` + web, ver esa celda): `ChampionshipsConfig.compare(_:_:_:_:)` + `countryIndex` + `cnSlotRank` cableado al principio de `RaceLogic.sortByCategory` (Hoy) y del sort inline de `MonthViewModel` (Mes), tras el guard de placeholders. Tests en `ChampionshipsConfigTests` (20 verde). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1078` = etiquetado GA4 de etapa en Resultados/Perfil/Orden de salida** (espejo 1:1 de Android `291`, ver esa celda): (a) **ResultsView** emite el `screen_view` desde `logStageView` (lo dispara `refreshHeaderRaceDay` en la carga inicial y en cada cambio de etapa) con `stage_name`+`race_day_id`, en vez del log único de `load()`. (b) **ElevationProfileView** añade `stage_name`+`race_name`. (c) **StartOrderView** añade `stage_name` vía `viewModel.fullRaceDay?.stageLabel`. Lado lectura: filtro `(not set)` en `top_stages` de la edge function `ga-analytics` (retroactivo). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1076`–`1077` = swipe de cambio de día "sigue el dedo" PROBADO Y REVERTIDO** (feedback Dani en TestFlight): el `.onChanged` que hacía seguir `contentOffset` al dedo (`1076`) dio una **fluidez grotesca en dispositivo** → `1077` revierte `TodayView.swift` al gesto previo (`.updating` + `.onEnded` con `animateNavigation`; sin seguir el dedo). La versión **web SÍ se quedó**. CURRENT_PROJECT_VERSION sube a 1077 (la 1076 ya se quemó en TestFlight). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1075` / `MARKETING_VERSION 3.1.1` = tooltip/indicación de abandono más limpia** (espejo de Android `288` y la web): el motivo bajo el corredor tachado en Inscritos ya NO antepone "Out of the race ·" / "Fuera de carrera ·" y un **prólogo** dice "prologue"/"prólogo" en vez de "stage 0"/"etapa 0". `StartlistView.swift` ramifica `sn == 0` → prólogo y devuelve solo el `label` en one-day. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1074` = pull-to-refresh (swipe down) en la vista Hoy** — `TodayView` tenía el `ScrollView` "pelado" SIN `.refreshable` (la frescura se delegaba al auto-refresco cada 5 min + recarga al recuperar red, así que el gesto NO existía y tirar hacia abajo no hacía nada). Ahora `swipe down → await viewModel.loadDay() + Haptics(.success)`, paridad con Resultados (`ResultsView`) y Jornada (`StageDetailView`, que ya lo tenían). Convive con el `DragGesture` HORIZONTAL de cambio de día (filtro `h > v*1.5` → el vertical no lo dispara). Sin `.swift` nuevos → NO requiere `./setup.sh`. (El repo ya estaba fijado a `1074` para el siguiente disparo de Xcode Cloud → este commit lo consume; sin re-bump.) **`1069` = 3 defectillos visuales de 3.1 cazados por Dani + esquema de URL registrado** (verificado en simulador con capturas): (1) los dots del loading volaban cruzando la pantalla — `.animation(value:)` con repeatForever animaba también la POSICIÓN al recolocarse la vista; ahora acotada a escala/opacidad con `animation(_:body:)` (`PulsingDotsView`, beneficia a toda la app); (2) la línea "Etapa N · salida › meta · km" del feed rebosaba la card — el `FlowLayout` mide el texto a su ancho IDEAL y el lineLimit no truncaba; ahora HStack con `truncationMode(.tail)` + badge `fixedSize` (puntos suspensivos); (3) el chip "País" de Temporada se veía "HDR" — `tertiarySystemBackground` dentro de la cápsula Liquid Glass del toolbar (iOS 26) dispara la adaptación de luminancia; inactivo ahora = tinte accent-dim 14% + texto accent. Bonus: **`CFBundleURLTypes` con el esquema `calendariociclismo` registrado en Info.plist** (onOpenURL existía pero el esquema no estaba declarado → `simctl openurl`/Safari no podían abrir la app; prerequisito del plan push→Resultados). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1068` / `MARKETING_VERSION 3.1.0` = APPS 3.1** (espejo 1:1 de Android `286`, ver esa celda): tab bar se queda en **4** (Hoy · Resultados · Calendario · Buscar; Ajustes sigue arriba — regla iOS); `CalendarTabView` con `@AppStorage("calendar_subview")` y toggle topBarTrailing en Month/Season (`switchAction`); deep links `month`/`season` → tab Calendario (NotificationManager.tabMap reindexado); `ResultsFeedView` + `ResultsFeedLogic` (11 tests); `RiderView`+`RiderRoute` por valor (drill-down ficha→clasificación→ficha) + `RiderProfileLogic` (15 tests) + `SupabaseService+Rider` (gate corta antes de cargar resultados); enlaces gateados por equipo ACTUAL (`currentTeamCategory`, `teamCategoriesByIds` slim compartida, fail-closed; en Inscritos el mapa aplica el override de season). Suite completa **TEST SUCCEEDED** post-merge con `6140f1a`. **Requiere `./setup.sh`** (8 `.swift` nuevos). **`1067` = startlist ordenada por el dorsal del PRIMER corredor** (espejo 1:1 de Android `285`, ver esa celda): sort al final de `StartlistViewModel.fetchTeams` (mínimo dorsal > 0; sin dorsal al final conservando sortOrder). Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1066` / `MARKETING_VERSION 3.0.2` = release de la ocultación de "Individual"** (3.0.1 ya estaba EN PRODUCCIÓN → estos cambios salen como 3.0.2; sin cambios de código respecto a `1065`, solo bump). **`1064`–`1065` = ocultación cosmética del equipo ficticio "Individual"** (espejo 1:1 de Android `282`–`283`, ver esa celda): helper libre `isIndividualPlaceholderTeam(teamId:teamName:)` en `Models/StartlistTeam.swift`, `buildByDorsal` (`SupabaseService+UciResults`) vacía el `teamName`, `StartlistViewModel.teamCount` lo excluye y `StartlistTeamCard` omite la cabecera; `1065` = header sin bloque "Equipos" cuando `teamCount == 0`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1063` = CRI con diferencias sobre tiempos truncados en Resultados** (espejo 1:1 de Android `281`, ver esa celda para la espec completa): `secondsToPressTime` + overload `secondsToGap(Double)` + `cleanTimeText` sin decimales + `buildIndividualRows` con tiempos Double/`isItt` + `RaceUciStage.raceType` + gate `isIttStage` cableado en `ResultsTableView`/`ResultsView` (param `isOneDay`). Tests espejo de los 12 de Android. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1062` = repaso integral del renderizado de las 270 banderas (auditoría CoreSVG completa).** Primera verificación del set entero contra el motor REAL de iOS: harness `FlagRenderAuditTests` (solo corre si existe `/tmp/flag-audit/names.json`; en CI se salta solo) vuelca el render CoreSVG de cada bandera en simulador y se pixel-diffea contra el canónico flag-icons 7.2.3 (= lo que ven web/Android). Salieron **48 banderas divergentes por 4 bugs más de CoreSVG** (además del `<use>` de `1059`): (a) patrón legacy `clip-path` userSpace + `transform` en el MISMO `<g>` → lienzo/clip a escala errónea (Tanzania se pintaba a 1/3, Aruba a 1/2, Libia a 3/4, bandas blancas en bordes; 60 banderas con el patrón, todas normalizadas: clip transformado a espacio raíz + transform anidado en `<g>` interior, geometría verbatim); (b) **`<marker>`/`marker-mid` no soportado** → us/um SIN sus 50 estrellas (expandidas a 50 `<path transform="translate()">`; OJO cairosvg coloca markers ~1px abajo-derecha de los navegadores → us/um se arbitran contra Chrome: 0.144% ✓); (c) **paints colgantes** `fill="url(#id)"` sin definición (sh-ac: upstream perdió 96 gradientes del escudo) — la spec/web los trata como `none`, CoreSVG los pintaba → `fill="none"` explícito; (d) **strokes con escala anisótropa** (es-ct franjas stroke-width 60 × `scale(.79,.89)`) → rects equivalentes (gb-sct diagonal renderiza PERFECTO → excluido a propósito). Tooling committeado: `scripts/flags/normalize-ios-flags.py` (idempotente) + `verify-flags-render.py` (puerta 1 cairo + puerta 2 simulador) + `scripts/flags/README.md` (catálogo de bugs + pipeline al actualizar el set). Resultado: **270/270 dentro de umbral**; residuo aceptado sub-visible: aw/es-ct ~0.56%, pf ~0.5%, dm/ni/kh ≤0.31% (AA de detalles finos); `gu` (`<text>`) y gradientes `href` (bz/fk/gt/gs/mx/ni) renderizan bien sin tocar. Android/web intactos (sus banderas son canónicas). Requiere `./setup.sh` (test `.swift` nuevo). **`1061` = pestaña Equipos de Resultados con chapa + nombre canónico** (espejo 1:1 de Android `279`, ver esa celda): `UciResultsLogic.normalizeTeamName`/`findMatchingTeam` + `Team.nameAliases` + `UciResultsData.raceTeams` + chapa `TeamBadgeView` sin gate `showTeam` en `ResultsRowView`. Sin `.swift` nuevos → NO requiere `./setup.sh`. **`1060` = autoscroll del selector de etapas en Resultados.** En etapas avanzadas de una Gran Vuelta (p. ej. la 18 del Giro) el selector horizontal de etapas mostraba 1·2·3… y la etapa activa quedaba fuera de pantalla. `ResultsStageSelector` (`Views/Results/ResultsComponents.swift`) ahora envuelve el `ScrollView` en un `ScrollViewReader` y centra (`scrollTo(anchor:.center)`) la píldora activa al aparecer (sin animación) y al cambiar de etapa (animado). Mismo arreglo en Android (`278`). **`1059` / `3.0.1` = fix de renderizado de banderas en iOS.** El motor SVG del asset catalog de iOS maneja mal el patrón `<use xlink:href>` de instanciación de formas (flag-icons construye así las estrellas/emblemas reutilizando un grupo con rotaciones y `scale(-1 1)`): la geometría colapsa/duplica. Lo cazó Dani con la **bandera de Camerún** (`cm.svg`, estrella central deforme; `1058` la arregló sola). Se **aplanaron las 62 banderas** del set que usaban `<use>` → cada `<use>` se expande a una copia real del elemento referenciado, llevando su `transform`/`x`/`y` como `<g transform>` envolvente; geometría copiada **verbatim** (sin recalcular coordenadas → curvas/arcos/trazos intactos), `<defs>` huérfanos podados pero gradientes conservados (gt). Verificado con pixel-diff (cairosvg) = 0 en las 62. El `transform` plano sí lo renderiza bien iOS (la estrella aplanada de `cm` ya lo probaba). **Solo afecta a iOS** (Android/web usan librerías SVG completas → nunca tuvieron el bug; sus banderas no se tocaron). Android se queda en `3.0` (sin cambios; resincroniza en su próximo cambio). **`1057` = 2 bugfixes post-3.0 cazados por Dani:** (1) el separador Equipos/Corredores de la cabecera de inscritos se estiraba media pantalla — con la cabecera FIJA (sticky, fuera del ScrollView) un `Divider` vertical sin acotar hace la fila codiciosa en altura y el header compite con la lista (`Divider().frame(maxHeight: 32)`); (2) **banners AdMob de Hoy/competición bloqueaban la card anterior de forma aleatoria** — `AdBannerSlot` no manejaba el FALLO de carga (no-fill de la unidad real): el `BannerView` vacío se quedaba ocupando el hueco reservado e interceptando toques. Fix triple en `BannerAdView.swift`: delegado `didFailToReceiveAdWithError` → el slot **colapsa a alto 0** si nunca cargó (si falla un refresco posterior se conserva el último anuncio), guardia de tamaño degenerado (alto 0 = fallo), y `.allowsHitTesting(loadedHeight != nil)` → el banner **solo es interactivo cuando hay anuncio cargado**. **`1056` / `3.0` (EN MAIN) = apps 3.0: port completo de Resultados in-house UCI** (pantalla nativa `ResultsView` + CRE colapsada + filtro por equipo + trofeo→nativa + CTA jornada con "También en") **+ tachado de abandonos en inscritos + cabeceras sticky (Inscritos/Resultados) + km/desnivel/fecha en idioma de CONTENIDO + fix buscador→jornada en carreras de un día** (el gate `isStageRace` de `SearchView` mandaba las de un día a competición; ahora `SearchResult.oneDayRaceDayId`). Re-sincroniza `versionName` con Android 3.0. **Requiere `./setup.sh`** (4 `.swift` nuevos: `UciResult`, `UciResultsLogic`, `SupabaseService+UciResults`, `Views/Results/*` + test). Fix de infra: el target `CalendarioCiclismoTests` ahora declara `GENERATE_INFOPLIST_FILE` en `project.yml` → la suite corre vía `xcodebuild test` (antes solo desde Xcode; fallaba "does not have an Info.plist"). **2.4.0 = render temporal de equipos (arriba); requiere `./setup.sh` (nuevo `TeamSeason.swift`).** Build `1055` (último subido a Xcode Cloud = `1053`; `1054` lo asignó el fix de orden de salida, `1055` el render temporal). _En producción 3.0.1 (2026-06-11)._ App Privacy ya marcado (IDFA "Used for Tracking" + "Third-Party Advertising" — persiste, no hay que re-marcarlo cada build). **Numeración de build alineada con Xcode Cloud (2026-06-07):** el contador del proyecto saltó de `42` a `1052` para igualar el numerador automático de Xcode Cloud (cumple monotonía de App Store Connect). Desde aquí, cada commit iOS bumpea a mano siguiendo a Xcode Cloud (ver "iOS bump"). **Builds 1052 rechazados antes de 1053:** `ITMS-91064` (NSPrivacyTracking debía ser true con dominios) y `ITMS-91056` (manifiesto inválido por corrupción de `PrivacyInfo.xcprivacy` con `plutil -extract`, NO usar para validar — solo `plutil -lint`); ambos resueltos en `1053`. La corrección del **manifiesto de privacidad** (`PrivacyInfo.xcprivacy`): con ATT + anuncios personalizados de AdMob, `NSPrivacyTracking=true` y el `DeviceID`/IDFA pasa a `Tracking=true` + `Linked=true` + propósito `ThirdPartyAdvertising` (antes `false`/`AppFunctionality` → App Store Connect bloqueaba el envío a revisión: "tu app contiene NSUserTrackingUsageDescription… actualiza la respuesta sobre privacidad"). `NSPrivacyTrackingDomains` = `["googleads.g.doubleclick.net"]` (Apple exige ≥1 dominio si `NSPrivacyTracking=true`; el manifiesto del SDK GoogleMobileAds NO declara dominios, solo tipos de datos → hay que ponerlo nosotros; al rechazar ATT Apple bloquea ese host → ads no personalizados, no se rompen). **OJO:** error `ITMS-91064` "NSPrivacyTracking must be true if NSPrivacyTrackingDomains isn't empty" = build anterior con `Tracking=false`+dominios; con `Tracking=true`+dominio queda coherente. **En la web (App Store Connect → App Privacy) hay que marcar el IDFA como "Used for Tracking" + "Third-Party Advertising" — eso NO está en el repo.** `41` incluía el fix del gate de la pantalla "sin anuncios" para upgraders (ver abajo). **Guía simplificada de horarios** (despliegue inline en detalle de etapa, ver `docs/memory/simplified-guide.md`). **FASE C de anuncios (AdMob — SDK integrado):** SPM `GoogleMobileAds` 13.x + `GoogleUserMessagingPlatform` 3.x; banner adaptativo (`BannerAdView`/`AdBannerSlot`) gateado por `shouldShowAds` en los mismos tres sitios; orden UMP → ATT → `await MobileAds.shared.start()` en `AdsConsentManager`, disparado desde `AppDelegate` gateado por `shouldShowAds`. **Requiere `./setup.sh`** (deps SPM nuevas + `.swift` nuevos). Validar build en Xcode Cloud (SDK 13.x pide Xcode 26.2). Arrastra FASE A + todo lo de 2.2.x.
